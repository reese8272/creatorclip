# ── Web Framework ───────────────────────────────────────────────────────────
fastapi==0.120.4
# starlette is a transitive dep of fastapi but MUST be pinned explicitly. We hold
# it at the patched 0.49.1 — FastAPI 0.120.x's pin is >=0.40.0,<0.50.0, so 0.49.1
# is the newest compatible release; it closes GHSA-2c2j-9gv5-cj73 (multipart blocks
# the event loop) and GHSA-7f5h-v6xp-fcq8 (Range-header quadratic DoS). Do NOT pull
# starlette 1.x: it is a major-line jump (needs FastAPI 0.136.x) and the documented
# on_startup/on_shutdown landmine. The 1.x-only Host-header advisory (PYSEC-2026-161)
# is accepted-risk for now — see docs/DECISIONS.md 2026-05-29 (CVE remediation) entry.
starlette==0.49.1
stripe==11.4.0
uvicorn[standard]==0.32.1
httpx==0.28.0
slowapi==0.1.9
python-multipart==0.0.27

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
psycopg[binary,pool]==3.2.3
pgvector==0.3.2

# ── Cache / Queue ────────────────────────────────────────────────────────────
redis[hiredis]==5.2.0
celery[redis]==5.4.0

# ── Config / Security ────────────────────────────────────────────────────────
pydantic-settings==2.6.1
python-dotenv==1.2.2
cryptography==46.0.7
PyJWT==2.13.0
bcrypt==4.2.0

# ── Anthropic / Embeddings ───────────────────────────────────────────────────
anthropic==0.105.2
voyageai==0.3.2
# aiohttp is a transitive dep of voyageai. Pinned to 3.14.1 to close
# CVE-2026-34993 and CVE-2026-47265 (both fixed in >=3.14.0).
aiohttp==3.14.1
tenacity==9.1.4

# ── Audio Analysis ───────────────────────────────────────────────────────────
librosa==0.10.2
pyloudnorm==0.1.1

# ── Vision ───────────────────────────────────────────────────────────────────
opencv-python==4.13.0.92

# ── Subtitles / Animated captions (Issue 133) ────────────────────────────────
# pysubs2 generates ASS files for ffmpeg's `subtitles=` filter (libass).
# MIT-licensed, pure Python, no native deps. See clip_engine/captions.py.
pysubs2==1.7.3

# ── ML / Ranking ─────────────────────────────────────────────────────────────
lightgbm==4.6.0
scikit-learn==1.5.2
numpy==2.1.3
scipy==1.14.1

# ── Observability ────────────────────────────────────────────────────────────
# Golden-signal metrics (Prometheus exposition at /metrics). Official client; the
# correlation-id + structured-logging layer is hand-rolled in observability.py.
prometheus-client==0.25.0

# ── Storage (S3-compatible / Cloudflare R2) ──────────────────────────────────
boto3==1.35.54

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0

# ── Transcription (WhisperX) ─────────────────────────────────────────────────
# WhisperX is not on PyPI with a stable release. Install separately:
#   pip install git+https://github.com/m-bain/whisperX.git
# Or use the hosted fallback (Deepgram / AssemblyAI) via TRANSCRIPTION_BACKEND env var.
# deepgram-sdk==3.7.7
# assemblyai==0.33.0

# ── Video / Source Acquisition ───────────────────────────────────────────────
# ffmpeg binary must be installed at the OS level (see Dockerfile).
# yt-dlp is a ToS-risk convenience — off by default; see docs/COMPLIANCE.md.
# yt-dlp==2024.11.4
