# ── Web Framework ───────────────────────────────────────────────────────────
fastapi==0.115.4
# starlette is a transitive dep of fastapi but MUST be pinned explicitly: an
# unpinned install will pull starlette 1.x (Kludex maintainership line, released
# 2026-03-22) which dropped on_startup/on_shutdown kwargs from Router.__init__
# and is incompatible with FastAPI 0.115.x's >=0.40.0,<0.42.0 constraint.
# See docs/DECISIONS.md 2026-05-28 entry.
starlette==0.41.3
stripe==11.4.0
uvicorn[standard]==0.32.1
httpx==0.28.0
slowapi==0.1.9
python-multipart==0.0.20

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
psycopg[binary,pool]==3.2.3
pgvector==0.3.2

# ── Cache / Queue ────────────────────────────────────────────────────────────
redis[hiredis]==5.2.0
celery[redis]==5.4.0

# ── Config / Security ────────────────────────────────────────────────────────
pydantic-settings==2.6.1
python-dotenv==1.0.1
cryptography==43.0.3
PyJWT==2.9.0
bcrypt==4.2.0

# ── Anthropic / Embeddings ───────────────────────────────────────────────────
anthropic==0.40.0
voyageai==0.3.2

# ── Audio Analysis ───────────────────────────────────────────────────────────
librosa==0.10.2
pyloudnorm==0.1.1

# ── Vision ───────────────────────────────────────────────────────────────────
opencv-python==4.13.0.92

# ── ML / Ranking ─────────────────────────────────────────────────────────────
lightgbm==4.5.0
scikit-learn==1.5.2
numpy==2.1.3
scipy==1.14.1

# ── Storage (S3-compatible / Cloudflare R2) ──────────────────────────────────
boto3==1.35.54

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0

# ── Transcription (WhisperX) ─────────────────────────────────────────────────
# WhisperX is not on PyPI with a stable release. Install separately:
#   pip install git+https://github.com/m-bain/whisperX.git
# Or use the hosted fallback (Deepgram / AssemblyAI) via TRANSCRIPTION_BACKEND env var.
# deepgram-sdk==3.7.7
# assemblyai==0.33.0

# ── Video / Source Acquisition ───────────────────────────────────────────────
# ffmpeg binary must be installed at the OS level (see Dockerfile).
# yt-dlp is a ToS-risk convenience — off by default; see docs/COMPLIANCE.md.
# yt-dlp==2024.11.4
