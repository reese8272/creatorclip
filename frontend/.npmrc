# Issue 498 — make the node version a mechanism, not a remembered rule.
# jsdom breaks under node 26 (bit twice: OFF_COURSE_BUGS :37, :135). With
# engine-strict, npm REFUSES to install outside the "engines" range in
# package.json instead of warning. The pinned version lives in ../.nvmrc.
engine-strict=true
