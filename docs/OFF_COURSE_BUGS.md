# OFF_COURSE_BUGS — incidental defects found while doing something else

> A running log of bugs, fragilities, and surprises discovered **outside the scope of the
> task in flight**. The rule (see `CLAUDE.md` → *Off-Course Bugs*): when you hit one, **log
> it here in one line and keep going** — don't fix it inline unless it blocks the current
> task, and don't abandon the current task to chase it. Triage later: promote real defects
> into `docs/issues.md`, delete entries that turn out to be non-issues. This keeps the main
> pipeline on-course while ensuring nothing is silently brushed off.

> **2026-06-22 — Log slimmed.** All resolved/withdrawn rows (2026-05-29 → 2026-06-19) were
> moved to `docs/archive/off_course_bugs_snapshot_2026-06-22.md` (full history preserved).
> The three still-open rows below were promoted into the rebuilt `docs/issues.md` backlog and
> remain here until their issue closes.

## How to add an entry

One row per finding. Keep it short; link to the full issue once promoted. Severity uses
the same scale as the assessment rubric (BLOCKER / SEV1 / SEV2 / cleanup).

| Date | Found while | Bug / surprise | Severity | Status | Tracked / fixed in |
|------|-------------|----------------|----------|--------|--------------------|
| 2026-08-04 | Issue 391 — registering `clip_edit_documents` in the RLS integration suite | **The reused-connection RLS regression test could not catch the regression it exists to catch.** `test_reused_connection_guc_less_query_denies_cleanly` (Issue 354's own test) iterated a **hardcoded `("clips", "signals")`** instead of `_TENANT_TABLES`, so it only ever proved the two tables it was written against. That is why `0048` (`video_feedback`) and `0049` (`creator_style_notes`) sat undetected: both were written AFTER `0045` but copied the pre-`0045` template, leaving them the only tenant tables still on the bare `::uuid` cast (SQLSTATE 22P02 → a 500 instead of a clean deny on a reused pooled connection), and neither emitted a `GRANT`. Noted in LEFT_OFF's open list but **never actually logged here** — this row closes that gap too. | SEV2 (a security gate that passed vacuously on 2 of 17 tenant tables) | ✅ FIXED 2026-08-04 (Issue 391, PR A, commit `5195120`) | The sweep now iterates `_TENANT_TABLES + _CHILD_TABLES` and fails with the offending table name plus the required `NULLIF` form; every newly-listed table is also **seeded**, because the clean-deny assertion is vacuous on an empty table. Migration `0052` repairs both policies and adds the missing grants; `downgrade()` restores the bare form so it stays a true inverse. See `docs/DECISIONS.md` 2026-08-04. |
| 2026-08-04 | Watching the PR #70 production deploy (migration `0051`) | **Production migrations run with NO pre-migration safety dump.** `deploy.yml`'s "Pre-migration safety dump (Issue 257)" step succeeded, but annotated: *"No `BACKUP_R2_BUCKET` configured — skipping pre-migration dump (Issue 256 not yet activated). Migrating WITHOUT a safety dump."* The step is wired and the guard is honest, but the env var has never been set, so every prod migration to date has run unprotected. `0051` was additive + nullable so nothing was at risk here — the concern is the next one that isn't. | SEV2 (no rollback point for a destructive migration; latent until one exists) | 📋 Open — **Issue 256** already exists for it; this is a note that it is now load-bearing | Set `BACKUP_R2_BUCKET` on the VM `.env` before shipping any migration that drops, renames, or rewrites data. **#391's `0052` is the next migration in the queue.** |
| 2026-08-03 | Issue 390 — writing a regression test for the shortcut bus | **The video player's scrub-bar arrow keys had NEVER worked.** `ScrubBar` paired its own ±1s `onKeyDown` with an `onKeyDownCapture` that called `stopPropagation()`. Stopping propagation in the CAPTURE phase also prevents the target's own bubble handler, so the ±1s never ran — and the native event never reached the document listener either, so the ±5s did not run as a fallback. Pressing an arrow on a focused scrub bar did nothing at all. Found only because the test was written from the WRONG hypothesis (a double-seek) and failed reporting zero movement. | SEV2 (a documented control silently inert) | ✅ FIXED 2026-08-03 | **Issue 390** — capture handler removed; `preventDefault` plus a `defaultPrevented` bail on both outer layers composes correctly. Regression test in `video-player.test.tsx`. |
| 2026-08-03 | Issue 390 — adopting `useTimelineViewport` in the master timeline | **`clientXToTime` derived zoom from STATE width while its siblings read the live rect.** Before any ResizeObserver had fired the state width is 0, which collapses `fit` to `MIN_PX_PER_SECOND` and maps every pointer x to the END of the clip (x=10 on a 300s source returned 300s, not 30s). Latent only because no component consumed the hook yet. | SEV2 (silent wrong answer in pointer→time conversion) | ✅ FIXED 2026-08-03 | **Issue 390** — reads the live rect like `applyZoom`/`scrollByPx` do. |
| 2026-08-03 | Issue 390 — adding the adaptive ruler | **A structural gate false-positived on an explanatory comment, for the second time.** `no-synthetic-waveform`'s "only Waveform.tsx paints" rule scanned RAW FILE TEXT for `getContext('2d')`, so `TimelineRuler.tsx`'s comment explaining why it is deliberately NOT a canvas failed the gate. The same failure mode cost a check that was deleted during #392. | cleanup (gate ergonomics; no product defect) | ✅ FIXED 2026-08-03 | **Issue 390** — rule is now AST-based (`CallExpression` whose callee property is `getContext`), which cannot see comments and is strictly stronger. |
| 2026-08-03 | Issue 389 — running the Layer-0 gate before close-out | **`pip-audit` went 0 → 7 findings across 3 pinned packages, with no dependency change on our side.** `aiohttp 3.14.1` (CVE-2026-59881, -69243 → fix 3.14.2; CVE-2026-69244 → 3.14.3), `cryptography 48.0.1` (CVE-2026-69247 → 50.0.0; -69248, -69249 → 49.0.0), `pytest 8.3.3` (PYSEC-2026-1845 → 9.0.3). Verified NOT a regression from this branch: `git diff main -- requirements*.txt '*.py'` is empty, so these are advisories published since the Batch A baseline (which measured pip-audit 0). **`cryptography` is beta-critical** — it backs the Fernet `TOKEN_ENCRYPTION_KEY` path for OAuth refresh tokens. `aiohttp` is on the request path. | SEV2 (known CVEs in prod dependencies; no known exploit path confirmed in our usage) | ✅ **FIXED 2026-08-04 — Issue 406** (aiohttp 3.14.1→3.14.3, cryptography 48.0.1→50.0.0; pip_audit gate back to 0 with NOTHING added to ignore-vulns) | Not fixed here: bumping three pinned deps mid-issue is a separate change with its own test surface, and Issue 389 touches no Python. Layer 0 is otherwise green (ruff 0 · mypy 0 · coverage 83.25 · bandit 0/0). **Triaged 2026-08-04 while opening PR #70:** the gate reads **6**, not 7 — the pytest advisory is already justified in `[tool.pip-audit].ignore-vulns`. Of the 6, **none has a live exposure path**: the two aiohttp WebSocket CVEs need a server or a live socket (Deepgram is prerecorded REST, `ingestion/transcribe.py:130-150`), and all three cryptography CVEs are X.509 path-validation / PKCS#7, while we import only `Fernet`/`MultiFernet`. Fix is cheap — aiohttp is transitive-only and unconstrained (`deepgram-sdk>=3.9.1`, `voyageai` unpinned); `cryptography` has `Required-by:` nothing. See Issue 406 for the per-CVE table. |
| 2026-08-03 | Issue 389 — building the tool-route app shell | **The Editor transcript scroll region was a latent `scrollable-region-focusable` failure.** `ShortFormEditor`'s transcript is `overflow-y-auto` and contains only plain `<span>`s — no natively focusable child — which axe flags at SERIOUS impact (WCAG 2.1.1/2.1.3). It passed the gate only because the test fixture is two words long and never actually overflowed the old `max-h-[200px]`. Any real transcript would have failed it. | SEV2 (a11y: keyboard users cannot scroll the transcript) | ✅ FIXED in 389 | `tabIndex={0}` added; the `[data-tool-scroll]` rule is now written into `docs/UI.md` so every future region inherits it. |
| 2026-08-03 | Issue 389 — building the tool-route app shell | **`mergeAdjacent` mutates caller-owned cut objects.** `ShortFormEditor.tsx` (and its duplicate in `review/TranscriptEditor.tsx`): `cuts.slice()` is a SHALLOW copy, so `out[0] === cuts[k]`, and `last.end_s = Math.max(...)` writes through to the object the caller still holds. Consequence today: `setUndo(cuts)` immediately before `setCuts(mergeAdjacent([...cuts, newCut]))` stores an "undo snapshot" whose element has already been mutated by the merge — **the existing single-level undo is already silently wrong for any merged cut.** Under #391's command stack every history entry would alias the same objects and undo would become a no-op. | SEV2 (silent data-correctness bug in undo) | ✅ FIXED 2026-08-03 | **Issue 390** — `mergeAdjacent` moved to `lib/editorCuts.ts` and rebuilt as a pure function; the test freezes its inputs so the old code throws in strict mode. |
| 2026-08-03 | Issue 389 — building the tool-route app shell | **Cuts are keyed and targeted by array index while `mergeAdjacent` re-sorts by `start_s`.** `key={idx}`, `removeCut(idx)` and `aria-label={`Cut ${i+1}`}` all assume stable positions. Once #390 adds draggable cut edges, dragging one cut's edge past another re-sorts mid-gesture: the drag detaches and, with pointer capture held, the user keeps dragging a *different* cut. | SEV2 (will be user-visible the moment edge-drag ships) | ✅ FIXED 2026-08-03 | **Issue 390** — `EditorCut.id`, keyed and targeted by identity; merging is also deferred to gesture completion so a neighbour cannot be absorbed mid-drag. |
| 2026-08-03 | Issue 389 — building the tool-route app shell | **`vitest` never restores spies and the ResizeObserver stub could not fire.** No `restoreMocks` in `vite.config.ts`, and `Timeline.test.tsx:7-12` spies on `Element.prototype.getBoundingClientRect`, returning the SAME 400×80 rect for every element for the rest of the file. Combined with a no-op `ResizeObserver`, any width-driven assertion (#390's zoom-to-fit, playhead-in-view) would pass for the wrong reason — `viewportWidth === contentWidth` is unconditionally true. | SEV2 (test harness gives false confidence) | ✅ FIXED 2026-08-03 | RO fake fireable (389); **Issue 390** added `restoreMocks: true` to `vite.config.ts` and replaced the prototype-wide rect spy with per-element stubs in the new `src/test/rect.ts`. |
| 2026-08-03 | Issue 386 — axe baseline spike before adding routes to the gate | **`/settings` has pre-existing serious contrast failures** (2.14–2.54:1 vs 4.5 required) from the "Soon" preview rows added in `80a7474` (2026-06-23): decorative disabled segmented-control mocks wrapped in `pointer-events-none opacity-50`, which halves effective contrast while leaving the fake controls in the accessibility tree — a screen reader announces controls that do not work. Likely one-attribute fix (`aria-hidden` on the mock, since it is purely decorative), but it is a different page and a different issue, so `settings` was NOT added to `e2e/a11y.spec.ts`; `editor` was (it passes clean). | SEV2 | ✅ FIXED 2026-08-04 — the one-attribute theory was WRONG: axe checks visible text for contrast regardless of `aria-hidden`. Fixed by `aria-hidden` (AT correctness) + dropping `opacity-50` so muted tokens carry disabled-ness at full AA contrast; `/settings` added to the axe gate (24/24 green) | **Issue 411** |
| 2026-08-03 | Issue 400a — writing the design-token contract test | **`App.tsx`'s `RootError` crash-recovery screen was written entirely in shadcn default token names** (`bg-background`, `text-foreground`, `text-muted-foreground`, `bg-primary`, `text-primary-foreground`, `border-border`) that this project never declares — so the screen shown *after the SPA has already crashed* rendered with no background, no foreground colour and a fill-less Reload button. Found by the new gate, not by review: the names look plausible. Also `ActivityPanel`'s `border-border`. | SEV2 | FIXED in 400a | Batch A (L25) |
| 2026-08-03 | Issue 385 — installing Radix deps | **Local Node was unpinned; CI pins 22.** Brew reinstall moved node 24.4.0 → 26.5.1, under which jsdom stops exposing a global `localStorage` and `Walkthrough.test.tsx:23` fails with `Cannot read properties of undefined (reading 'clear')` — verified pre-existing (fails identically on a clean tree), passes on node 22. Added `frontend/.nvmrc` = 22 to match `ci.yml`'s three `node-version: "22"` jobs. Consider a `package.json` `engines` field so a wrong local Node fails loudly instead of via one confusing test. | cleanup | .nvmrc added; engines pin open | Batch A (L25) |
| 2026-08-03 | Issue 385 — installing Radix deps | **Homebrew npm 11.5.2 on node 24.4.0 could not install anything** — `npm install`/`ci`/`cache verify` all exited 1 with an empty error and left a half-extracted `node_modules` with no `.bin`. Environment-level (reproduced in an empty scratch dir), not project-level. Fixed by `brew reinstall node`. Full diagnosis + the ruled-out hypotheses in `~/.claude/ISSUES_LOG.md` ISSUE-2026-08-03-01. | cleanup | RESOLVED | ISSUES_LOG.md |
| 2026-08-03 | Brutal-honesty review of the editor + presentation layer | **The primary input path rejects most real creator footage.** `UPLOAD_MAX_MB = 500` (`config.py:521`), but a 20-min 1080p OBS recording is routinely 1–3 GB and 4K screen capture passes 500 MB in minutes. Because the YouTube ToS makes raw upload **mandatory** (we never download from a link), the compliant architecture and the cap are in direct conflict. Mechanism compounds it: single-shot XHR multipart POST **through the FastAPI app server** (`routers/videos.py:336`, `UploadVideoForm.tsx`), holding a worker for the whole transfer, with **no resume** — a blip at 94% restarts from zero. Progress also reads 100% when bytes reach the app server, then the user waits through the R2 leg with no feedback. | **BLOCKER** (beta: core upload journey fails for typical source files) | 📋 Open | Promoted to **Issue 395** (`docs/issues.md`, Lane L25 Batch C): presigned multipart direct to R2, 5–10 MiB parallel parts, cross-session resume, cap raised/removed. See DECISIONS 2026-08-03. |
| 2026-08-03 | Brutal-honesty review of the editor + presentation layer | **The long-form "Source timeline" shows a fabricated waveform.** `frontend/src/components/editor/LongFormEditor.tsx:131-138` draws 48 bars at height `20 + ((i * 37) % 60)%` — a deterministic arithmetic pattern with no relationship to the audio — under a label reading "Source timeline", over sources up to 22 minutes (`frontend/e2e/__screenshots__/desktop-editor-long.png`). A creator will read those peaks as loud moments and navigate by them. This is the one item in the sweep that goes past "unfinished": it asserts something false about the user's own content, on the surface whose job is helping them find moments. The short-form editor does a real WebAudio decode (`pages/Editor.tsx:180-203`) and falls back to an honest 20%-opacity placeholder (`Timeline.tsx:254-267`) — that fallback is the correct pattern to copy. | SEV2 (misleading data on a navigation surface; no data loss) | ✅ FIXED 2026-08-03 | **Issue 392** — real BBC-format peaks computed at ingest and served per source; absent peaks render a flat track that says "Waveform unavailable". Source-scanning gate `no-synthetic-waveform.test.ts` keeps the generator from returning (verified it fires). |
| 2026-08-03 | Brutal-honesty review of the editor + presentation layer | **Review action links render collided: "rewrite hook**Suggest caption**".** `frontend/src/components/review/WhyThisClip.tsx:98` and `:181` are two inline `<button>` elements rendered as siblings with `mt-2` / `mt-1` — margin-top has no effect on inline elements — and no separator between them, so they run together with no space. Visible as shipped in `frontend/e2e/__screenshots__/desktop-review.png`. Two distinct AI actions read as one broken string. | cleanup (cosmetic, but on a primary surface and visible in the shipped screenshot) | ✅ FIXED by Issue 388 (2026-08-03, PR #69) — row left Open by mistake until the 2026-08-04 assessment caught it | **Issue 388** (`docs/issues.md`, Lane L25 Batch A): action row rebuilt as a flex row with a real gap; regression test asserts the two actions are separated. |
| 2026-07-02 | Watching CI on PR #39 | **Playwright + visual-regression CI jobs always fail on the self-hosted runner** — `npx playwright install --with-deps chromium` exits 1 (no passwordless sudo), and `libatk-1.0.so.0` is missing so chromium crashes at launch. Same failure present on every merged PR including #38. The jobs fail-fast before any test runs, so results carry no signal about actual app behaviour. | SEV2 (CI noise; no code regression is masked because 0 tests run) | 📋 Open | Same root cause as libGL/libgomp gaps (runner needs system libs pre-installed). Needs a runner maintenance task or docker-in-docker for Playwright. Track in docs/issues.md. |
| 2026-06-30 | Debugging a prod "render loop" (clip stuck on "Rendering your clip…" spinner) | **`ClipPlayer.triggerRender` latches the render spinner forever.** It sets `requesting=true` but only resets it on the non-409 *error* path — a 202 (or 409) success never clears it. The spinner shows while `requesting \|\| render_status==='running'` and clears only when `render_uri` lands, so any render that never produces a `render_uri` spins forever, masking the real terminal state. Triggered live by the backend gap below: clip `ebd4cee8` failed permanently (`render_status=failed`, source purged), but the UI sat on the spinner instead of "Render failed". Confirmed via prod worker log (`render_clip_failed_permanent exc_type=ValueError 'Source video not available'`) + DB (`clips.render_status=failed`, never `running`). | SEV2 (UX: clip appears to render forever; no error, no recovery) | ✅ Fixed 2026-06-30 (frontend) | `triggerRender` now resets `requesting` in a `finally`, so the spinner is driven by server `render_status` — a fast-failing render surfaces as "Render failed" + retry. Regression test `ClipPlayer.test.tsx` "does not latch the spinner after a render request settles". |
| 2026-06-30 | Rung-4 live test: uploaded a real video + watched the pipeline | **CORE LOOP BROKEN — no uploaded video could ever render.** `ingest_video` overwrote `video.source_uri` with the extracted audio WAV and **deleted the original mp4** seconds after ingest. Render reads `source_uri`, so it downloaded audio-only and the 9:16 reframe's keyframe grab failed: `ffmpeg -i …wav -vframes 1 …jpg → "Output file does not contain any stream"`. Every clip → permanent `failed`; explains the historical **0/18 clips ever rendered**. Auto-render enqueue + worker pickup WORK — the source was the problem. Rungs 1–3 (local repro + live-smoke canary) all passed because they seed a real *video* as `source_uri`; only rung 4 exercised the real ingest→source=audio path. (Supersedes the earlier "source purged at 72h" framing — the video was discarded at ingest in seconds.) | **SEV1** (core upload→playable-clip loop non-functional in prod) | ✅ Fixed 2026-07-01 (migration `0039`) — needs deploy + re-upload | **migration `0039_video_audio_uri`**: added `videos.audio_uri`; ingest now stores the WAV on `audio_uri` and LEAVES `source_uri` = the video (no overwrite, no immediate delete). `transcribe`/`signals` read `audio_uri`; render reads `source_uri`. `purge_stale_source_media` purges both video + audio at 72h after `ingest_done_at` — matching COMPLIANCE.md. Reverses the now-obsolete Issue 110 Fix D (no overwrite ⇒ no orphan). Verified: `scripts/repro_ingest_render.py` (real ffmpeg ingest → render `done`), integration test in `test_worker_pipeline.py`, inverted static guard `test_issue_110.py::test_ingest_async_retains_source_video`. Existing clips unrecoverable (videos already deleted) → creators re-upload. See DECISIONS 2026-07-01. |
| 2026-06-30 | Debugging a prod `oauth_failed` sign-in error (error.png) | **The Issue 343 RLS role split broke ALL sign-ins.** The OAuth `/callback` runs PRE-auth: it creates the creator (RLS-exempt `creators`) then, in the SAME bare-session transaction, writes RLS-FORCED tenant tables — `youtube_tokens` (store_or_update_tokens, every sign-in) and `minute_packs`/`minute_deductions` (grant_minutes, new signups). There is no `get_current_creator` in this path, so db.py's `after_begin` listener never sets `app.creator_id`. Once the app started connecting as the non-BYPASSRLS `creatorclip_app` role (Issue 343, deployed 2026-06-30), those writes hit the `tenant_isolation` WITH CHECK against a NULL GUC → SQLSTATE 42501 → `ProgrammingError`, swallowed by the callback catch-all as `/app/login?error=oauth_failed`. Confirmed in prod logs (`OAuth callback failed: ProgrammingError`). 343's prod verification checked SELECT fail-closed + the RLS-exempt bootstrap but never exercised the callback's pre-auth WRITE path. | SEV1 (prod: complete sign-in outage, new + returning creators) | ✅ Fixed 2026-06-30 — needs redeploy | `routers/auth._exchange_and_persist` emits `SELECT set_config('app.creator_id', <creator.id>, true)` right after the flush (once the creator id exists), so the subsequent tenant-table writes satisfy RLS — scoped to that one creator, deny-by-default intact. Unit guard: `test_callback_sets_creator_rls_context_before_tenant_writes` (tests/test_auth.py). Integration proof under the real role: `test_oauth_callback_tenant_write_requires_guc` (tests/test_rls_isolation_integration.py) — insert fails without the GUC, succeeds with it. Full unit suite 2011 passed. Related Issue 343, [[Issue 344]] (the sibling authenticated-write fix). |
| 2026-06-30 | L21 Issue 340b RLS deny-by-default test (parallel build wave) | **Two tenant tables have RLS DISABLED:** `improvement_briefs` (added in migration 0009) and `creator_insights` both carry a `creator_id` but `relrowsecurity=false` — they were never added to migration 0010's `_TENANT_TABLES` list, so unlike clips/videos/dna_embeddings they have NO `tenant_isolation` policy. With the **Issue 343 role split now active** (app connects as `creatorclip_app`, `bypassrls=false`), RLS only protects tables that HAVE a policy — so these two leak cross-tenant at the DB layer; isolation rests **entirely** on app-level `WHERE creator_id=` filters with no deny-by-default backstop (the exact gap Issue 340 / Issue 79 set out to close). The 340b unset-context proof correctly EXCLUDED them (they fail the zero-rows assertion) and flagged it. | SEV2 (security: missing RLS backstop on 2 tenant tables; app-layer filter is sole protection) | ✅ Fixed 2026-06-30 — migration `0038` | **migration `0038_rls_improvement_briefs_creator_insights`** mirrors 0010: `ENABLE`+`FORCE ROW LEVEL SECURITY` + `tenant_isolation` policy (`USING creator_id = current_setting('app.creator_id', true)::uuid`) on both tables, re-grants DML to `creatorclip_app`. Both tables added to `_TENANT_TABLES` in `tests/test_rls_isolation_integration.py` so the cross-tenant + deny-by-default unset-context proofs now cover them (4 RLS tests green; full suite 2156 passed). Related [[Issue 340]], Issue 343. |
| 2026-06-30 | Running the Issue 341 smoke harness on the **prod VM** (`--seed`), `isolation` check | **Per-creator RLS isolation is NOT enforced in prod.** The harness `isolation` check failed: under a *different* creator's RLS context, the canary's clip was still visible (no cross-tenant filtering). Root cause (verified on the live DB): the app DB role **`creatorclip` has `rolbypassrls=true`**, and a `BYPASSRLS` role overrides even `FORCE ROW LEVEL SECURITY` (the tables ARE correctly `rls=true force=true owner=creatorclip`). So the RLS defense-in-depth backstop designed in Issue 79 / required by Issue 340 is inactive in prod — isolation currently rests **entirely** on app-level `creator_id` WHERE-clause filters; any query that omits one would leak cross-tenant with no DB backstop. The intended "prod role split" (app role WITHOUT bypassrls + separate privileged migration/worker role) was never completed (`db.py` comment: "until the prod role split lands"). No unit/integration test caught this — it only shows up against the live role. | SEV1 (security: load-bearing tenant isolation backstop disabled in prod) | ✅ Fixed in Issue 343 (2026-06-30) — VERIFIED on prod | **Activated the Issue 79 role split.** App now connects as `creatorclip_app` (`bypassrls=false`); `DATABASE_MIGRATION_URL` → the superuser `creatorclip` (BYPASSRLS) for Alembic + the 50 worker `AdminSessionLocal` cross-tenant sweeps + the 1 worker app-session (already sets `creator_id`). Verified live as `creatorclip_app`: no-context→0 videos (fail-closed), wrong-tenant canary→0 (harness `isolation` PASS), real creator under context→1049 (correct per-tenant), `creators` (RLS-exempt) visible for the auth bootstrap; app+worker healthy post-cutover. Kept the superuser as the migrate role (skipped the runbook's ownership-transfer to `creatorclip_migrate`) — see DECISIONS 2026-06-30. |
| 2026-06-29 | First live run of the Issue 341 smoke harness (`--with-llm`) against the real Anthropic API | **The per-clip LLM generators crash on the real API: `claude-sonnet-4-6` returns valid JSON wrapped in a ` ```json ` markdown fence, and `_parse_result` does a bare `json.loads(raw)` → `JSONDecodeError: Expecting value: line 1 column 1 (char 0)`.** Confirmed on `knowledge/clip_titles.py`, `clip_captions.py`, `clip_explain.py` (all three identical: `data = json.loads(raw_json)` with no fence strip / structured-output constraint). These power the **deployed** Review-surface features (Issues 322/323/325). Mocked unit tests never caught it because the mocks return clean unfenced JSON — the exact gap a live test exists to close. Likely intermittent in prod (fencing is stochastic), so it reads as "AI suggestions temporarily unavailable" rather than a hard outage. Same latent risk in every other `json.loads`-on-LLM-text site (`scoring.py`, `knowledge/titles|hooks|thumbnails|chapters`). | SEV2 (deployed Review LLM features broken/flaky against the real API) | ✅ Fixed in Issue 342 (2026-06-29) | **Two-track fix shipped + LIVE-verified.** Track A (no web_search) → native structured outputs (`output_config.format`) on `clip_titles`/`clip_captions`/`clip_explain` (verified at SDK 0.105.2 against the real API: fence-free, `json.loads` clean). Track B → `extract_json_block` on `hooks` (web_search→citations, structured output 400s), `chapters`, `scoring`, `thumbnails`; the clip_* trio also routes through the helper as defense-in-depth. Regression: `tests/test_llm_fence_parsing.py` feeds fenced JSON to each parser (the unit-lane gap). The Issue 341 smoke harness `--with-llm` now passes title/caption/explain (was 0/1 fail). |
| 2026-06-17 | Running the suite after the Issue 143 starlette 1.3.1 bump | `fastapi.testclient`/`starlette.testclient` now emits `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.` on every TestClient construction. Tests pass, but it's noise and a future migration signal (httpx → httpx2 for the test client). | cleanup (test-infra noise) | ✅ Fixed in Issue 274 | `httpx==0.28.0` replaced with `httpx2==2.4.0` in `requirements.txt` — starlette 1.3.1 prefers httpx2 when installed; warning resolves on install. |
| 2026-06-18 | Porting the dashboard to React (Issue 85c) | The dashboard fetches clip counts with one `GET /videos/{id}/clips` request **per done video** (N+1) — both the vanilla page and the React port do this (port parallelises via `useQueries`). For a creator with many processed videos this is N round-trips on every dashboard load. | SEV3 (perf, bounded by done-video count; not user-blocking) | 📋 Open | Promoted: folded into the per-video clips-map issue (batched `GET /videos/clips/counts` endpoint replaces the N+1). Backend change. |
| 2026-07-01 | Issue 349 ruff check on worker/tasks.py | **Pre-existing E501 line-too-long at `worker/tasks.py:4750`** — honesty disclaimer string literal in `_build_inapp_notification` is 101 chars (limit 100). Not introduced by Issue 349; was already in the codebase. | cleanup | 📋 Open | Log only — out of scope for Issues 348–350. |
| 2026-06-19 | Issue 164 live-site paid-flow run (`npm run test:prod:flows`) | **Video-analysis + title-optimizer flows timed out at 60s on the real account** (chat flow passed). Could be genuinely slow LLM generation (a UX gap — long spinner) or a real latency issue; one timeout isn't conclusive. Not chased further to avoid extra paid runs. | SEV3 (needs investigation) | ✅ Fixed in Issue 274 | Global timeout raised to 120s in `playwright.config.prod.ts`; flow assertions now check HTTP 200 response status (deterministic) rather than fully-rendered output, removing sensitivity to slow LLM rendering while still catching endpoint failures. If endpoints genuinely exceed 120s, file a dedicated perf issue. |
| 2026-06-29 | Verbose-logging task — owner surfaced 3 field bugs via prod screenshots (autoclip.studio) | **Render retry-storm:** the Review screen sits on "Rendering your clip… (~30s)" indefinitely while `render_clip` keeps retrying (max_retries=3, 60s delay) a render that won't succeed. | SEV2 (core Review UX; wasted compute/minutes) | ✅ Fixed 2026-06-29 | `render_clip` now classifies errors like `build_dna`: permanent failures (`ValueError`/`FileNotFoundError` — missing clip/source, bad range) are TERMINAL (set `failed`, no retry) instead of burning 3 retries; only transient errors retry. `_render_clip_async`'s error emit no longer falsely says "retrying". Tests: `tests/test_render_retry.py`. Verbose ffmpeg `_run` + Celery `task_retry`/`task_failure` logging capture the root cause when a real transient failure recurs. |
| 2026-06-29 | Same — chat screenshot | **Chat "Connection lost.":** the Pro assistant SSE drops and the UI dead-ends on a red "Connection lost." banner with no auto-reconnect/retry. | SEV2 (Pro chat unusable on drop) | ✅ Fixed 2026-06-29 | Root cause was client-side: `subscribeToChatStream` called `es.close()` on the FIRST transport `error`, killing EventSource's built-in auto-reconnect. Now a transport drop is transient — native auto-reconnect (CONNECTING) is allowed, a CLOSED socket is reopened with backoff, and only a server-sent named error or exhausting `MAX_CHAT_RECONNECTS` surfaces a terminal error. `Chat.tsx` shows a soft "Reconnecting…" state, preserves partial streamed text on terminal failure, and offers a Retry button. Server already sends `: keepalive` + `Last-Event-ID` resume (no change needed). Tests: `frontend/src/lib/taskStream.test.ts`. |
| 2026-06-29 | Same — job-list screenshot | **Stale FAILED jobs persist:** `FAILED` transcription rows linger with no way to act on the "please try again" message — `ActionCell` returned `null` for failed and `POST /videos/{id}/queue` rejected anything not `pending`. | SEV3 (UX clutter; not blocking) | ✅ Fixed 2026-06-29 | `/videos/{id}/queue` now accepts a `failed` video: resets it to `pending`, clears `failure_reason`, and re-runs the pipeline (re-deduction idempotent via `MinuteDeduction` UNIQUE). `VideoTable` shows a **Retry** button for failed+clippable rows (upload-path hint for failed rows with no stored source). Tests: `tests/test_issue_125.py::test_queue_endpoint_retries_failed_video`. |
| 2026-06-28 | Auto-render feature — running `ruff format --check worker/tasks.py` | `worker/tasks.py` carried pre-existing `ruff format` drift (hunks far from the auto-render edit — lines ~203, 758, 2347, 2373, 2611, 4314). The changed-file format gate would have failed, so the file was reformatted whole as part of this change; the incidental hunks are whitespace-only. | cleanup (format drift) | ✅ Fixed incidentally | Reformatted in the auto-render commit; no behavior change. |
| 2026-06-28 | Auto-render feature — running the integration lane (`-m integration`) on the web box | `test_generate_clips_*_integration.py` (and other integration tests) fail with `psycopg.OperationalError: password authentication failed for user "creatorclip"` — the SessionStart-provisioned Postgres credentials don't match the test DSN in this remote env. Unit lane (the verified gate here) is unaffected. | SEV3 (env/CI config, not product) | ✅ Resolved in Issue 344 (2026-06-30) | Root cause: the SessionStart hook's role-creation step silently failed (only `postgres`/`postgres` existed). Provisioned `creatorclip:dev_password` SUPERUSER + db + pgvector + `alembic upgrade head` (mirrors `scripts/dev_session_setup.sh`); the full integration lane then ran live (134 tests). The remaining bit-rot failures were real and are fixed in Issue 344 (see new row below). |
| 2026-06-28 | Auto-render feature — full unit lane | `tests/ingestion/test_transcribe.py::test_default_transcription_backend_sdk_is_installed` fails because the `deepgram` SDK isn't installed in this venv (default `TRANSCRIPTION_BACKEND=deepgram`). Pre-existing; unrelated to auto-render. | SEV3 (env/dep) | 📋 Open | Not chased — `pip install deepgram-sdk` (or pin in requirements) on this box; out of scope for auto-render. |
| 2026-06-29 | Batch-render task — running the integration lane (`-m integration`) on the web box | `test_worker_pipeline.py::test_poll_clip_outcomes_uses_per_creator_median` fails (`RuntimeError: Event loop is closed` in teardown) **on base too** (verified via `git stash`) — unrelated to the batch-render change. Pre-existing async-fixture teardown sensitivity in this test. The other 5 integration tests in the file (incl. the new `test_render_video_clips_downloads_source_once` and the render idempotency test) pass once the `creatorclip` PG role is provisioned. | SEV3 (test-infra flakiness) | ✅ Fixed in Issue 344 (2026-06-30) | Two root causes: (1) the test asserted the **obsolete** full-video `VideoMetrics` median — Issue 201 moved `performed_well` to a ≥3 **comparable-Shorts** baseline, so it now seeds 3 comparable Shorts/creator; (2) the "Event loop is closed" / silent skip was a **leaked session-level advisory lock** (a prior poll test's `pg_advisory_unlock` `finally` is skipped when its rollback raises on a dead-loop connection → later poll on a different pooled backend gets `acquired=False`). `tests/conftest.py` now clears leaked advisory locks before every integration test, generalizing this file's `_reset_admin_pool` fixture lane-wide. |
| 2026-06-30 | Standing up the live integration lane (Issue 344) | **The integration lane had bit-rotted — 21/134 failing — because CI's `integration` job sat red/unenforced and nothing ran it locally.** Three classes: (a) stale **mock stubs** drifted from the real signature — `generate_brief`/`generate_improvement_brief` now return `(text, usage)` but stubs returned a bare string → `ValueError: too many values to unpack`; (b) tests didn't seed the **minutes/billing gate** (`check_positive_balance`) or the **Issue 206 `payment_status=='paid'`** webhook guard added after they were written → 402/`ignored`; (c) a real **compliance bug**: `AuditLog.before_jsonb/after_jsonb` stored the JSONB `'null'` literal (SQLAlchemy `JSON` default) instead of SQL NULL, so the never-purged `creator.deleted` audit failed the Issue 247 "no PII payload retained" `IS NULL` assertion. | SEV2 (test-fidelity + 1 compliance correctness bug) | ✅ Fixed in Issue 344 (2026-06-30) | (a) conformed every stub to the real `(text, usage)` contract; (b) seeded `minutes_balance`/`payment_status`; (c) **product fix** — `JSONB(none_as_null=True)` on both audit columns so `None` → SQL NULL (no DDL change). Added per-integration-test DB isolation (TRUNCATE + advisory-lock clear) so the lane is order-independent. Full lane: 134 passed (det. + 2 random seeds); unit lane 1774 passed, 0 failed. |
| 2026-06-30 | Diagnosing the Issue 344 advisory-lock test leak | **Product concern (not just test-infra): the Beat tasks' session-level `pg_advisory_lock` release can be skipped.** `_poll_clip_outcomes_async` / `_refresh_youtube_analytics_async` acquire `pg_try_advisory_lock` and release in a `finally` that does `rollback()` then `pg_advisory_unlock`; if the connection is unhealthy the `rollback()` itself can raise, skipping the unlock and leaking the lock onto the pooled backend until `pool_recycle` (or process exit). In prod a leaked lock makes every future poll on that backend log "advisory lock held — skipping" and no-op until the connection recycles — delayed outcome polling / analytics refresh, self-healing within the recycle window. | SEV3 (prod: bounded, self-healing; observability gap) | ✅ Fixed 2026-07-29 (w2/worker-tail eff0d28) | Triage: switch to `pg_advisory_xact_lock` where per-creator commit isn't required, or wrap the unlock so a failed rollback still attempts it on a fresh connection; add a metric/log when a Beat task skips on a held lock. Test lane is already protected (conftest advisory-lock clear). |
| 2026-06-23 | Issue 235 — adding `_is_first_keep` EXISTS query in `routers/review.py` | Existing `test_review.py` mocks use `session.execute = AsyncMock(return_value=result)` where `result` is a `MagicMock()`. When `_is_first_keep` calls `result.scalar()`, it gets a coroutine from AsyncMock that is never awaited — producing a `RuntimeWarning: coroutine 'AsyncMockMixin._execute_mock_call' was never awaited`. Tests still pass (coroutine truthiness makes `not result.scalar()` evaluate correctly), but it's noisy. Fix: update `test_review.py` fake_session to return a `MagicMock` with `scalar.return_value = True` for the EXISTS result. | cleanup (test-infra warning) | ✅ Fixed 2026-07-29 (w2/test-flakes) | Actual emitter was tests/test_feedback_tags.py (test_review.py already used owned_lookup_result); _fake_session now routes session.execute through owned_lookup_result; vestigial session.get removed. File RuntimeWarnings 3→0. |
| 2026-06-22 | Running the Layer-0 gates locally (GH Actions out of minutes) during the backlog-rebuild close-out | **`pip-audit` reports 1 non-baselined CVE: `msgpack` 1.1.2 `GHSA-6v7p-g79w-8964` (fix 1.2.1).** Transitive via `librosa` (runtime); not pinned in `requirements.txt`. Pre-existing / newly-published — not introduced by the docs-only commit. Exposure is low (msgpack is deserialized only from the trusted Celery/Redis broker + librosa internals, not from untrusted external input). The pytest CVE-2025-71176 alias `GHSA-6w46-j5rx-g56g` is already in the Issue-107 ignore-list. | SEV2 (dependency hygiene) | 📋 Open | Triage into the dependency-CVE lane (Issue 75 / Issue 107 re-baseline): either pin `msgpack>=1.2.1` in `requirements.txt` (preferred — real fix exists) or add `GHSA-6v7p-g79w-8964` to the accepted-risk ignore-list in `pyproject.toml` + `run_layer0.py` with a DECISIONS justification. |
| 2026-06-22 | Building Issue 182 (clip download endpoint) — tracing how clips are served | **Clip playback binds `<video src={clip.render_uri}>` to a raw `s3://bucket/key` URI** (`ClipPlayer.tsx`); there is **no media-serving endpoint or `/media` static mount**, so the browser cannot play it in prod (and a local FS path fails in dev too). The e2e fixtures null `render_uri` and `smoke.spec.ts` explicitly treats clip media as un-loadable noise — so this has been latent. | SEV2 (core Review playback) | ✅ Fixed in Issue 182 | Fixed inline (user opted in): the new `GET /clips/{id}/download?disposition=inline` presigned-URL endpoint now backs the player `<video src>`; the same helper serves the attachment download. |
| 2026-06-23 | Running full test suite after Issue 226 (retire legacy static UI) | **`test_data_export.py::test_start_export_returns_202_and_enqueues` and `test_issue_113.py::test_improvement_post_handles_concurrent_insert_race` fail with 429 Too Many Requests.** The in-process rate-limiter (using Redis or in-memory sliding window) leaks state across test modules when the full suite runs in a single process — these two tests run after rate-limit-heavy test files and exhaust the per-creator quota. Not introduced by security-platform; pre-existing ordering sensitivity. | SEV3 (test-infra flakiness) | ✅ Fixed 2026-06-24 | `tests/conftest.py` autouse `_isolate_app_state` now calls `limiter.reset()` (clears the RedisStorage limiter-prefixed keys) before/after every test, so accumulated buckets can't leak across modules. Verified: 3 consecutive full-lane runs all 1400 passed / 0 failed. |
| 2026-06-23 | Building Wave W0 lane agentic-caching-cost (Issues 218/220-223) — running unit test suite | **`test_data_export.py::test_start_export_returns_202_and_enqueues` fails with 429 Too Many Requests** even in isolation. The rate limiter is triggered when Redis is unavailable (test env). The `POST /creators/me/export` endpoint returns 429 instead of mocking through. Pre-existing — confirmed fails on the baseline commit (65a1d4f) with no code change. | SEV2 (test infra gap — endpoint mocks don't stub the rate limiter) | ✅ Fixed 2026-06-24 | Same root cause/fix as the row above: the autouse `limiter.reset()` clears buckets per-test. (The "fails in isolation" framing was a Redis-down env artifact; with Redis up + per-test reset the limiter starts empty.) Now passes in isolation and across 3 full-lane runs. |
| 2026-06-23 | Building Wave W1 lane observability (Issues 234/238/281) — running full unit test suite with `--override-ini="addopts="` to bypass pytest.ini addopts | **`conftest.py` Postgres guard check uses substring match `"integration" in marker_expr`** which is truthy for `-m "not integration"` (marker_expr contains the word "integration" as a substring). This means the guard fires even when the intent is to *exclude* integration tests, blocking test collection on this box where Postgres is unavailable. Workaround: `--override-ini="addopts="`. Pre-existing — introduced in Issue 267 conftest guard. | SEV3 (test-infra flakiness / developer friction) | ✅ Fixed 2026-06-24 | `tests/conftest.py` now strips `"not integration"` from the marker expr before the substring check, so the default unit lane (`-m "not integration and not quarantine"`) no longer trips the Postgres guard. Unit lane is runnable without Postgres again. |
| 2026-06-26 | Building Wave W0 lane llm-features-hardening (Issues 318-321) — running new unit tests | **`tests/conftest.py` fails to load because `routers/chat.py:259` has `@router.delete(..., status_code=HTTP_204_NO_CONTENT)` which triggers a FastAPI 0.115.x assertion `"Status code 204 must not have a response body"`.** The locally installed FastAPI (0.115.4) is older than the pinned version in `requirements.txt` (0.137.1). Same assertion applies to `routers/auth.py:454` (DELETE /me) and `routers/activity.py:34` (POST 204). Blocks the entire test suite on dev box; does not affect CI which installs pinned deps. | SEV3 (dev-box test-infra — blocks local unit runs when deps aren't from requirements.txt) | ✅ Fixed inline during Issue 326 (blocked tests) | Added `response_class=Response` to all three affected routes (`routers/chat.py`, `routers/auth.py`, `routers/activity.py`). This is the correct FastAPI pattern for 204 routes and is compatible with both 0.115.4 and 0.137.1. |
| 2026-06-24 | Verifying LLM backend; fixed the conftest guard above, then ran the FULL unit lane (first time runnable without Postgres) | **~10 backend unit tests were silently red** — masked because (a) the conftest guard blocked the unit lane on no-Postgres boxes and (b) self-hosted CI isn't running backend pytest yet (apt-deps step pending, per LEFT_OFF). After the guard fix: **1389 passed, 10 failed**. Triage: **no real prod regressions** — 8 are stale tests lagging shipped code [brand-kit migration renumber 0028→0029 (`test_brand_kit`); SPA-cutover `next_action` URLs `/static/*.html`→`/app/*` 85g (`test_static`, `test_empty_state_envelopes` ×2, `test_spa_serving`); Deepgram mock missing the `addons` arg added in Issue 251 (`test_ingest`); virality matcher flagging *negated* mentions "no virality language" (`test_compliance_no_virality`); `billing_refund` expecting 1 dispatch when `on_failure` now fires refund+notification (`test_billing_refund`)], and 1 is DB-dependent (`test_clip_counts_requires_auth` — needs Postgres / integration marker). | SEV2 (the suite drifted red unnoticed; code is correct) | ✅ 9/10 fixed 2026-06-24 — lane now **1399 passed / 1 pre-existing flake**. 📋 1 remaining (`test_clip_counts`). | Fixed: 2 cache-marker tests (`test_dna`/`test_identity_unit`); migration-renumber (`test_brand_kit` 0028→0029); SPA-cutover URLs (`test_static`→/app/dashboard, `test_empty_state_envelopes`→/app/insights); legacy-UI retirement (`test_spa_serving`→404, Issue 226); Deepgram `addons` mock (`test_ingest`); virality-negation whitelist (`test_compliance_no_virality`); Signals mock for the skip-reason path (`test_empty_state_envelopes::clips_empty`); refund dispatch count 1→2 (`test_billing_refund`). Added an autouse `dependency_overrides` clear in conftest. **Remaining:** `test_clip_counts_requires_auth` fails ONLY in full-suite order (passes in isolation) — an earlier test leaks engine state so the request path attempts a **sync** Postgres connection (`engine.raw_connection()`); needs a dedicated isolation hunt (likely `db.recreate_engine()`/`test_db_engine_config` global leak). Structural root cause unchanged: get backend pytest running in self-hosted CI so drift can't recur. |
| 2026-06-24 | Auditing LLM call sites for cost-correctness | **LLM cost ledger ignores cached tokens.** `billing/ledger._estimate_cost_usd` prices only `input_tokens`+`output_tokens`. The Anthropic SDK's `usage.input_tokens` is the **uncached remainder only** — `cache_read_input_tokens` (should bill 0.1× input) and `cache_creation_input_tokens` (1.25×) are dropped. `config.COST_CACHE_READ_MULTIPLIER=0.1` is referenced nowhere. `record_llm_usage` receives `cache_read`/`cache_creation` in its `usage` dict but doesn't thread them in. Net: cached traffic billed at 0×. `clip_engine/scoring.py` (1h-TTL DNA cache) and chat/runner are the biggest under-bills. Cache floor is 1024 tokens for Sonnet 4.6 (live-verified vs SOT) → more prefixes cache than assumed, so the under-bill is real. | SEV2 (systematic under-billing of cached LLM usage; money path) | 📋 Open — awaiting approval (money path) | Price cache tiers in `_estimate_cost_usd`; thread `cache_read`/`cache_creation` through `record_llm_usage` + the call sites that compute cost (`clip_engine/scoring.py`, `chat/runner.py`). |
| 2026-06-23 | Building Issue 244 (notification triggers) — running test_compliance_no_virality.py | **`test_no_virality_in_openapi_schema_descriptions` fails on the phrase "no virality language" in a docstring in `routers/creators.py:262`.** The phrase contains the regex-matched word "virality" and is not in the WHITELIST (only "does not promise virality" etc. are whitelisted). Confirmed pre-existing: fails on the baseline commit with no Issue 244 changes. | cleanup (false-positive in compliance scan) | ✅ Verified already resolved (WHITELIST negation block since bf95ce2) | Add "no virality language" to the WHITELIST in test_compliance_no_virality.py, or rephrase the docstring in routers/creators.py to avoid the pattern. Low-priority false-positive. |
| 2026-06-23 | Building Issue 304 (redesign foundation) — running `npm run lint` | **Frontend `npm run lint` carries 10 pre-existing problems (6 errors, 4 warnings)**, none in redesign files: `Onboarding.test.tsx` unused `_type`/`_cb` args; `react-refresh/only-export-components` in `ActivityPanel.tsx` + `insights/InsightsNarrative.tsx`; `react-hooks/set-state-in-effect` + `exhaustive-deps` in `Editor.tsx` and `useStageStream.ts`; 2 unused `eslint-disable` directives. Confirmed pre-existing on the baseline commit. Issue 304 added **zero** new lint problems. | cleanup (lint debt; lint gate not currently clean at baseline) | ✅ Errors cleared (2026-06-24) | All **6 errors** resolved so `npm run lint` exits 0 (frontend CI job green): 2 unused-var via `eslint.config.js` `^_` ignore convention; `react-refresh/only-export-components` via extracting `deriveWhyNarrative` → `insights/narrative.ts` (project convention); 3 `react-hooks/set-state-in-effect` via justified `eslint-disable-next-line` (intentional reset/sync effects in just-shipped, not-yet-visually-verified Editor/useStageStream — proper `key`/during-render refactor still tracked below). **4 warnings remain** (non-blocking): exhaustive-deps + react-refresh warnings. Proper refactor of the 3 effect sites = open follow-up for the lint sweep. |
| 2026-06-24 | Issue 100 — running the full vitest suite after the frontend gate fixes | **Suite-wide vitest timing flake (~1 in 5 full runs).** A `findByRole(...)` would intermittently time out (1000ms default) under full-suite parallel load — the async render (react-query mock resolution + effects) hadn't settled yet. Hit *different* tests across runs (Dashboard "Queue for analysis", Insights "page-level framing") incl. files untouched this session → pre-existing, surfaced by load, not a logic bug (every failing test passes deterministically in isolation). | SEV3 (test-infra flakiness; intermittent CI redness) | ✅ Fixed (2026-06-24) | Added `configure({ asyncUtilTimeout: 5000 })` to `frontend/src/test/setup.ts` — gives `findBy*` room to settle under contention (esp. the self-hosted CI runner). Verified: 8/8 consecutive full-suite runs green (was ~1-in-5 red). Content assertions unchanged. |
| 2026-06-23 | Standing up the hybrid local CI/CD (DECISIONS.md) — running the gates locally before wiring the pre-push hook | **`ruff format --check .` fails on 43 tracked files** (`worker/tasks.py` + ~42 `tests/test_*.py`); the tree drifted from its own mandated formatting while the billing-disabled GH CI never enforced it. CI's `ruff check` is clean (the 7 issues seen locally were the untracked `tests/eval/` scaffold). This means the now-self-hosted `ci.yml` lint job (`ruff format --check .`) will go **red on its first real run**. The local pre-push hook ratchets format on *changed* files only, so this pre-existing drift does not block day-to-day pushes. | cleanup (style debt; self-hosted CI lint job red until cleared) | ✅ Fixed (2026-06-24) | Ran `ruff format` on all 43 tracked files (`git ls-files '*.py' \| xargs ruff format`); `ruff format --check` now clean (289/289), `ruff check` still green, all files `py_compile`-verified. `ci.yml` lint job will be green. |
| 2026-06-24 | Diagnosing a prod `/auth/callback` 500 (new-signup OAuth) on autoclip.studio | **`alembic upgrade head` silently rolls back in prod — every deploy's migration step is a no-op (exit 0, no error).** Root cause in `alembic/env.py` `do_run_migrations`: the Issue-270 `connection.execute("SET lock_timeout…/statement_timeout…")` calls run *before* `context.begin_transaction()`, auto-beginning a SQLAlchemy 2.0 transaction that alembic then treats as caller-owned and never commits; the async connection close discards all DDL. Result: prod DB stuck at rev `0027` while deployed code was at `0034`, so the new-signup callback's writes to `terms_version`/`privacy_version`/`terms_accepted_at` (0033) + `minimum_age_confirmed_at` (0034) hit `UndefinedColumn` → uncaught 500. `scripts/deploy.sh:51` runs the broken CLI path, so the drift was invisible. **Proven on prod**: buggy pattern → `persisted=False`; timeouts via `connect_args` → `persisted=True`. | BLOCKER (silent prod migration failure → new-user signup outage) | ✅ Fixed 2026-06-24 | **Prod unblocked**: backed up DB (`backups/pre-0034-*.sql.gz`), generated offline SQL (`alembic upgrade 0027:head --sql`) and applied via `psql` (carries its own `BEGIN/COMMIT`) → prod now at `0034`, all columns/tables present. **Code fix**: `alembic/env.py` moves the timeouts to `create_async_engine(..., connect_args={"options": "-c lock_timeout=5s -c statement_timeout=120s"})` and removes the pre-transaction `SET` execs, so migrations commit again. Needs: redeploy with the env.py fix; consider `/log-issue`; promote a hardening issue (callback should map `httpx.HTTPStatusError`/DB errors to a clean redirect instead of leaking 500). |
| 2026-06-24 | Visual-walking the redesign fidelity-polish screens (Settings + short Editor) under the mocked Playwright harness | **The e2e mock-api fixture has no `/creators/me/brand-kit` entry**, so `dispatch()` returns `{}` for it. `BrandKitSection`/`CaptionStylePanel` initialize controlled inputs from that data, so on load the values flip defined→undefined → React `"controlled input to be uncontrolled"` console warning. Only surfaces now because Settings + short-Editor were never in `smoke.spec.ts` `AUTHED_PAGES` (so the gap stayed latent). NOT a product bug — prod returns real brand-kit data; pure test-fixture gap. | cleanup (test-fixture gap) | 📋 Open | Add a `BRAND_KIT` fixture to `e2e/fixtures/mock-api.ts` `GET_TABLE` (`/creators/me/brand-kit`), and add `settings` + `editor` (both modes) to `smoke.spec.ts` so they're walked in CI. Low priority. |
| 2026-06-24 | Running the eval-harness/full suite at W0-fix integration (311–315) | **Quarantined clipboard-stealer's COMPILED BYTECODE still on disk** in `tests/eval/client/__pycache__/` (`clipboard_watch`, `clipboard_monitor`, `autostart`, `windows_runner`, `api_client`, `os_detect`, `track_rules`, `event_log` `.pyc`) + empty `tests/eval/dump/`. Source `.py` is gone (per LEFT_OFF quarantine) but `.pyc` is import-executable. Untracked (gitignored via `__pycache__`) so it never deploys, and no test imports `tests.eval.client` — inert. The `rm -rf` was safety-blocked (untracked dirs not agent-created), so removal is **pending user approval**. | SEV2 (malware residue, inert/un-deployed) | 📋 Open — awaiting user OK to `rm -rf tests/eval/client tests/eval/dump` | Manual deletion once approved; consider a `.gitignore`-independent CI check that fails if `tests/eval/client` reappears. |
| 2026-06-24 | Building Issue 311 (notify) — agent review | **`notify/copy.py` in-app body strings use host-less `/pricing` paths** (`trial_ending`: "Top up your minutes at /pricing"; `balance_low`: "Add more at /pricing"). These feed the in-app notification body (not the email templates fixed in 311), so the relative path renders wrong in the in-app surface. | SEV3 (in-app copy, low blast radius) | 📋 Open | Promote into the 316 notify backlog: build in-app links from `APP_BASE_URL` like the email templates now do. |
| 2026-06-24 | Building Issue 312/315 — running tests under system `python3.12` | **System `python3.12` has FastAPI 0.115.4 but `requirements.txt` pins 0.137.1**; the older FastAPI raises `AssertionError: Status code 204 must not have a response body` on `routers/chat.py:259`'s registration, blocking `conftest` import. The **code is correct** (`-> None`); it's a dev-env hygiene issue — tests must run via the project `.venv` (0.137.1), where the full suite is green. | cleanup (dev-env, not a code bug) | 📋 Open | Document the `.venv`-only test invariant; optionally pin the interpreter in a `Makefile`/`tox` target so `python3.12` system isn't used by mistake. |
| 2026-07-02 | W1 CHECK research — Issue 219 Batch-API spike | **`docs/assessment/llm/clip_scoring.md:72` claims scoring is "not user-blocking past the SSE progress bar" (202+poll)** — stale post-Wave-3-Fix-E: the SSE consumer deliberately stays subscribed until scoring completes (worker/tasks.py:2237→2283) and auto-render + `clips_ready` gate on it. The stale claim nearly justified routing scoring through the no-SLA Batch API. | SEV3 (doc staleness with decision blast radius) | 📋 Open | Correct the assessment doc's latency section; the 219 DECISIONS entry (2026-07-02) records the accurate flow. |
| 2026-07-02 | W1 kickoff — confirming prod deploy of `2dbd17c` | **`LLM E2E Nightly` workflow failed on main (run 28565459895, 53s — likely setup/credential failure, not a test run).** Flag-gated live-API harness from Issue 319; not investigated to stay on-course for W1 round 1. | SEV3 (nightly signal red; prod deploy itself succeeded) | 📋 Open | Pull the run log; if it's a missing `RUN_LLM_E2E`/API-key secret on the runner, fix the workflow env; else triage as a real regression. |
| 2026-07-02 | Building Issue 202 (per-retrain NDCG emission) — mypy on the harness extraction | **`scripts/eval_efficacy.py:37` imports `get_sessionmaker` from `db`, which doesn't exist** (db.py exports `AsyncSessionLocal`/`AdminSessionLocal`/`get_session`) — the Issue-198 real-data script would `ImportError` on a DB host. Pre-existing at the base commit (mypy flags it there too); the script has never been run against real data (that's the parked staging step of 198/200). | SEV3 (dead code path in an ops script; blocks the 198/200 staging runs when attempted) | 📋 Open | Fix the import to `AsyncSessionLocal` (or add a session-factory accessor) before the 198/200 staging runs; one-line fix + smoke `--help` run. |
| 2026-07-02 | PR #41 CI — first run of the Migration-lint gate with real migration changes | **The Squawk gate (Issue 270) had never actually run: `npm install -g squawk-cli` fails on the self-hosted runner (no node toolchain, exit 127), AND the lint step had an off-by-one — `alembic upgrade "REV:+1" --sql` renders the migration AFTER the changed one, with the newest one erroring into suppressed stderr → empty stdout → trivial pass.** Also: env.py's Issue-270 timeouts live in online-mode `connect_args` only, so offline `--sql` scripts (a real prod path — the 0027→0034 psql apply) carried no lock/statement timeouts. | SEV2 (a deploy-safety gate that could never fail) | ✅ Fixed inline 2026-07-02 (blocked PR #41) | ci.yml: `pip install squawk-cli` + render `down_revision:revision` per changed file + `set -o pipefail`; env.py: offline mode now emits `SET lock_timeout='5s'`/`statement_timeout='120s'`; `.squawk.toml` excludes the two style rules (bigint/text) contradicting the existing schema — unsafe-op rules stay active per the Issue-270 DEC. Verified locally: all 3 new migrations (0041–0043) lint clean. |
| 2026-07-02 | Fixing the PR #41 order-dependent publish-test failure — ran the full suite randomized locally (pytest-randomly seed 20260702) | **2 more order-dependent failures: `test_health.py::test_health_storage_ok_in_local_backend` + `test_health_postgres_down_returns_degraded`** — pass in isolation, fail only in a specific full-suite random order. Poisoner NOT identified (not the W1-new test files, not test_rate_limiting alone — needs the dedicated isolation hunt). Same class as the 2026-06-24 `test_clip_counts_requires_auth` entry (engine/app-state leak). Pre-existing: test_health untouched since main; only newly visible because pytest-randomly wasn't installed locally until today. | SEV3 (flake landmine — a future CI seed can hit it) | 📋 Open | Dedicated isolation hunt: bisect the seed-20260702 module order ahead of test_health; likely a settings/storage/engine global leak. Consider pinning `-p randomly --randomly-seed=<fixed>` in CI until fixed. |
| 2026-07-02 | Deploying PR #41 (migrations 0041–0043) to prod | **Deploy failed at "Run migrations": 0041 emitted `CREATE TYPE render_status_enum` (DuplicateObject) despite `create_type=False`** — the kwarg was on generic `sa.Enum`, where it is dialect-dependent; AND the deploy's migration step runs with the VM's own `/root/.local` Python packages (unpinned SQLAlchemy/alembic — version skew vs CI's pinned stack, which is why CI's fresh-DB bootstrap passed). Prod safe: transaction rolled back (DB stayed at 0040), failure was before the image swap, /health green on the prior image. | SEV1 (deploy pipeline blocked; no prod impact) | ✅ Fixed 2026-07-02 (hotfix PR) | 0041 rewritten with dialect `postgresql.ENUM(create_type=False)` for both enums + explicit `checkfirst=True` create for `summary_status_enum`; proven on real PG (0001→0043, down/re-up) + offline `--sql` render + squawk clean. FOLLOW-UP to promote: pin the VM migration runner to the image's deps (run `alembic upgrade` inside the app container, not host Python) — tracked as a residual under Issue 352. Also: deploy log warns "No BACKUP_R2_BUCKET configured — migrating WITHOUT safety dump" (Issue 256 operator step still pending). |
| 2026-07-02 | Building Issue 352 Batch K (CaptionStylePanel SSE follow) | **Styled re-render of an already-rendered clip is a worker no-op:** `POST /clips/{id}/render` (routers/clips.py ~:425-509) persists the new `style_preset` but never resets `render_status`, and the render task (worker/tasks.py ~:1373) skips any clip with `render_status=done AND render_uri` — emits `done` without applying the style. "Render with style" on a done clip silently changes nothing. | SEV2 (silent UX lie on the review surface) | ✅ Promoted → Issue 353 (W3, 2026-07-02) | Promote to issues triage: the render endpoint should reset `render_status`/`render_uri` (or version the render) when a new style is submitted for a done clip; add a regression test on the re-render path. |
| 2026-07-02 | Building Issue 352 Batches B/G (agents' full-suite runs) | **Two pre-existing test-infra flakes surfaced:** (a) `tests/test_notifications_triggers.py` `TestReauthRequiredTrigger` (~:287) emits an unawaited-coroutine RuntimeWarning from `sync_channel_catalog` (traced via tracemalloc; not introduced by Batch B); (b) `tests/test_youtube_edges.py::test_check_captions_available_empty_items_returns_false` uses deprecated `asyncio.get_event_loop()` — order-dependent flake, same class as the 2026-07-02 CI `asyncio.run()` fixes. | SEV3 (test-infra) | ✅ (a) Fixed 2026-07-29 (w2/test-flakes f66b485); (b) verified already fixed (f0d8e1f) | (a) await/mock the coroutine properly in the trigger test; (b) convert to `async def` per the established pattern. Both are one-liners for the next test-hygiene pass. |
| 2026-07-02 | Building Issue 352 Batch I (chat) — full-suite runs | **Flaky teardown race: `RuntimeError: Event loop is closed` in `main.py:111` `_health_redis.aclose()` during session-scoped TestClient lifespan shutdown** (surfaced after `test_preference_scorer_cache.py::test_new_version_busts_cache`; reruns clean). Same family as the 2026-06-29 entry and the seed-20260702 test_health pair — the app's module-level async Redis client outlives the loop that created it in multi-loop test runs. | SEV3 (test-infra flake) | ✅ Fixed 2026-07-29 (w2/test-flakes) — ROOT CAUSE CORRECTED: poisoner was test_compliance_no_virality's bare TestClient (fresh loop per request) parking dead-loop conns in the health pool; per-lifespan client ownership + context-managed crawler; 3 full runs with 0 noise | Fold into the dedicated isolation hunt already queued (see 2026-07-02 test_health entry): likely fix is loop-affinity guarding or per-lifespan client creation for `_health_redis`. |
| 2026-07-02 | Isolation hunt for the seed-1/5/20260702 test_health failures (round 2b gate) | **CORRECTION of today's earlier "order-dependent test_health" entry — the real root cause: the unit suite was hitting the LIVE R2 bucket.** Local `.env` sets `STORAGE_BACKEND=r2` + real credentials; conftest pinned many env vars but not this one, so every `/health` unit test probed live R2 over the network — green when the probe succeeded, red when it flaked. The "poisoner test" never existed (spy-plugin instrumentation showed STORAGE_BACKEND='r2' from the first test of every run). Also explains the R2-warning noise and 60-113s full-suite runs (now ~24s). | SEV2 (unit lane non-hermetic; live network + real bucket touched by tests) | ✅ Fixed 2026-07-02 (w1/round2b) | `tests/conftest.py` pins `STORAGE_BACKEND=local` via setdefault (explicit env still overrides). Verified: seeds 1, 5, 20260702 all 2126-green. The `_health_redis` teardown-race entry above remains a real, separate (rarer) flake. |
| 2026-07-02 | Building Issue 231 (worker RLS sweep) — WITH-CHECK write-path integration tests | **RLS policies 500 (not deny) on GUC-less app-role queries over reused pooled connections:** after a connection has once carried the transaction-local GUC, `current_setting('app.creator_id', true)` returns `''` (not NULL) on later transactions, and the policies' bare `::uuid` cast raises `invalid input syntax for type uuid: ""`. Fail-closed holds (error, not leak), but a legitimately GUC-less app-role query on a reused connection becomes a 500. | SEV2 (correctness/availability edge on the RLS backstop) | ✅ Promoted → Issue 354 (W3, 2026-07-02; scope corrected to 27 policies/11 migrations) | Promote to an issue: harden all tenant policies (0010/0037/0038/0040/0041/0044) to `NULLIF(current_setting('app.creator_id', true), '')::uuid` via one new migration; add the reused-connection regression to the RLS integration suite. |
| 2026-07-02 | Issue 298 CHECK — reading deploy.yml + docker-compose.prod.yml for the staging-parity gate | **Issue 271's auto-rollback is a no-op image swap:** deploy.yml's `_rollback_and_fail` runs `IMAGE_TAG="${PREV_IMAGE}" docker compose -f docker-compose.prod.yml up -d`, but docker-compose.prod.yml hardcodes `image: ghcr.io/reese8272/creatorclip:latest` on app/worker/beat — `${IMAGE_TAG}` is never interpolated, so a "rollback" re-launches `:latest` (the exact bad image just pulled). The `docker pull "${PREV_IMAGE}"` succeeds but is never used. Rollback currently only "works" when the failure happened before the pull. | SEV2 (safety net that can't actually roll back) | ✅ Fixed (verified 2026-07-20 assessment: `${IMAGE_TAG:-...}` interpolation + `:rollback` re-tag present at HEAD) | Was: Change app/worker/beat to `image: ${IMAGE_TAG:-ghcr.io/reese8272/creatorclip:latest}` and verify a manual PREV_IMAGE up-cycle on the VM; natural to fold into Issue 298's deploy.yml touch or fix alongside the #352 alembic-in-container residual. |
| 2026-07-02 | Issue 78-R (disallow_untyped_defs ratchet) — mypy sweep over scripts/ | **`scripts/eval_efficacy.py` imported a non-existent `db.get_sessionmaker` (masked by a stale blanket `# type: ignore[import-untyped]`)** — the Issue-200/202 eval harness would crash with ImportError on every invocation; it has evidently never been run since the import was written. | SEV3 (dev-only eval script, broken since Issue 200) | ✅ Fixed inline 2026-07-02 (blocked the mypy-0 gate) | Replaced with `from db import AsyncSessionLocal` + `async with AsyncSessionLocal() as session:` and dropped the stale ignore. Note: the harness reads clips cross-tenant under the app role — if RLS gating bites when it is next actually run, switch it to an admin factory then. |
| 2026-07-02 | Staging gate attempt #2 (post-#47) | **The Issue-264 PgBouncer digest pin was dead:** `edoburu/pgbouncer:v1.25.2-p0@sha256:37a605...` returns "not found" on Docker Hub — upstream re-pushed the tag (live digest now `7d7a27d9...`). The pin did exactly what supply-chain pins exist to do: it refused to silently run different bytes under the same tag. Prod was never touched (gate skipped it twice). | SEV3 (staging-only sidecar; gate blocked deploys until fixed) | ✅ Fixed 2026-07-02 (digest updated to the live manifest, resolved via the registry API) | Follow-up worth filing with #264's owner: a scheduled check that pinned digests still resolve, so a re-push is caught before it blocks a deploy. |
| 2026-07-03 | First-user UX walkthrough of the SPA (mocked Playwright, authed) | **Recap page (Issue 192) white-screens on a malformed/empty summaries response.** `Recap.tsx:90` did `summariesQuery.data?.summaries[0]` — the `?.` only guards `data` being null; when the endpoint returns a truthy body without a `summaries` array, `undefined[0]` throws and the root ErrorBoundary shows "Something went wrong". Also had NO `isError` branch (a failed fetch fell through to the empty state). 192 shipped without visual verification because the e2e fixture never mocked `/videos/:id/summaries`. | SEV2 (white-screen on the just-shipped recap surface; prod-safe only while the endpoint returns well-formed 200s) | ✅ Fixed 2026-07-03 (PR #54): `data?.summaries?.[0]` guard + `isError` retry state; added the SUMMARIES fixture + recap/video-detail to the smoke loop so it's permanently exercised. |
| 2026-07-03 | Staging-drills first successful dispatch | **Drills auth probe 401'd — wrong session cookie name.** `scripts/drills.py` set `cookies={"session": ...}`; the app reads `cc_session` (auth.SESSION_COOKIE). The workflow itself worked end-to-end (stack up, health, migrate, seed all passed). | SEV3 (drill-only; prod unaffected) | ✅ Fixed 2026-07-03 (PR #54): cookie name → `cc_session`, matching scripts/llm_harness.py. |
| 2026-07-20 | youtube module post-fix re-assessment — verifying the DECISIONS 2026-07-20 fix-batch entry | **Two stray `=======` merge-conflict separators left in `docs/DECISIONS.md` (lines ~10632, ~10672)** — leftover from the PR #57 multi-branch integration merge (both sides' entries were kept, only the marker lines remain; no content lost). | SEV4 (docs cosmetics) | 📋 Open | Delete the two bare `=======` lines. |
| 2026-07-20 | Diagnosing owner-reported "rendering isn't working" on prod (clip b4c87d6f, video 0d01628d ingested 2026-07-01) | **`POST /clips/{id}/render` has no source-expired pre-check:** the recap endpoint (routers/clips.py ~:1590) returns a friendly 4xx "Source media expired (72-hour retention) — re-upload", but the clip render endpoint enqueues anyway; the worker then fails permanently (`ValueError: Source video not available`) and the user only sees a generic "Render failed" / `render_status=failed` with no explanation. Videos list does expose `clippable: source_uri is not None`, but the per-clip render path never checks it. | SEV2 (confusing dead-end UX on the core render surface for any clip older than 72h) | ✅ Promoted → Issue 362 + fixed same day (endpoint 409 pre-check + regression test; `render_video_clips` needs no guard — worker-enqueued inside the retention window). SPA "source expired" card state left as the 362 residual. | Issue 362 / ISSUE-2026-07-20-02 |
| 2026-07-20 | Same diagnosis — reading worker logs | **`render_clip`'s `log_event` calls mislabel `clip_id` as `video_id`** (worker/tasks.py ~:505-520: `video_id=clip_id` on started/failed events), sending the investigation to the wrong table on first read. Identical mislabel found in `render_summary` (`video_id=summary_id`). | SEV4 (log hygiene; misleading during incident triage) | ✅ Fixed 2026-07-29 (w1/worker-fixes 44e386f) | Fields relabeled `clip_id`/`summary_id`, event names unchanged; RUNBOOKS.md has no saved queries on the old field. Regression: `tests/test_render_retry.py::test_render_clip_events_carry_clip_id_not_video_id`. |
| 2026-07-29 | w1/metadata + w1/review-ui lanes (409 restructure) | **`frontend/src/lib/api.ts` typed error bodies as `{detail?: string}` and passed object details straight into the Error message** — the live `pending_clean_or_edit` dict 409s already rendered as "[object Object]" in CleanPassPanel/TranscriptEditor status lines; the new `source_expired` 409 would have too. | SEV2 (creator-facing garbled error) | ✅ Fixed 2026-07-29 (w1/review-ui 00f2586) | ApiError now preserves raw `.detail` + machine-readable `.code`, `.message` string-compatible for both shapes. Regression tests: `api.test.ts`, `ClipPlayer.test.tsx`. |
| 2026-07-29 | w1/trim-rerender CHECK research (reading the feedback trim path) | **"Save trim" was effectively broken for most clips:** `submit_feedback`'s Issue-339 bounds check compared the frontend's CLIP-RELATIVE trim values against video-absolute `clip.start_s`/`end_s` → 422 "trim_start_s is before the clip start" for nearly any clip starting later in the source. Sibling: `Review.tsx` `clipDur` ignored `setup_start_s` (filmstrip range overstated for setup clips). | SEV2 (core Review feedback action broken) | ✅ Fixed 2026-07-29 at integration (dd92fcd) | Bounds now validate against `clip_duration_s` with origin `setup_start_s ?? start_s` (the /transcript,/cuts,/trim-render timebase); `clipDur` uses the same origin. Regression: `test_trim_clip_relative_values_accepted_for_late_starting_clip`. |
| 2026-07-29 | w1/trim-rerender Layer-0 gate run | **`run_layer0.py` pip_audit gate FAILS locally with 20 findings vs baseline** — reproduced on a clean base checkout with zero dependency changes (live advisory DB vs pinned requirements + local venv drift); `tests/test_security_baselines.py` pip-audit sync tests intermittently red in the same env. CI's pinned-requirements pip-audit gate is authoritative (LEFT_OFF invariant). | SEV2 (blocks a clean local Layer 0; masks real new CVEs) | 📋 Open — W2 triage | W2 dependency-CVE lane: re-baseline the ignore-list or bump the affected pins so local Layer 0 is clean again. |
| 2026-07-29 | w1 build wave (all 4 lanes) | **Workflow-harness worktree cleanup fired mid-build:** all four lane worktrees were externally deleted while agents were still working (one transient stash/pop ran in the MAIN checkout; tracked `.claude/` files showed as deleted until `git restore`). No committed work lost — branches survived; lanes rebuilt worktrees from `.git/worktrees/*` metadata. | SEV2 (build infra; risk of losing lane work) | 📋 Open — harness | Audit the worktree auto-clean behavior before the next wave (likely the "auto-cleaned if unchanged" heuristic racing long-running lanes); consider lane branches pushed to origin as the safety net. |
| 2026-07-29 | w1/review-ui build (trim → re-render flow) | **"Keep original" discard is client-side only** — no server discard endpoint exists, so a discarded clean/trim leaves `cleaned_render_uri` set and future clean/trim-render POSTs 409 `pending_clean_or_edit` until confirmed. UI now resurfaces the pending preview on that 409 (mitigation), but a real `DELETE`/discard endpoint would close the loop. | SEV3 (UX dead-end mitigated in-lane) | ✅ Fixed 2026-07-30 (Issue 364, w1/issue-364-discard) | `POST /clips/{id}/clean/discard` clears `cleaned_render_uri` + best-effort R2 purge; UI call sites wired to it. |
| 2026-07-29 | Running the #24/#25 operator-gate verification (ready-pass W0) | **PROD FULLY DOWN, silently, for up to 9 days:** autoclip.studio → HTTP 530 / Cloudflare 1033 (origin unregistered from tunnel); SSH to the VM (147.182.136.107) times out; 100% ping loss. Whole-droplet outage (not app-level) — signature matches ISSUES_LOG ISSUE-2026-07-03-01 (recovered there by out-of-band cloud-API reboot). No monitoring caught it: `health-check.yml` hasn't run since 2026-06-17 (scheduled-workflow stopped) and #282 (Better Stack uptime) is still OPEN — the outage was only found by manually curling. Also blocks every deploy (self-hosted runner lives on the VM). | **BLOCKER** (prod outage; deploys blocked) | ✅ Resolved 2026-07-29 — **ROOT CAUSE: owner-intentional** | Owner had disabled the droplet (clean systemd poweroff Jul 28 11:59 UTC) and re-enabled it 2026-07-29; NOT a DO/billing/crash event. Post-recovery: /health green, #24/#25 verified GREEN, 3 deploys shipped. Standing lesson (memory `project_live_deployment_topology`): check for intentional shutdown before incident response. #282 stays beta-critical — friends need the box up and a monitor watching it. |
| 2026-07-29 | w2/test-flakes noise hunt | **~20 out-of-lane test files still emit unawaited-coroutine RuntimeWarnings** (test_summary_endpoints, test_billing_refund, test_analyze_performer, test_chapters, …; emitter sites incl. routers/improvement.py:123, routers/clips.py:1764) — blocks a future `filterwarnings=error::RuntimeWarning` ratchet. test_retention_tasks' two instances fixed in-lane. | SEV3 (test-infra) | 📋 Open | Sweep in the next test-hygiene pass, then ratchet pytest.ini. |
| 2026-07-29 | w2/test-flakes noise hunt | **Lazy async-Redis module singletons with no close path/loop guard:** worker/tasks.py `_WORKER_REDIS`, routers/thumbnails.py `_aio_redis`, youtube/_redis.py `_REDIS_CLIENT` — safe in prod (one loop/process) but leak loop-bound connections in tests; disarmed via conftest pytest_sessionfinish. | SEV3 (app hygiene) | 📋 Open | Consider register_aclose or loop-guard pattern in a later hygiene pass. |
| 2026-07-29 | w2/frontend-misc (font fix) | **static/_design-tokens.css:20 still @imports Google Fonts (Inter + JetBrains Mono) on the surviving legal pages** (tos/privacy/accessibility) — same GDPR IP-leak class (LG München 3 O 17493/20) on exactly the pages an EU visitor reads pre-signup; no bundler exists for /static so vendoring needs its own approach. | SEV3 (compliance polish) | 📋 Open | Vendor woff2 under static/fonts + rewrite the @import; operator/product call on scope. |
| 2026-07-29 | w2/frontend-misc (URL sanitizer) | **Cleaned-preview `<video src>` bound the raw `cleaned_render_uri` (`s3://` URI) — unplayable in prod R2** (same class as Issue 182's render_uri bug; latent because mocks used http URLs). | SEV2 (Editor clean-pass preview broken in prod) | ✅ Fixed 2026-07-29 (w2/frontend-misc e84e1e8) | CleanedPreviewConfirm/TranscriptEditor/Editor now use GET /clips/{id}/download?variant=cleaned&disposition=inline. Needs a live smoke after next deploy. |
| 2026-07-29 | w2/clip-engine (assessment cleanups) | **ffmpeg filter-path escaping in clip_engine/render.py** (subtitles/fontsdir f-string + sendcmd arg) left unescaped — DEFERRED: multi-level ffmpeg escaping semantics, not unit-test-guardable, low actual risk (worker-created /tmp paths, list argv, no shell). | SEV4 (theoretical) | 📋 Open | Revisit only if user-influenced paths ever reach these filters. |
| 2026-07-29 | w2/e2e (visual prep) | **The 'empty-dashboard' @visual baseline test is misnamed** — it renders a POPULATED dashboard (3 fixture videos) and its 4 mask locators match nothing. | SEV4 (test naming/masks) | 📋 Open | Rename + fix masks when baselines are first generated (#272 dispatch). |
| 2026-07-29 | w2/e2e (fixture audit) | **Settings.test.tsx brand-kit mock had a non-existent `caption_style` field** and its `includes('/brand-kit')` matcher also fed the suggestion endpoint a fabricated body. | SEV4 (test fidelity) | ✅ Fixed 2026-07-29 (w2/e2e 1a2d743) | Mock corrected to BrandKitOut shape + explicit 204 suggestion stub. |
| 2026-07-29 | W2 tail — ratcheting per-module coverage floors | **`clip_engine`/`preference` rates read `None` in the module-coverage gate:** the multi-root `--cov` invocation makes coverage.py relativize filenames per source root (preference files appear as bare `model.py` etc.), so `_module_line_rate`'s package/class match misses them. Not missing coverage — a measurement quirk. | SEV4 (gate blind spot on 2 of 5 floored modules; clip_engine independently gated by the eval harness) | 📋 Open | Make the measurement deterministic (single-root cov for the module gate, or filename-set matching) in the next production-assessment skill pass; floors stay 0.0 for those two until then. |
| 2026-07-29 | W2 tail — pip-audit triage | **The 20 local pip-audit findings were venv staleness, not code:** all pillow 12.2.0 (transitive via voyageai; CI/prod resolve fresh to ≥12.3.0) + the venv's own pip 24.2. Local venv upgraded → pip_audit gate 0; Layer 0 fully green locally again. | resolved (env) | ✅ Fixed 2026-07-29 (venv upgrade, no repo change) | LEFT_OFF "local pip-audit = noise" gotcha can be retired next time it's rewritten. |
| 2026-07-29 | w2/billing-audit CHECK (full LLM cost-path sweep) | **`chat/intake.py run_intake_turn` (POST /creators/me/identity/chat, Sonnet, 40/hour limit) was entirely unbilled** — it accumulated cache tokens and recorded Prometheus token metrics but never wrote the ledger: 0× billed for input/output/cache AND invisible to the Issue-290 spend guard (the real exposure; dollars are small, ~$0.02–0.05 per completed intake). | SEV2 (revenue leak + spend-guard blind spot) | ✅ Fixed 2026-07-29 (w2/billing-audit 452a700) | Bills via record_llm_usage at Sonnet rates incl. cache tiers; regression: tests/test_identity_chat.py::test_intake_turn_writes_billing_ledger_with_cache_tokens. |
| 2026-07-29 | w2/billing-audit CHECK (same sweep) | **`knowledge/thumbnails.py analyze_thumbnail_patterns` (Sonnet multimodal, ~10 images ≈ $0.055/cold call — the largest per-call leak found) discarded its usage, so BOTH compute paths (worker `_generate_thumbnail_concepts_async` + GET /creators/me/thumbnail-patterns) billed 0×** despite the router docstring calling these "billed multimodal calls". Sibling under-bill: the five dna_system_block ttl:"1h" features billed cold cache writes at the 1.25× 5-min default instead of the official 2×. | SEV2 (revenue leak) | ✅ Fixed 2026-07-29 (w2/billing-audit 452a700) | Returns (patterns, usage); billed inside both compute paths (Redis-cache hits bill zero); cache_write_multiplier=2.0 threaded when the 1h marker attached. Regression: tests/test_thumbnails.py billing tests + the repo-wide unbilled-LLM AST sweep in tests/test_usage_coverage.py. |
| 2026-07-29 | w2/billing-audit (approved money path) | **`chat/intake.py run_intake_turn` was entirely unbilled** — identity-chat turns wrote Prometheus token metrics but never the cost ledger, so they were invisible to the Issue-290 spend guard and monthly caps. | SEV2 (money path — revenue leak + cap blindness) | ✅ Fixed 2026-07-29 (452a700) | Bills at standard Sonnet rates incl. cache tokens; counts toward spend guard. Regression: `test_intake_turn_writes_billing_ledger_with_cache_tokens`. |
| 2026-07-29 | w2/billing-audit (approved money path) | **`analyze_thumbnail_patterns` vision usage discarded — both compute paths billed 0×** (~$0.055/cold multimodal call, the largest single leak) + the five `dna_system_block` 1h-TTL features billed cold cache-writes at 1.25× instead of the official 2×. | SEV2 (money path) | ✅ Fixed 2026-07-29 (452a700) | Bills once per compute (cache hits + single-flight waiters bill zero); 2.0 multiplier applied ONLY when the floor-gated 1h marker actually attached. NEW CI guard: repo-wide AST sweep fails on any unbilled `_ANTHROPIC` call site (`tests/test_usage_coverage.py`). |
| 2026-07-30 | L23 close-out Layer-0 run | **pip-audit gate red on a NEW advisory published since yesterday's green run: pytest 8.3.3 → PYSEC-2026-1845 (fix: 9.0.3).** Dev-only dependency (pytest never ships in the prod runtime); zero dependency changes in the L23 lane. A pytest 8→9 MAJOR bump mid-close-out risks plugin breakage (pytest-asyncio et al.) — exactly the side-quest this log exists to defer. | SEV3 (dev-dep advisory, no prod exposure) | logged | Bump `pytest==9.0.3` (+ plugin compat check + full suite) as a small standalone dependency chore; until then the Layer-0 pip_audit gate reads 1 vuln vs the 0 baseline. |
| 2026-07-30 | W1 #365 merge verification (all-waves run) | **CSP still allow-lists Google Fonts after both font fixes landed — the permission is now dead.** `main.py:309-310` keeps `style-src ... https://fonts.googleapis.com` and `font-src 'self' https://fonts.gstatic.com`, and the comment at `main.py:293` still claims "the SPA stylesheet @imports" them. Neither is true since the SPA fix (2026-07-29) and #365 (2026-07-30, static legal pages) — `grep -rn "fonts.googleapis\|fonts.gstatic" static/ frontend/src/` is now zero. Leaving the origins allow-listed means a future accidental remote-font `@import` would silently WORK instead of being CSP-blocked, quietly re-opening the LG München GDPR exposure both issues just closed. | SEV3 (dead permission weakens the guardrail on a closed GDPR issue; no live leak today) | ✅ Fixed 2026-07-30 (Issue 376a) | Dropped both remote font origins from `_CSP_BASE` (`style-src 'self' 'unsafe-inline'`, `font-src 'self'`) and corrected the stale comment at `main.py:308-317`; kept `'unsafe-inline'` in style-src (separate concern, still needed for the retained static pages' + new landing page's inline `<style>` blocks). Batched with the Issue 376a `main.py` change as suggested. `tests/test_static.py::test_security_headers_present_on_every_response` now asserts the origins are ABSENT instead of present. |
| 2026-07-30 | Issue 364 build — wiring `frontend/src/components/review/TranscriptEditor.tsx` for server-side discard | **`TranscriptEditor.tsx` is dead code — not imported by any page.** `grep`-confirmed: no `import ... TranscriptEditor` anywhere. Editor.tsx (the page that actually ships the same word-drag-select-to-cut UI) reimplements the identical apply/confirm/discard logic inline instead of rendering this component, per a stale comment at Editor.tsx:215/231 ("mirrors TranscriptEditor... stays in sync"). Fixed the discard bug in both places since Issue 364 explicitly named both call sites, but the duplication itself is unaddressed. | SEV3 (dead code + DRY violation, not a live-bug risk) | 📋 Open | Either delete `TranscriptEditor.tsx` if fully superseded by `Editor.tsx`, or refactor `Editor.tsx`'s inline cuts/confirm/discard logic to render it — confirm which with the owner before touching either. |
| 2026-07-30 | Issue 364 build — writing the discard regression test for `Editor.tsx` | **No test harness exists for `Editor.tsx`'s word drag-select → apply cuts → confirm/discard flow** (`Editor.test.tsx` has no coverage of it at all, confirm included). Reaching the "Keep original" button requires simulating `window.getSelection()`-based word selection, which the existing test file has no scaffolding for. Covered the discard fix instead via the identical shared logic in `CleanedPreviewConfirm.test.tsx` + `YourCall.test.tsx` (same endpoint, same idempotent-clear behavior); `Editor.tsx`'s own copy is un-regression-tested pending the DRY fix above. | SEV3 (test-infra gap, pre-existing) | 📋 Open | Add drag-select test scaffolding to `Editor.test.tsx` in a follow-up, or resolve via the DRY refactor above so `Editor.tsx` reuses the already-tested `CleanedPreviewConfirm`. |
| 2026-07-30 | W3 #375 merge verification | **`ORIGINALITY_SIMILARITY_THRESHOLD=0.92` is UNVALIDATED against real data.** No live `VOYAGE_API_KEY`, no Postgres/pgvector, and no real clip corpus exist on this box, so voyage-3.5's actual same-channel pairwise-cosine distribution was never measured. 0.92 was chosen deliberately high (few alarms) as the documented conservative fallback — it is NOT the literature 0.85, which is derived from other embedding models and does not transfer. Same for `ORIGINALITY_MIN_CLUSTER_SIZE=4` (not statistically derived). | SEV3 (advisory may under-fire, i.e. stay silent when it should warn; over-firing is the guarded-against direction and is unlikely at 0.92) | 📋 Open | Recalibrate on real creator clips once a beta channel has ≥12 clips with embeddings: measure the same-channel cosine distribution, pick the knee, and record the measurement in DECISIONS. Until then treat a silent advisory as "unmeasured", not "clean". |
| 2026-07-30 | close-out issue recount | **Issues 194 and 195 have no `Status` line of their own in `docs/issues.md`** — a status scan walks past their headings into a neighbouring issue's block and reads Issue 181's loudnorm text ("Two-pass `loudnorm` (−14 LUFS)…"), which has nothing to do with publishing to YouTube. The by-number index correctly shows both as ✅ DONE (2026-06-22), so this is a tracker-formatting defect, not a status error — but it silently corrupts any automated open/closed count. | SEV4 (tracker hygiene; corrupts automated counts) | 📋 Open | Give #194 and #195 their own `**Status**` lines matching the index. Worth doing next time `issues.md` is edited — the same class of gap hid #374's missing status this session. |
| 2026-07-31 | Issue 355 Phase 2 — rewriting the Dashboard's Review-queue card | **The Review-queue badge counts RENDERED clips, not shortlisted-unreviewed ones, so it disagrees with the queue it links to.** `Dashboard.tsx` sums `counts.rendered` from `/videos/clips/counts`, but since #377 the Review page defaults to the top-3 shortlist — so the card can read "8" and the queue show 3. Fixing it properly needs a new shortlisted-unreviewed field on `/videos/clips/counts`, i.e. a backend change that Issue 355 (a frontend naming/IA pass) deliberately avoids. | SEV3 (misleading count on the main dashboard; no data loss, no wrong action) | 📋 Open | Add a `shortlisted_unreviewed` count to the batched counts endpoint and point the card at it. In the meantime 355 fixed only the zero state (it now offers a way to fill the queue instead of a "0" over a button into nothing). |
| 2026-07-31 | Issue 355 Phase 3 — auditing the route table for the nav rewrite | **`App.tsx`'s `*` catch-all silently redirects every unknown path to `/dashboard` — there is no 404 and no index route.** A mistyped or stale URL (including a dead deep link from an email or an old bookmark) looks to the creator like "the app just took me to the dashboard", with no signal that the thing they asked for doesn't exist. Adjacent to the IA work but not in the #355 brief, and it changes error-handling behaviour rather than naming. | SEV4 (confusing but non-destructive; no data or auth impact) | 📋 Open | Add a real NotFound route rendering an `EmptyStatePrompt` back to `/dashboard`, and an explicit index route for `/app`. Small, self-contained follow-up. |
| 2026-07-31 | PR #67 CI — diagnosing the Visual-regression failure on the W5 branch | **The `Upload snapshot diffs on failure` step uploads nothing, so a failed visual-regression run leaves no way to see WHAT changed.** Run `30638142969` failed `empty-dashboard` (desktop+mobile) and the step reported `No files were found with the provided path: frontend/e2e/.results/. No artifacts will be uploaded.` — even though the Playwright log references `e2e/.results/…/empty-dashboard-diff.png` by name, so the files existed during the run. Root cause NOT established (candidates: `actions/upload-artifact@v4` glob behaviour on the dot-prefixed `.results` dir; a path resolved against the job's `defaults.run.working-directory: frontend` rather than the workspace root; or cleanup between steps) — logged rather than guessed. Consequence: the only way to inspect a visual diff today is to dispatch `ci.yml -f update_snapshots=true` and diff the regenerated baselines by hand, which is what this session did. | SEV3 (CI diagnosability — a failing visual gate cannot be triaged from the run alone; no product impact) | 📋 Open | Reproduce by forcing one `@visual` failure, then fix the path (try `frontend/e2e/.results/**` or an absolute `${{ github.workspace }}/…`) and confirm the artifact appears. Small, self-contained CI chore. `.github/workflows/ci.yml` "Upload snapshot diffs on failure". |
| 2026-07-31 | Pre-beta prod verification sweep (health + prod Playwright audit + live_smoke) | **Cloudflare Web Analytics is auto-injected by the edge on every HTML response and then blocked by our own CSP — so RUM data is silently ZERO and every visitor gets 2 console errors per page load.** Confirmed two ways: the prod audit logged the pair on all 30 audited pages, and `curl -A '<Chrome UA>' https://autoclip.studio/app/login` shows the injected inline script + `https://static.cloudflareinsights.com/beacon.min.js` in the HTML tail. `_CSP_BASE` sets `default-src 'self'` with no `script-src`, and prod `CSP_EXTRA_SOURCES` is empty, so both the external beacon and its inline bootstrap are refused. The inline script's sha256 differs per page, so a static hash allowance is not viable. | SEV3 (analytics blind spot + console noise on every page; no functional breakage) | 📋 Open | Preferred: turn OFF Web Analytics auto-injection in the Cloudflare dashboard (Speed → Optimization) so the strict CSP stays intact and the noise disappears. If the RUM data is actually wanted, add the beacon origin to `CSP_EXTRA_SOURCES` **with a nonce**, not `'unsafe-inline'` — weakening `script-src` to `'unsafe-inline'` for analytics is not an acceptable trade on a token-handling app. |
| 2026-07-31 | Pre-beta prod verification sweep (live_smoke on the VM) | **`/metrics` is DISABLED in production because `METRICS_TOKEN` is unset** — `config.py:935` fail-safes the endpoint off and logs `METRICS_TOKEN is unset in production — disabling /metrics`. Surfaced in the live_smoke run's own log output. This means the Prometheus scrape target that Issue 2519's SLO work assumes (`celery_tasks_total`, `http_request_duration_seconds`) does not exist on prod today, so there is currently no metrics-based signal behind the beta — only Cloudflare Health Checks on `/health`. | SEV3 (monitoring gap ahead of a multi-user beta; fail-safe behaviour is correct, the config is just unset) | 📋 Open | Set `METRICS_TOKEN` in the VM `.env` and restart `app`, then confirm `curl -H "Authorization: Bearer <token>" /metrics` returns a Prometheus exposition. Prerequisite for the SLO/alerting issue. |
| 2026-07-31 | Pre-beta prod verification sweep (prod Playwright audit) | **The logged-out SPA fires `GET /api/notifications` and takes a 401.** Seen once per browser context on the login page across all three viewports (3× in the 30-page sweep). Harmless but it means an unauthenticated visitor generates an authenticated-only API call, adding a 401 to prod logs and a failed request to the console on the very first screen a beta tester sees. | SEV4 (cosmetic + log noise; no data or auth impact) | 📋 Open | Gate the notifications poll on an authenticated session (skip the query when `/auth/me` has not resolved a creator). |
| 2026-07-31 | Pre-beta prod verification sweep (prod Playwright audit) | **`e2e/prod/audit.spec.ts` "audit analysis + review with a real video" FAILS by 120s timeout instead of skipping when the account has no video.** The guard `const href = await page.locator('a[href*="video_id="]').first().getAttribute('href').catch(() => null)` cannot reach its `.catch()` — `getAttribute()` waits for the element until the *test* timeout fires first, so `test.skip(true, 'no video…')` is dead code. Hit on all 3 projects in this run; it will bite identically on a real authenticated account that simply has no analyzed videos yet, making a clean account look like a failing audit. | SEV4 (test-harness defect; produces false failures, no product impact) | 📋 Open | Guard with `await locator.count()` (non-blocking) or pass an explicit short timeout, e.g. `.getAttribute('href', { timeout: 3000 })`, before deciding to skip. |
| 2026-07-31 | PR #68 CI — watching the Playwright job hang for 9+ min on browser install | **The Playwright browser cache never accelerates anything: `actions/cache@v4` is declared AFTER the `Install Playwright Chromium` step it is meant to accelerate, in BOTH the `playwright` and `visual` jobs.** `actions/cache` restores at the point it appears in the step list, so the restore always runs *after* `npx playwright install` has already downloaded Chromium — the save-on-post works, but nothing ever consumes it. Every CI run re-downloads Chromium twice (once per job). Normally that is a hidden ~60-90s tax per job; on run `30658123694` it stretched the install step past 9 minutes and made the gating job look hung. Confirmed at `.github/workflows/ci.yml` (playwright job ~549-560, visual job ~592-601) — identical inverted order. | SEV4 (CI latency + bandwidth waste on every run; no correctness impact, but it disguises a slow install as a hung gate) | 📋 Open | Move the `actions/cache@v4` step ABOVE `Install Playwright Chromium` in both jobs. The install then becomes a near-no-op on a cache hit. Alternatively drop the cache steps entirely and accept the download — but do not leave the current order, which pays the download cost AND the cache-save cost for zero benefit. |
| 2026-08-04 | Issue 413 font swap (frontend lint gate) | **Unused `eslint-disable` directive at `frontend/src/hooks/useStageStream.ts:100`** — `react-hooks/exhaustive-deps` no longer reports anything there, so the suppression is dead and lint exits with 1 warning on every run. Pre-existing (file untouched by 413). | cleanup | 📋 Open | Delete the directive (one line; `eslint --fix` also clears it). |
| 2026-08-04 | L26 serial merge (first vitest run on the merged tree) | **One vitest test flaked on the cold first run** — 623/624 with 1 failure on a run whose environment setup took ~300s (cold jsdom/env warm-up); two immediately-following full runs were 624/624 green, so the name was not captured. Likely a timeout-sensitive test under cold-start load, not a merge regression (frontend files were byte-identical to the green C branch). | SEV4 (CI/dev flake risk; no product impact) | 📋 Open | If it recurs, run with `--reporter=verbose` on the FIRST attempt to capture the test name, then either raise its timeout or de-flake the wait. |
| 2026-08-04 | L26 Issue 415 (Track A full-suite run before commit) | **`tests/test_response_models.py::test_every_documented_json_route_declares_response_model` FAILS on the pristine base commit (9fc4685)** — `GET /videos/{video_id}/peaks` and `GET /videos/{video_id}/stream` are documented JSON routes with no `response_model`. Pre-existing (verified by stashing all L26 changes and re-running); presumably introduced by whichever issue added the peaks/stream routes without updating the gate. Makes the unit lane red on a clean checkout. **L26 merge finding (2026-08-04): the failure is environment-dependent — under the main checkout's fastapi 0.137.1, `app.routes` holds deferred `_IncludedRouter` entries (no `APIRoute`s), so the guard iterates ZERO routes and passes VACUOUSLY (verified at 9fc4685 and on the merged tree, standalone and full-suite). The peaks/stream gap is real where routers flatten eagerly, AND the guard itself needs updating to traverse included routers under lazy-include FastAPI — otherwise it checks nothing.** | SEV3 (a structural gate is red on main in some envs and vacuous in others — masks new violations either way) | 📋 Open | Fix BOTH: exempt/declare peaks+stream, and make the guard resolve `_IncludedRouter` entries (e.g. walk `app.router` recursively or force flatten) so it actually inspects routes under fastapi 0.137+. |
| 2026-08-05 | Issue 395 build (frontend full-suite run) | **The node-26-breaks-jsdom gotcha (already documented in LEFT_OFF.md "nvm use 22 LIES") bit again: 35 phantom localStorage failures until `export PATH=…/v22.17.1/bin` was applied.** The gotcha lives only in LEFT_OFF prose, so every fresh session re-trips it. | SEV4 (repeat time sink; no product impact) | 📋 Open | Make it structural instead of prose: commit a `frontend/.nvmrc` with `22.17.1` AND a `package.json` `"engines"` field so the mismatch fails loudly at npm/vitest start. |
