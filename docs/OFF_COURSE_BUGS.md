# OFF_COURSE_BUGS — incidental defects found while doing something else

> A running log of bugs, fragilities, and surprises discovered **outside the scope of the
> task in flight**. The rule (see `CLAUDE.md` → *Off-Course Bugs*): when you hit one, **log
> it here in one line and keep going** — don't fix it inline unless it blocks the current
> task, and don't abandon the current task to chase it. Triage later: promote real defects
> into `docs/issues.md`, delete entries that turn out to be non-issues. This keeps the main
> pipeline on-course while ensuring nothing is silently brushed off.

## How to add an entry

One row per finding. Keep it short; link to the full issue once promoted. Severity uses
the same scale as the assessment rubric (BLOCKER / SEV1 / SEV2 / cleanup).

| Date | Found while | Bug / surprise | Severity | Status | Tracked / fixed in |
|------|-------------|----------------|----------|--------|--------------------|
| 2026-05-29 | Validating the `build_dna` idempotency fix (Issue 76) under a live Postgres | A mid-session **Redis death** surfaced as ~25 opaque `500`s across the TestClient suites (every limiter/health route) and a silent coverage drop — it looked exactly like a code regression, costing real time to rule out. Root cause: the slowapi limiter has no in-memory fallback (by design) and nothing asserted Redis was reachable before the suite ran. | SEV2 (test-infra legibility) | ✅ Fixed | conftest `pytest_configure` Redis fail-fast guard (`tests/conftest.py`) + `scripts/dev_session_setup.sh` + `.claude/` SessionStart hook |
| 2026-05-30 | Running Layer 0 gates for Issue 78a | ~~`run_layer0.py`'s `_CANDIDATE_SOURCES` lists a `knowledge/` package that does not exist; mypy aborts and the gate mis-counts.~~ **WITHDRAWN 2026-05-30 (Issue 78c): misdiagnosis.** `gate_mypy()` calls `_sources()`, which *does* filter non-existent paths (`if (REPO_ROOT / s).exists()`), so `knowledge/` is dropped and the gate reports the true count (verified: `run_layer0.py --gates mypy` → 30, matching a manual count). The `mypy=1` I saw earlier came from running a *raw manual* `mypy` with the unfiltered `_CANDIDATE_SOURCES` list, not from the gate. No code bug; nothing to fix. | — | ✅ Withdrawn (non-issue) | n/a |
| 2026-05-30 | User triggered "Build Creator DNA" from onboarding after Issue 83 deploy | `build_dna` Celery task crashed 4× with `ModuleNotFoundError: No module named 'dna'` (task id `c3b02e43-689d…`, 19:48–19:51 UTC). Root cause: Celery is started via the `celery` script at `/root/.local/bin/celery`, so Python's `sys.path[0]` is the script dir, not the WORKDIR `/app`. Master inserts CWD before loading `worker.celery_app`, but the lazy `from dna.brief import generate_brief` at `worker/tasks.py:498` runs in a forked pool worker where the resolver still can't find `/app/dna/`. The same fragility silently shadowed every other lazy first-party import in `worker/tasks.py` — only `dna.*` blew up because none of its modules were transitively pulled in at celery boot, so `sys.modules` had nothing cached. | SEV1 (prod DNA build broken end-to-end) | ✅ Fixed | `Dockerfile` — `ENV PYTHONPATH=/app` so first-party packages are discoverable regardless of script entry point. Decision logged in `docs/DECISIONS.md`. |
| 2026-05-30 | User reported "sync finds no shorts and no videos over 10 minutes" on `reesepludwick@gmail.com` / "backboard media" (actual: 20 Shorts + 3 long-form) | `youtube/analytics.py::sync_video_catalog` had **zero callers** in the entire repo — `grep -rn` returned only the definition. Neither OAuth callback nor the hourly `refresh_youtube_analytics` Beat task populated the `videos` table from the uploads playlist. Compounded by `/videos/link` and `/videos/upload` hardcoding `kind=VideoKind.long` and `classify_video_kind` using the stale `<=60s` Shorts threshold (YouTube raised the limit to 180s in Oct 2024). | SEV-0 (onboarding-blocking; no new creator could ever build DNA) | ✅ Fixed | Issue 87 (`docs/issues.md`) + DECISIONS entry. New `sync_channel_catalog` Celery task wired into OAuth callback + Beat refresh + new `POST /creators/me/catalog/sync` endpoint; link/upload now resolve kind from real duration; `SHORTS_MAX_DURATION_S=180` configurable. |
| 2026-05-31 | Adding Wave-3 Fix B router tests (`test_improvement_brief_router_returns_stream_url_on_happy_path`) | Two new TestClient router tests failed with **slowapi `429` rate-limit** when run in the full `pytest -q` lane (passed in isolation). Root cause: the slowapi limiter uses Redis storage and `_creator_key()` (`limiter.py:15`) falls back to `get_remote_address(request)` for unauthenticated requests — every `TestClient` call appears as IP `testclient`, so multiple tests hitting the same endpoint share the rate-limit bucket. The `10/hour` limit on `/me/improvement-brief` (line 39 of `routers/improvement.py`) is hit after ~10 tests across the suite. **The trap:** the test passes when run alone or in a slice but fails in the full lane — a classic ordering-dependent flake. The next dev adding a TestClient-driven router test will hit the same surprise. | SEV2 (test-infra legibility) | ✅ Fixed (workaround pattern) + ⚠️ not surfaced in conftest | Workaround: include a per-test session cookie (`create_session_token(creator.id)` for a fresh creator UUID per test) so the limiter keys per-creator instead of per-IP. `tests/test_progress_emit_wiring.py::test_improvement_brief_router_returns_stream_url_on_happy_path` + `..._fails_open_on_redis_down` both apply this. Follow-up: consider a conftest fixture that auto-resets the slowapi Redis state between tests, OR document the per-creator-cookie requirement in `tests/conftest.py` alongside the existing Redis fail-fast guard. |
| 2026-05-31 | Wave 6 done-vs-visible audit — scoping Fix D | `routers/videos.py::list_videos` filters `Video.source_uri IS NOT NULL` (Issue 90, intended to exclude catalog-sync rows). But `routers/videos.py::link_video` never sets `source_uri` — only `/videos/upload` does. So **every linked video is also excluded from the dashboard `/videos` list**, indistinguishable from catalog-sync rows. A creator who clicks "Link a video" sees a 200 OK, then the video silently disappears from "Your videos" — the canonical "done but not visible" pattern the audit was investigating. The Issue 90 DECISIONS entry incorrectly described `source_uri` as "unset for uploads/links" — uploads DO set it; links don't. | SEV1 (user-observable: link succeeds, row vanishes) | ✅ Fixed (Issue 139) | New `Video.origin` enum (`catalog\|link\|upload`); `list_videos` filters `origin != catalog` so linked videos appear (carrying `clippable: false`). Migration 0024 backfills from `source_uri`. DECISIONS 2026-06-16. |
| 2026-05-31 | Running Issue 103 Wave-9 carry-forward sweep | 11 pre-existing test failures in `test_catalog_sync.py`, `test_input_hardening.py`, `test_oauth_lifecycle.py`, and `test_retention_tasks.py` — all unrelated to Issue 103 scope, none in files I touched. Cause TBD (likely Issue 105 worker idempotency changes still in-flight). None blocked the Issue 103 test suite. | SEV2 | ✅ Resolved (no longer reproduces) | Triaged in Issue 146: the full suite is green on CI — 974 unit + 127 integration, 0 failures (runs 2026-06-17). These were in-flight Issue-105 worker-idempotency failures since landed; no open defect remains. |
| 2026-06-08 | Reviewing CI runs after pushing the empty-state envelopes commit | `tests/test_worker_pipeline.py::test_poll_clip_outcomes_uses_per_creator_median` failed in CI with `assert None is False` for `a_outcome.performed_well`. Log: `"advisory lock held — skipping poll_clip_outcomes"`. The task short-circuits when it can't acquire the advisory lock — a stale lock holder kept the column `NULL`. (Sat red 9+ days because integration ran main-only, never on PRs.) | SEV2 (flake → red 9+ days) | ✅ Fixed (Issue 143) | Root cause: the session-level `pg_advisory_lock` leaked across pytest-asyncio's per-test event loops on the shared module `admin_engine` pool. Fix: `worker/tasks.py` rollback-before-unlock (an aborted-txn poll no longer leaks the lock in prod) + an autouse `admin_engine`-dispose fixture (`tests/test_worker_pipeline.py`). Integration also moved onto PRs (Issue 144) so this class can't hide again. Verified green (127/127). DECISIONS 2026-06-17. |
| 2026-06-16 | Issue 142 (standing up the LLM harness on staging) | The staging stack (`root-*`, Issue 112) had been permanently degraded: `/health` = `{postgres:"error"}`, `FATAL: server login failed: wrong password type`. Root cause: PgBouncer `AUTH_TYPE=md5` against Postgres 16's `scram-sha-256` password encryption. This also silently blocked the **deferred Locust load test** (a pre-launch gate) — staging never actually worked. | SEV2 (staging unusable; blocks load-test gate) | ✅ Fixed (Issue 142) | `docker-compose.staging.yml` → `AUTH_TYPE: scram-sha-256`. Rebuilt staging from branch (project `cc139`); `/health` green; harness 10/10. Also: pinned `edoburu/pgbouncer:1.23.1-p3` is gone from Docker Hub → using cached `:latest`, re-pin later. |
| 2026-06-16 | Issue 139 (linked-video visibility fix) | The clip pipeline (`start_pipeline` → `ingest_video`) hard-requires `Video.source_uri` (`worker/tasks.py:446` raises "upload the file first" when null). Only **uploaded** videos ever set `source_uri`; **linked** videos (`link_video`) and **catalog** videos (`sync_video_catalog`) never do. So the Issue 125 "Queue for analysis" CTA — which targets `pending` catalog rows — leads straight to an ingest failure, same as linked videos. yt-dlp own-content download (`youtube/ingest.py::download_via_ytdlp`) exists but is NOT wired into the ingest chain. Net: clip generation works ONLY for uploads; link + catalog-queue are dead clip paths. | SEV1 (core CTA leads to failure; surfaced by Issue 139) | ✅ Fixed (Issue 139, Option A) | Decided AGAINST yt-dlp: research confirmed it violates the YouTube API Services ToS even for own content + risks Google OAuth verification (DECISIONS 2026-06-16). Instead: `POST /videos/{id}/queue` now returns 409 with upload guidance when `source_uri` is null (no doomed ingest); dashboard shows linked rows with an "Upload source file to clip" affordance (Google-Takeout-guided) instead of a clip CTA. |
| 2026-06-16 | Issue 138 SEV1 #6 (inert cache markers) | `routers/insights.py:570` carries a 4th live `cache_control: {type: ephemeral}` marker (analyze-performer, Haiku 4.5 = 4096-token floor) not in the SEV1 #6 scope. The system prefix there (instructions + DNA snapshot, `max_tokens=256` call) is likely well under 4096, making the marker inert (1.25× write premium, zero reads) — same class as the titles/thumbnails markers fixed in Issue 138. Not verified token-for-token; left untouched to keep the sweep scoped. | cleanup (cost waste, not correctness) | ✅ Fixed (Issue 140) | The system prefix is a single static ~30-token string (no DNA snapshot — that confirmation was wrong; DNA goes in the user message, capped at 800 chars). Far below the 4096 floor. Marker removed; regression test `tests/test_analyze_performer.py::test_analyze_performer_no_inert_cache_marker` pins absence. DECISIONS 2026-06-16. |
| 2026-06-17 | Running the suite after the Issue 143 starlette 1.3.1 bump | `fastapi.testclient`/`starlette.testclient` now emits `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.` on every TestClient construction. Tests pass (976), but it's noise and a future migration signal (httpx → httpx2 for the test client). | cleanup (test-infra noise) | 📋 Logged | Re-evaluate when bumping the test stack; likely a `httpx2` dependency swap. Not blocking. |
| 2026-06-17 | Issue 149 (insight sort) — rewriting the performer render block | `insights.html` rendered the performer **title** (`p.title`), `kind`, and `video_id` into `innerHTML` **unescaped**. Issue 138's XSS sweep escaped reflected LLM output + saved insights but missed this YouTube-sourced row, so a malicious video title would execute. | SEV1 (stored XSS via YouTube title) | ✅ Fixed (Issue 149) | Wrapped title/kind/video_id in `escapeHtml()` in `renderPerfPanel` (the block was being rewritten for the sort feature, so fixed inline). Regression pinned: `test_static.py::test_insights_performers_have_sort_control` asserts `escapeHtml(p.title` + `escapeHtml(p.kind)`. |
