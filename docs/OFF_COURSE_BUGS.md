# OFF_COURSE_BUGS — incidental defects found while doing something else

> A running log of bugs, fragilities, and surprises discovered **outside the scope of the
> task in flight**. The rule (see `CLAUDE.md` → *Off-Course Bugs*): when you hit one, **log
> it here in one line and keep going** — don't fix it inline unless it blocks the current
> task, and don't abandon the current task to chase it. Triage later: promote real defects
> into `docs/issues.md`, delete entries that turn out to be non-issues. This keeps the main
> pipeline on-course while ensuring nothing is silently brushed off.

> **2026-06-22 — Log slimmed.** All resolved/withdrawn rows (2026-05-29 → 2026-06-19) were
> moved to `docs/archive/off_course_bugs_snapshot_2026-06-22.md` (full history preserved).
> The three still-open rows below were promoted into the rebuilt `docs/issues.md` backlog and
> remain here until their issue closes.

## How to add an entry

One row per finding. Keep it short; link to the full issue once promoted. Severity uses
the same scale as the assessment rubric (BLOCKER / SEV1 / SEV2 / cleanup).

| Date | Found while | Bug / surprise | Severity | Status | Tracked / fixed in |
|------|-------------|----------------|----------|--------|--------------------|
| 2026-07-02 | Watching CI on PR #39 | **Playwright + visual-regression CI jobs always fail on the self-hosted runner** — `npx playwright install --with-deps chromium` exits 1 (no passwordless sudo), and `libatk-1.0.so.0` is missing so chromium crashes at launch. Same failure present on every merged PR including #38. The jobs fail-fast before any test runs, so results carry no signal about actual app behaviour. | SEV2 (CI noise; no code regression is masked because 0 tests run) | 📋 Open | Same root cause as libGL/libgomp gaps (runner needs system libs pre-installed). Needs a runner maintenance task or docker-in-docker for Playwright. Track in docs/issues.md. |
| 2026-06-30 | Debugging a prod "render loop" (clip stuck on "Rendering your clip…" spinner) | **`ClipPlayer.triggerRender` latches the render spinner forever.** It sets `requesting=true` but only resets it on the non-409 *error* path — a 202 (or 409) success never clears it. The spinner shows while `requesting \|\| render_status==='running'` and clears only when `render_uri` lands, so any render that never produces a `render_uri` spins forever, masking the real terminal state. Triggered live by the backend gap below: clip `ebd4cee8` failed permanently (`render_status=failed`, source purged), but the UI sat on the spinner instead of "Render failed". Confirmed via prod worker log (`render_clip_failed_permanent exc_type=ValueError 'Source video not available'`) + DB (`clips.render_status=failed`, never `running`). | SEV2 (UX: clip appears to render forever; no error, no recovery) | ✅ Fixed 2026-06-30 (frontend) | `triggerRender` now resets `requesting` in a `finally`, so the spinner is driven by server `render_status` — a fast-failing render surfaces as "Render failed" + retry. Regression test `ClipPlayer.test.tsx` "does not latch the spinner after a render request settles". |
| 2026-06-30 | Rung-4 live test: uploaded a real video + watched the pipeline | **CORE LOOP BROKEN — no uploaded video could ever render.** `ingest_video` overwrote `video.source_uri` with the extracted audio WAV and **deleted the original mp4** seconds after ingest. Render reads `source_uri`, so it downloaded audio-only and the 9:16 reframe's keyframe grab failed: `ffmpeg -i …wav -vframes 1 …jpg → "Output file does not contain any stream"`. Every clip → permanent `failed`; explains the historical **0/18 clips ever rendered**. Auto-render enqueue + worker pickup WORK — the source was the problem. Rungs 1–3 (local repro + live-smoke canary) all passed because they seed a real *video* as `source_uri`; only rung 4 exercised the real ingest→source=audio path. (Supersedes the earlier "source purged at 72h" framing — the video was discarded at ingest in seconds.) | **SEV1** (core upload→playable-clip loop non-functional in prod) | ✅ Fixed 2026-07-01 (migration `0039`) — needs deploy + re-upload | **migration `0039_video_audio_uri`**: added `videos.audio_uri`; ingest now stores the WAV on `audio_uri` and LEAVES `source_uri` = the video (no overwrite, no immediate delete). `transcribe`/`signals` read `audio_uri`; render reads `source_uri`. `purge_stale_source_media` purges both video + audio at 72h after `ingest_done_at` — matching COMPLIANCE.md. Reverses the now-obsolete Issue 110 Fix D (no overwrite ⇒ no orphan). Verified: `scripts/repro_ingest_render.py` (real ffmpeg ingest → render `done`), integration test in `test_worker_pipeline.py`, inverted static guard `test_issue_110.py::test_ingest_async_retains_source_video`. Existing clips unrecoverable (videos already deleted) → creators re-upload. See DECISIONS 2026-07-01. |
| 2026-06-30 | Debugging a prod `oauth_failed` sign-in error (error.png) | **The Issue 343 RLS role split broke ALL sign-ins.** The OAuth `/callback` runs PRE-auth: it creates the creator (RLS-exempt `creators`) then, in the SAME bare-session transaction, writes RLS-FORCED tenant tables — `youtube_tokens` (store_or_update_tokens, every sign-in) and `minute_packs`/`minute_deductions` (grant_minutes, new signups). There is no `get_current_creator` in this path, so db.py's `after_begin` listener never sets `app.creator_id`. Once the app started connecting as the non-BYPASSRLS `creatorclip_app` role (Issue 343, deployed 2026-06-30), those writes hit the `tenant_isolation` WITH CHECK against a NULL GUC → SQLSTATE 42501 → `ProgrammingError`, swallowed by the callback catch-all as `/app/login?error=oauth_failed`. Confirmed in prod logs (`OAuth callback failed: ProgrammingError`). 343's prod verification checked SELECT fail-closed + the RLS-exempt bootstrap but never exercised the callback's pre-auth WRITE path. | SEV1 (prod: complete sign-in outage, new + returning creators) | ✅ Fixed 2026-06-30 — needs redeploy | `routers/auth._exchange_and_persist` emits `SELECT set_config('app.creator_id', <creator.id>, true)` right after the flush (once the creator id exists), so the subsequent tenant-table writes satisfy RLS — scoped to that one creator, deny-by-default intact. Unit guard: `test_callback_sets_creator_rls_context_before_tenant_writes` (tests/test_auth.py). Integration proof under the real role: `test_oauth_callback_tenant_write_requires_guc` (tests/test_rls_isolation_integration.py) — insert fails without the GUC, succeeds with it. Full unit suite 2011 passed. Related Issue 343, [[Issue 344]] (the sibling authenticated-write fix). |
| 2026-06-30 | L21 Issue 340b RLS deny-by-default test (parallel build wave) | **Two tenant tables have RLS DISABLED:** `improvement_briefs` (added in migration 0009) and `creator_insights` both carry a `creator_id` but `relrowsecurity=false` — they were never added to migration 0010's `_TENANT_TABLES` list, so unlike clips/videos/dna_embeddings they have NO `tenant_isolation` policy. With the **Issue 343 role split now active** (app connects as `creatorclip_app`, `bypassrls=false`), RLS only protects tables that HAVE a policy — so these two leak cross-tenant at the DB layer; isolation rests **entirely** on app-level `WHERE creator_id=` filters with no deny-by-default backstop (the exact gap Issue 340 / Issue 79 set out to close). The 340b unset-context proof correctly EXCLUDED them (they fail the zero-rows assertion) and flagged it. | SEV2 (security: missing RLS backstop on 2 tenant tables; app-layer filter is sole protection) | ✅ Fixed 2026-06-30 — migration `0038` | **migration `0038_rls_improvement_briefs_creator_insights`** mirrors 0010: `ENABLE`+`FORCE ROW LEVEL SECURITY` + `tenant_isolation` policy (`USING creator_id = current_setting('app.creator_id', true)::uuid`) on both tables, re-grants DML to `creatorclip_app`. Both tables added to `_TENANT_TABLES` in `tests/test_rls_isolation_integration.py` so the cross-tenant + deny-by-default unset-context proofs now cover them (4 RLS tests green; full suite 2156 passed). Related [[Issue 340]], Issue 343. |
| 2026-06-30 | Running the Issue 341 smoke harness on the **prod VM** (`--seed`), `isolation` check | **Per-creator RLS isolation is NOT enforced in prod.** The harness `isolation` check failed: under a *different* creator's RLS context, the canary's clip was still visible (no cross-tenant filtering). Root cause (verified on the live DB): the app DB role **`creatorclip` has `rolbypassrls=true`**, and a `BYPASSRLS` role overrides even `FORCE ROW LEVEL SECURITY` (the tables ARE correctly `rls=true force=true owner=creatorclip`). So the RLS defense-in-depth backstop designed in Issue 79 / required by Issue 340 is inactive in prod — isolation currently rests **entirely** on app-level `creator_id` WHERE-clause filters; any query that omits one would leak cross-tenant with no DB backstop. The intended "prod role split" (app role WITHOUT bypassrls + separate privileged migration/worker role) was never completed (`db.py` comment: "until the prod role split lands"). No unit/integration test caught this — it only shows up against the live role. | SEV1 (security: load-bearing tenant isolation backstop disabled in prod) | ✅ Fixed in Issue 343 (2026-06-30) — VERIFIED on prod | **Activated the Issue 79 role split.** App now connects as `creatorclip_app` (`bypassrls=false`); `DATABASE_MIGRATION_URL` → the superuser `creatorclip` (BYPASSRLS) for Alembic + the 50 worker `AdminSessionLocal` cross-tenant sweeps + the 1 worker app-session (already sets `creator_id`). Verified live as `creatorclip_app`: no-context→0 videos (fail-closed), wrong-tenant canary→0 (harness `isolation` PASS), real creator under context→1049 (correct per-tenant), `creators` (RLS-exempt) visible for the auth bootstrap; app+worker healthy post-cutover. Kept the superuser as the migrate role (skipped the runbook's ownership-transfer to `creatorclip_migrate`) — see DECISIONS 2026-06-30. |
| 2026-06-29 | First live run of the Issue 341 smoke harness (`--with-llm`) against the real Anthropic API | **The per-clip LLM generators crash on the real API: `claude-sonnet-4-6` returns valid JSON wrapped in a ` ```json ` markdown fence, and `_parse_result` does a bare `json.loads(raw)` → `JSONDecodeError: Expecting value: line 1 column 1 (char 0)`.** Confirmed on `knowledge/clip_titles.py`, `clip_captions.py`, `clip_explain.py` (all three identical: `data = json.loads(raw_json)` with no fence strip / structured-output constraint). These power the **deployed** Review-surface features (Issues 322/323/325). Mocked unit tests never caught it because the mocks return clean unfenced JSON — the exact gap a live test exists to close. Likely intermittent in prod (fencing is stochastic), so it reads as "AI suggestions temporarily unavailable" rather than a hard outage. Same latent risk in every other `json.loads`-on-LLM-text site (`scoring.py`, `knowledge/titles|hooks|thumbnails|chapters`). | SEV2 (deployed Review LLM features broken/flaky against the real API) | ✅ Fixed in Issue 342 (2026-06-29) | **Two-track fix shipped + LIVE-verified.** Track A (no web_search) → native structured outputs (`output_config.format`) on `clip_titles`/`clip_captions`/`clip_explain` (verified at SDK 0.105.2 against the real API: fence-free, `json.loads` clean). Track B → `extract_json_block` on `hooks` (web_search→citations, structured output 400s), `chapters`, `scoring`, `thumbnails`; the clip_* trio also routes through the helper as defense-in-depth. Regression: `tests/test_llm_fence_parsing.py` feeds fenced JSON to each parser (the unit-lane gap). The Issue 341 smoke harness `--with-llm` now passes title/caption/explain (was 0/1 fail). |
| 2026-06-17 | Running the suite after the Issue 143 starlette 1.3.1 bump | `fastapi.testclient`/`starlette.testclient` now emits `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.` on every TestClient construction. Tests pass, but it's noise and a future migration signal (httpx → httpx2 for the test client). | cleanup (test-infra noise) | ✅ Fixed in Issue 274 | `httpx==0.28.0` replaced with `httpx2==2.4.0` in `requirements.txt` — starlette 1.3.1 prefers httpx2 when installed; warning resolves on install. |
| 2026-06-18 | Porting the dashboard to React (Issue 85c) | The dashboard fetches clip counts with one `GET /videos/{id}/clips` request **per done video** (N+1) — both the vanilla page and the React port do this (port parallelises via `useQueries`). For a creator with many processed videos this is N round-trips on every dashboard load. | SEV3 (perf, bounded by done-video count; not user-blocking) | 📋 Open | Promoted: folded into the per-video clips-map issue (batched `GET /videos/clips/counts` endpoint replaces the N+1). Backend change. |
| 2026-07-01 | Issue 349 ruff check on worker/tasks.py | **Pre-existing E501 line-too-long at `worker/tasks.py:4750`** — honesty disclaimer string literal in `_build_inapp_notification` is 101 chars (limit 100). Not introduced by Issue 349; was already in the codebase. | cleanup | 📋 Open | Log only — out of scope for Issues 348–350. |
| 2026-06-19 | Issue 164 live-site paid-flow run (`npm run test:prod:flows`) | **Video-analysis + title-optimizer flows timed out at 60s on the real account** (chat flow passed). Could be genuinely slow LLM generation (a UX gap — long spinner) or a real latency issue; one timeout isn't conclusive. Not chased further to avoid extra paid runs. | SEV3 (needs investigation) | ✅ Fixed in Issue 274 | Global timeout raised to 120s in `playwright.config.prod.ts`; flow assertions now check HTTP 200 response status (deterministic) rather than fully-rendered output, removing sensitivity to slow LLM rendering while still catching endpoint failures. If endpoints genuinely exceed 120s, file a dedicated perf issue. |
| 2026-06-29 | Verbose-logging task — owner surfaced 3 field bugs via prod screenshots (autoclip.studio) | **Render retry-storm:** the Review screen sits on "Rendering your clip… (~30s)" indefinitely while `render_clip` keeps retrying (max_retries=3, 60s delay) a render that won't succeed. | SEV2 (core Review UX; wasted compute/minutes) | ✅ Fixed 2026-06-29 | `render_clip` now classifies errors like `build_dna`: permanent failures (`ValueError`/`FileNotFoundError` — missing clip/source, bad range) are TERMINAL (set `failed`, no retry) instead of burning 3 retries; only transient errors retry. `_render_clip_async`'s error emit no longer falsely says "retrying". Tests: `tests/test_render_retry.py`. Verbose ffmpeg `_run` + Celery `task_retry`/`task_failure` logging capture the root cause when a real transient failure recurs. |
| 2026-06-29 | Same — chat screenshot | **Chat "Connection lost.":** the Pro assistant SSE drops and the UI dead-ends on a red "Connection lost." banner with no auto-reconnect/retry. | SEV2 (Pro chat unusable on drop) | ✅ Fixed 2026-06-29 | Root cause was client-side: `subscribeToChatStream` called `es.close()` on the FIRST transport `error`, killing EventSource's built-in auto-reconnect. Now a transport drop is transient — native auto-reconnect (CONNECTING) is allowed, a CLOSED socket is reopened with backoff, and only a server-sent named error or exhausting `MAX_CHAT_RECONNECTS` surfaces a terminal error. `Chat.tsx` shows a soft "Reconnecting…" state, preserves partial streamed text on terminal failure, and offers a Retry button. Server already sends `: keepalive` + `Last-Event-ID` resume (no change needed). Tests: `frontend/src/lib/taskStream.test.ts`. |
| 2026-06-29 | Same — job-list screenshot | **Stale FAILED jobs persist:** `FAILED` transcription rows linger with no way to act on the "please try again" message — `ActionCell` returned `null` for failed and `POST /videos/{id}/queue` rejected anything not `pending`. | SEV3 (UX clutter; not blocking) | ✅ Fixed 2026-06-29 | `/videos/{id}/queue` now accepts a `failed` video: resets it to `pending`, clears `failure_reason`, and re-runs the pipeline (re-deduction idempotent via `MinuteDeduction` UNIQUE). `VideoTable` shows a **Retry** button for failed+clippable rows (upload-path hint for failed rows with no stored source). Tests: `tests/test_issue_125.py::test_queue_endpoint_retries_failed_video`. |
| 2026-06-28 | Auto-render feature — running `ruff format --check worker/tasks.py` | `worker/tasks.py` carried pre-existing `ruff format` drift (hunks far from the auto-render edit — lines ~203, 758, 2347, 2373, 2611, 4314). The changed-file format gate would have failed, so the file was reformatted whole as part of this change; the incidental hunks are whitespace-only. | cleanup (format drift) | ✅ Fixed incidentally | Reformatted in the auto-render commit; no behavior change. |
| 2026-06-28 | Auto-render feature — running the integration lane (`-m integration`) on the web box | `test_generate_clips_*_integration.py` (and other integration tests) fail with `psycopg.OperationalError: password authentication failed for user "creatorclip"` — the SessionStart-provisioned Postgres credentials don't match the test DSN in this remote env. Unit lane (the verified gate here) is unaffected. | SEV3 (env/CI config, not product) | ✅ Resolved in Issue 344 (2026-06-30) | Root cause: the SessionStart hook's role-creation step silently failed (only `postgres`/`postgres` existed). Provisioned `creatorclip:dev_password` SUPERUSER + db + pgvector + `alembic upgrade head` (mirrors `scripts/dev_session_setup.sh`); the full integration lane then ran live (134 tests). The remaining bit-rot failures were real and are fixed in Issue 344 (see new row below). |
| 2026-06-28 | Auto-render feature — full unit lane | `tests/ingestion/test_transcribe.py::test_default_transcription_backend_sdk_is_installed` fails because the `deepgram` SDK isn't installed in this venv (default `TRANSCRIPTION_BACKEND=deepgram`). Pre-existing; unrelated to auto-render. | SEV3 (env/dep) | 📋 Open | Not chased — `pip install deepgram-sdk` (or pin in requirements) on this box; out of scope for auto-render. |
| 2026-06-29 | Batch-render task — running the integration lane (`-m integration`) on the web box | `test_worker_pipeline.py::test_poll_clip_outcomes_uses_per_creator_median` fails (`RuntimeError: Event loop is closed` in teardown) **on base too** (verified via `git stash`) — unrelated to the batch-render change. Pre-existing async-fixture teardown sensitivity in this test. The other 5 integration tests in the file (incl. the new `test_render_video_clips_downloads_source_once` and the render idempotency test) pass once the `creatorclip` PG role is provisioned. | SEV3 (test-infra flakiness) | ✅ Fixed in Issue 344 (2026-06-30) | Two root causes: (1) the test asserted the **obsolete** full-video `VideoMetrics` median — Issue 201 moved `performed_well` to a ≥3 **comparable-Shorts** baseline, so it now seeds 3 comparable Shorts/creator; (2) the "Event loop is closed" / silent skip was a **leaked session-level advisory lock** (a prior poll test's `pg_advisory_unlock` `finally` is skipped when its rollback raises on a dead-loop connection → later poll on a different pooled backend gets `acquired=False`). `tests/conftest.py` now clears leaked advisory locks before every integration test, generalizing this file's `_reset_admin_pool` fixture lane-wide. |
| 2026-06-30 | Standing up the live integration lane (Issue 344) | **The integration lane had bit-rotted — 21/134 failing — because CI's `integration` job sat red/unenforced and nothing ran it locally.** Three classes: (a) stale **mock stubs** drifted from the real signature — `generate_brief`/`generate_improvement_brief` now return `(text, usage)` but stubs returned a bare string → `ValueError: too many values to unpack`; (b) tests didn't seed the **minutes/billing gate** (`check_positive_balance`) or the **Issue 206 `payment_status=='paid'`** webhook guard added after they were written → 402/`ignored`; (c) a real **compliance bug**: `AuditLog.before_jsonb/after_jsonb` stored the JSONB `'null'` literal (SQLAlchemy `JSON` default) instead of SQL NULL, so the never-purged `creator.deleted` audit failed the Issue 247 "no PII payload retained" `IS NULL` assertion. | SEV2 (test-fidelity + 1 compliance correctness bug) | ✅ Fixed in Issue 344 (2026-06-30) | (a) conformed every stub to the real `(text, usage)` contract; (b) seeded `minutes_balance`/`payment_status`; (c) **product fix** — `JSONB(none_as_null=True)` on both audit columns so `None` → SQL NULL (no DDL change). Added per-integration-test DB isolation (TRUNCATE + advisory-lock clear) so the lane is order-independent. Full lane: 134 passed (det. + 2 random seeds); unit lane 1774 passed, 0 failed. |
| 2026-06-30 | Diagnosing the Issue 344 advisory-lock test leak | **Product concern (not just test-infra): the Beat tasks' session-level `pg_advisory_lock` release can be skipped.** `_poll_clip_outcomes_async` / `_refresh_youtube_analytics_async` acquire `pg_try_advisory_lock` and release in a `finally` that does `rollback()` then `pg_advisory_unlock`; if the connection is unhealthy the `rollback()` itself can raise, skipping the unlock and leaking the lock onto the pooled backend until `pool_recycle` (or process exit). In prod a leaked lock makes every future poll on that backend log "advisory lock held — skipping" and no-op until the connection recycles — delayed outcome polling / analytics refresh, self-healing within the recycle window. | SEV3 (prod: bounded, self-healing; observability gap) | 📋 Open | Triage: switch to `pg_advisory_xact_lock` where per-creator commit isn't required, or wrap the unlock so a failed rollback still attempts it on a fresh connection; add a metric/log when a Beat task skips on a held lock. Test lane is already protected (conftest advisory-lock clear). |
| 2026-06-23 | Issue 235 — adding `_is_first_keep` EXISTS query in `routers/review.py` | Existing `test_review.py` mocks use `session.execute = AsyncMock(return_value=result)` where `result` is a `MagicMock()`. When `_is_first_keep` calls `result.scalar()`, it gets a coroutine from AsyncMock that is never awaited — producing a `RuntimeWarning: coroutine 'AsyncMockMixin._execute_mock_call' was never awaited`. Tests still pass (coroutine truthiness makes `not result.scalar()` evaluate correctly), but it's noisy. Fix: update `test_review.py` fake_session to return a `MagicMock` with `scalar.return_value = True` for the EXISTS result. | cleanup (test-infra warning) | 📋 Open | Not fixed inline — tests pass, warning is pre-existing pattern in the suite. Triage with the next test-infra cleanup pass. |
| 2026-06-22 | Running the Layer-0 gates locally (GH Actions out of minutes) during the backlog-rebuild close-out | **`pip-audit` reports 1 non-baselined CVE: `msgpack` 1.1.2 `GHSA-6v7p-g79w-8964` (fix 1.2.1).** Transitive via `librosa` (runtime); not pinned in `requirements.txt`. Pre-existing / newly-published — not introduced by the docs-only commit. Exposure is low (msgpack is deserialized only from the trusted Celery/Redis broker + librosa internals, not from untrusted external input). The pytest CVE-2025-71176 alias `GHSA-6w46-j5rx-g56g` is already in the Issue-107 ignore-list. | SEV2 (dependency hygiene) | 📋 Open | Triage into the dependency-CVE lane (Issue 75 / Issue 107 re-baseline): either pin `msgpack>=1.2.1` in `requirements.txt` (preferred — real fix exists) or add `GHSA-6v7p-g79w-8964` to the accepted-risk ignore-list in `pyproject.toml` + `run_layer0.py` with a DECISIONS justification. |
| 2026-06-22 | Building Issue 182 (clip download endpoint) — tracing how clips are served | **Clip playback binds `<video src={clip.render_uri}>` to a raw `s3://bucket/key` URI** (`ClipPlayer.tsx`); there is **no media-serving endpoint or `/media` static mount**, so the browser cannot play it in prod (and a local FS path fails in dev too). The e2e fixtures null `render_uri` and `smoke.spec.ts` explicitly treats clip media as un-loadable noise — so this has been latent. | SEV2 (core Review playback) | ✅ Fixed in Issue 182 | Fixed inline (user opted in): the new `GET /clips/{id}/download?disposition=inline` presigned-URL endpoint now backs the player `<video src>`; the same helper serves the attachment download. |
| 2026-06-23 | Running full test suite after Issue 226 (retire legacy static UI) | **`test_data_export.py::test_start_export_returns_202_and_enqueues` and `test_issue_113.py::test_improvement_post_handles_concurrent_insert_race` fail with 429 Too Many Requests.** The in-process rate-limiter (using Redis or in-memory sliding window) leaks state across test modules when the full suite runs in a single process — these two tests run after rate-limit-heavy test files and exhaust the per-creator quota. Not introduced by security-platform; pre-existing ordering sensitivity. | SEV3 (test-infra flakiness) | ✅ Fixed 2026-06-24 | `tests/conftest.py` autouse `_isolate_app_state` now calls `limiter.reset()` (clears the RedisStorage limiter-prefixed keys) before/after every test, so accumulated buckets can't leak across modules. Verified: 3 consecutive full-lane runs all 1400 passed / 0 failed. |
| 2026-06-23 | Building Wave W0 lane agentic-caching-cost (Issues 218/220-223) — running unit test suite | **`test_data_export.py::test_start_export_returns_202_and_enqueues` fails with 429 Too Many Requests** even in isolation. The rate limiter is triggered when Redis is unavailable (test env). The `POST /creators/me/export` endpoint returns 429 instead of mocking through. Pre-existing — confirmed fails on the baseline commit (65a1d4f) with no code change. | SEV2 (test infra gap — endpoint mocks don't stub the rate limiter) | ✅ Fixed 2026-06-24 | Same root cause/fix as the row above: the autouse `limiter.reset()` clears buckets per-test. (The "fails in isolation" framing was a Redis-down env artifact; with Redis up + per-test reset the limiter starts empty.) Now passes in isolation and across 3 full-lane runs. |
| 2026-06-23 | Building Wave W1 lane observability (Issues 234/238/281) — running full unit test suite with `--override-ini="addopts="` to bypass pytest.ini addopts | **`conftest.py` Postgres guard check uses substring match `"integration" in marker_expr`** which is truthy for `-m "not integration"` (marker_expr contains the word "integration" as a substring). This means the guard fires even when the intent is to *exclude* integration tests, blocking test collection on this box where Postgres is unavailable. Workaround: `--override-ini="addopts="`. Pre-existing — introduced in Issue 267 conftest guard. | SEV3 (test-infra flakiness / developer friction) | ✅ Fixed 2026-06-24 | `tests/conftest.py` now strips `"not integration"` from the marker expr before the substring check, so the default unit lane (`-m "not integration and not quarantine"`) no longer trips the Postgres guard. Unit lane is runnable without Postgres again. |
| 2026-06-26 | Building Wave W0 lane llm-features-hardening (Issues 318-321) — running new unit tests | **`tests/conftest.py` fails to load because `routers/chat.py:259` has `@router.delete(..., status_code=HTTP_204_NO_CONTENT)` which triggers a FastAPI 0.115.x assertion `"Status code 204 must not have a response body"`.** The locally installed FastAPI (0.115.4) is older than the pinned version in `requirements.txt` (0.137.1). Same assertion applies to `routers/auth.py:454` (DELETE /me) and `routers/activity.py:34` (POST 204). Blocks the entire test suite on dev box; does not affect CI which installs pinned deps. | SEV3 (dev-box test-infra — blocks local unit runs when deps aren't from requirements.txt) | ✅ Fixed inline during Issue 326 (blocked tests) | Added `response_class=Response` to all three affected routes (`routers/chat.py`, `routers/auth.py`, `routers/activity.py`). This is the correct FastAPI pattern for 204 routes and is compatible with both 0.115.4 and 0.137.1. |
| 2026-06-24 | Verifying LLM backend; fixed the conftest guard above, then ran the FULL unit lane (first time runnable without Postgres) | **~10 backend unit tests were silently red** — masked because (a) the conftest guard blocked the unit lane on no-Postgres boxes and (b) self-hosted CI isn't running backend pytest yet (apt-deps step pending, per LEFT_OFF). After the guard fix: **1389 passed, 10 failed**. Triage: **no real prod regressions** — 8 are stale tests lagging shipped code [brand-kit migration renumber 0028→0029 (`test_brand_kit`); SPA-cutover `next_action` URLs `/static/*.html`→`/app/*` 85g (`test_static`, `test_empty_state_envelopes` ×2, `test_spa_serving`); Deepgram mock missing the `addons` arg added in Issue 251 (`test_ingest`); virality matcher flagging *negated* mentions "no virality language" (`test_compliance_no_virality`); `billing_refund` expecting 1 dispatch when `on_failure` now fires refund+notification (`test_billing_refund`)], and 1 is DB-dependent (`test_clip_counts_requires_auth` — needs Postgres / integration marker). | SEV2 (the suite drifted red unnoticed; code is correct) | ✅ 9/10 fixed 2026-06-24 — lane now **1399 passed / 1 pre-existing flake**. 📋 1 remaining (`test_clip_counts`). | Fixed: 2 cache-marker tests (`test_dna`/`test_identity_unit`); migration-renumber (`test_brand_kit` 0028→0029); SPA-cutover URLs (`test_static`→/app/dashboard, `test_empty_state_envelopes`→/app/insights); legacy-UI retirement (`test_spa_serving`→404, Issue 226); Deepgram `addons` mock (`test_ingest`); virality-negation whitelist (`test_compliance_no_virality`); Signals mock for the skip-reason path (`test_empty_state_envelopes::clips_empty`); refund dispatch count 1→2 (`test_billing_refund`). Added an autouse `dependency_overrides` clear in conftest. **Remaining:** `test_clip_counts_requires_auth` fails ONLY in full-suite order (passes in isolation) — an earlier test leaks engine state so the request path attempts a **sync** Postgres connection (`engine.raw_connection()`); needs a dedicated isolation hunt (likely `db.recreate_engine()`/`test_db_engine_config` global leak). Structural root cause unchanged: get backend pytest running in self-hosted CI so drift can't recur. |
| 2026-06-24 | Auditing LLM call sites for cost-correctness | **LLM cost ledger ignores cached tokens.** `billing/ledger._estimate_cost_usd` prices only `input_tokens`+`output_tokens`. The Anthropic SDK's `usage.input_tokens` is the **uncached remainder only** — `cache_read_input_tokens` (should bill 0.1× input) and `cache_creation_input_tokens` (1.25×) are dropped. `config.COST_CACHE_READ_MULTIPLIER=0.1` is referenced nowhere. `record_llm_usage` receives `cache_read`/`cache_creation` in its `usage` dict but doesn't thread them in. Net: cached traffic billed at 0×. `clip_engine/scoring.py` (1h-TTL DNA cache) and chat/runner are the biggest under-bills. Cache floor is 1024 tokens for Sonnet 4.6 (live-verified vs SOT) → more prefixes cache than assumed, so the under-bill is real. | SEV2 (systematic under-billing of cached LLM usage; money path) | 📋 Open — awaiting approval (money path) | Price cache tiers in `_estimate_cost_usd`; thread `cache_read`/`cache_creation` through `record_llm_usage` + the call sites that compute cost (`clip_engine/scoring.py`, `chat/runner.py`). |
| 2026-06-23 | Building Issue 244 (notification triggers) — running test_compliance_no_virality.py | **`test_no_virality_in_openapi_schema_descriptions` fails on the phrase "no virality language" in a docstring in `routers/creators.py:262`.** The phrase contains the regex-matched word "virality" and is not in the WHITELIST (only "does not promise virality" etc. are whitelisted). Confirmed pre-existing: fails on the baseline commit with no Issue 244 changes. | cleanup (false-positive in compliance scan) | 📋 Open | Add "no virality language" to the WHITELIST in test_compliance_no_virality.py, or rephrase the docstring in routers/creators.py to avoid the pattern. Low-priority false-positive. |
| 2026-06-23 | Building Issue 304 (redesign foundation) — running `npm run lint` | **Frontend `npm run lint` carries 10 pre-existing problems (6 errors, 4 warnings)**, none in redesign files: `Onboarding.test.tsx` unused `_type`/`_cb` args; `react-refresh/only-export-components` in `ActivityPanel.tsx` + `insights/InsightsNarrative.tsx`; `react-hooks/set-state-in-effect` + `exhaustive-deps` in `Editor.tsx` and `useStageStream.ts`; 2 unused `eslint-disable` directives. Confirmed pre-existing on the baseline commit. Issue 304 added **zero** new lint problems. | cleanup (lint debt; lint gate not currently clean at baseline) | ✅ Errors cleared (2026-06-24) | All **6 errors** resolved so `npm run lint` exits 0 (frontend CI job green): 2 unused-var via `eslint.config.js` `^_` ignore convention; `react-refresh/only-export-components` via extracting `deriveWhyNarrative` → `insights/narrative.ts` (project convention); 3 `react-hooks/set-state-in-effect` via justified `eslint-disable-next-line` (intentional reset/sync effects in just-shipped, not-yet-visually-verified Editor/useStageStream — proper `key`/during-render refactor still tracked below). **4 warnings remain** (non-blocking): exhaustive-deps + react-refresh warnings. Proper refactor of the 3 effect sites = open follow-up for the lint sweep. |
| 2026-06-24 | Issue 100 — running the full vitest suite after the frontend gate fixes | **Suite-wide vitest timing flake (~1 in 5 full runs).** A `findByRole(...)` would intermittently time out (1000ms default) under full-suite parallel load — the async render (react-query mock resolution + effects) hadn't settled yet. Hit *different* tests across runs (Dashboard "Queue for analysis", Insights "page-level framing") incl. files untouched this session → pre-existing, surfaced by load, not a logic bug (every failing test passes deterministically in isolation). | SEV3 (test-infra flakiness; intermittent CI redness) | ✅ Fixed (2026-06-24) | Added `configure({ asyncUtilTimeout: 5000 })` to `frontend/src/test/setup.ts` — gives `findBy*` room to settle under contention (esp. the self-hosted CI runner). Verified: 8/8 consecutive full-suite runs green (was ~1-in-5 red). Content assertions unchanged. |
| 2026-06-23 | Standing up the hybrid local CI/CD (DECISIONS.md) — running the gates locally before wiring the pre-push hook | **`ruff format --check .` fails on 43 tracked files** (`worker/tasks.py` + ~42 `tests/test_*.py`); the tree drifted from its own mandated formatting while the billing-disabled GH CI never enforced it. CI's `ruff check` is clean (the 7 issues seen locally were the untracked `tests/eval/` scaffold). This means the now-self-hosted `ci.yml` lint job (`ruff format --check .`) will go **red on its first real run**. The local pre-push hook ratchets format on *changed* files only, so this pre-existing drift does not block day-to-day pushes. | cleanup (style debt; self-hosted CI lint job red until cleared) | ✅ Fixed (2026-06-24) | Ran `ruff format` on all 43 tracked files (`git ls-files '*.py' \| xargs ruff format`); `ruff format --check` now clean (289/289), `ruff check` still green, all files `py_compile`-verified. `ci.yml` lint job will be green. |
| 2026-06-24 | Diagnosing a prod `/auth/callback` 500 (new-signup OAuth) on autoclip.studio | **`alembic upgrade head` silently rolls back in prod — every deploy's migration step is a no-op (exit 0, no error).** Root cause in `alembic/env.py` `do_run_migrations`: the Issue-270 `connection.execute("SET lock_timeout…/statement_timeout…")` calls run *before* `context.begin_transaction()`, auto-beginning a SQLAlchemy 2.0 transaction that alembic then treats as caller-owned and never commits; the async connection close discards all DDL. Result: prod DB stuck at rev `0027` while deployed code was at `0034`, so the new-signup callback's writes to `terms_version`/`privacy_version`/`terms_accepted_at` (0033) + `minimum_age_confirmed_at` (0034) hit `UndefinedColumn` → uncaught 500. `scripts/deploy.sh:51` runs the broken CLI path, so the drift was invisible. **Proven on prod**: buggy pattern → `persisted=False`; timeouts via `connect_args` → `persisted=True`. | BLOCKER (silent prod migration failure → new-user signup outage) | ✅ Fixed 2026-06-24 | **Prod unblocked**: backed up DB (`backups/pre-0034-*.sql.gz`), generated offline SQL (`alembic upgrade 0027:head --sql`) and applied via `psql` (carries its own `BEGIN/COMMIT`) → prod now at `0034`, all columns/tables present. **Code fix**: `alembic/env.py` moves the timeouts to `create_async_engine(..., connect_args={"options": "-c lock_timeout=5s -c statement_timeout=120s"})` and removes the pre-transaction `SET` execs, so migrations commit again. Needs: redeploy with the env.py fix; consider `/log-issue`; promote a hardening issue (callback should map `httpx.HTTPStatusError`/DB errors to a clean redirect instead of leaking 500). |
| 2026-06-24 | Visual-walking the redesign fidelity-polish screens (Settings + short Editor) under the mocked Playwright harness | **The e2e mock-api fixture has no `/creators/me/brand-kit` entry**, so `dispatch()` returns `{}` for it. `BrandKitSection`/`CaptionStylePanel` initialize controlled inputs from that data, so on load the values flip defined→undefined → React `"controlled input to be uncontrolled"` console warning. Only surfaces now because Settings + short-Editor were never in `smoke.spec.ts` `AUTHED_PAGES` (so the gap stayed latent). NOT a product bug — prod returns real brand-kit data; pure test-fixture gap. | cleanup (test-fixture gap) | 📋 Open | Add a `BRAND_KIT` fixture to `e2e/fixtures/mock-api.ts` `GET_TABLE` (`/creators/me/brand-kit`), and add `settings` + `editor` (both modes) to `smoke.spec.ts` so they're walked in CI. Low priority. |
| 2026-06-24 | Running the eval-harness/full suite at W0-fix integration (311–315) | **Quarantined clipboard-stealer's COMPILED BYTECODE still on disk** in `tests/eval/client/__pycache__/` (`clipboard_watch`, `clipboard_monitor`, `autostart`, `windows_runner`, `api_client`, `os_detect`, `track_rules`, `event_log` `.pyc`) + empty `tests/eval/dump/`. Source `.py` is gone (per LEFT_OFF quarantine) but `.pyc` is import-executable. Untracked (gitignored via `__pycache__`) so it never deploys, and no test imports `tests.eval.client` — inert. The `rm -rf` was safety-blocked (untracked dirs not agent-created), so removal is **pending user approval**. | SEV2 (malware residue, inert/un-deployed) | 📋 Open — awaiting user OK to `rm -rf tests/eval/client tests/eval/dump` | Manual deletion once approved; consider a `.gitignore`-independent CI check that fails if `tests/eval/client` reappears. |
| 2026-06-24 | Building Issue 311 (notify) — agent review | **`notify/copy.py` in-app body strings use host-less `/pricing` paths** (`trial_ending`: "Top up your minutes at /pricing"; `balance_low`: "Add more at /pricing"). These feed the in-app notification body (not the email templates fixed in 311), so the relative path renders wrong in the in-app surface. | SEV3 (in-app copy, low blast radius) | 📋 Open | Promote into the 316 notify backlog: build in-app links from `APP_BASE_URL` like the email templates now do. |
| 2026-06-24 | Building Issue 312/315 — running tests under system `python3.12` | **System `python3.12` has FastAPI 0.115.4 but `requirements.txt` pins 0.137.1**; the older FastAPI raises `AssertionError: Status code 204 must not have a response body` on `routers/chat.py:259`'s registration, blocking `conftest` import. The **code is correct** (`-> None`); it's a dev-env hygiene issue — tests must run via the project `.venv` (0.137.1), where the full suite is green. | cleanup (dev-env, not a code bug) | 📋 Open | Document the `.venv`-only test invariant; optionally pin the interpreter in a `Makefile`/`tox` target so `python3.12` system isn't used by mistake. |
| 2026-07-02 | W1 CHECK research — Issue 219 Batch-API spike | **`docs/assessment/llm/clip_scoring.md:72` claims scoring is "not user-blocking past the SSE progress bar" (202+poll)** — stale post-Wave-3-Fix-E: the SSE consumer deliberately stays subscribed until scoring completes (worker/tasks.py:2237→2283) and auto-render + `clips_ready` gate on it. The stale claim nearly justified routing scoring through the no-SLA Batch API. | SEV3 (doc staleness with decision blast radius) | 📋 Open | Correct the assessment doc's latency section; the 219 DECISIONS entry (2026-07-02) records the accurate flow. |
| 2026-07-02 | W1 kickoff — confirming prod deploy of `2dbd17c` | **`LLM E2E Nightly` workflow failed on main (run 28565459895, 53s — likely setup/credential failure, not a test run).** Flag-gated live-API harness from Issue 319; not investigated to stay on-course for W1 round 1. | SEV3 (nightly signal red; prod deploy itself succeeded) | 📋 Open | Pull the run log; if it's a missing `RUN_LLM_E2E`/API-key secret on the runner, fix the workflow env; else triage as a real regression. |
| 2026-07-02 | Building Issue 202 (per-retrain NDCG emission) — mypy on the harness extraction | **`scripts/eval_efficacy.py:37` imports `get_sessionmaker` from `db`, which doesn't exist** (db.py exports `AsyncSessionLocal`/`AdminSessionLocal`/`get_session`) — the Issue-198 real-data script would `ImportError` on a DB host. Pre-existing at the base commit (mypy flags it there too); the script has never been run against real data (that's the parked staging step of 198/200). | SEV3 (dead code path in an ops script; blocks the 198/200 staging runs when attempted) | 📋 Open | Fix the import to `AsyncSessionLocal` (or add a session-factory accessor) before the 198/200 staging runs; one-line fix + smoke `--help` run. |
| 2026-07-02 | PR #41 CI — first run of the Migration-lint gate with real migration changes | **The Squawk gate (Issue 270) had never actually run: `npm install -g squawk-cli` fails on the self-hosted runner (no node toolchain, exit 127), AND the lint step had an off-by-one — `alembic upgrade "REV:+1" --sql` renders the migration AFTER the changed one, with the newest one erroring into suppressed stderr → empty stdout → trivial pass.** Also: env.py's Issue-270 timeouts live in online-mode `connect_args` only, so offline `--sql` scripts (a real prod path — the 0027→0034 psql apply) carried no lock/statement timeouts. | SEV2 (a deploy-safety gate that could never fail) | ✅ Fixed inline 2026-07-02 (blocked PR #41) | ci.yml: `pip install squawk-cli` + render `down_revision:revision` per changed file + `set -o pipefail`; env.py: offline mode now emits `SET lock_timeout='5s'`/`statement_timeout='120s'`; `.squawk.toml` excludes the two style rules (bigint/text) contradicting the existing schema — unsafe-op rules stay active per the Issue-270 DEC. Verified locally: all 3 new migrations (0041–0043) lint clean. |
| 2026-07-02 | Fixing the PR #41 order-dependent publish-test failure — ran the full suite randomized locally (pytest-randomly seed 20260702) | **2 more order-dependent failures: `test_health.py::test_health_storage_ok_in_local_backend` + `test_health_postgres_down_returns_degraded`** — pass in isolation, fail only in a specific full-suite random order. Poisoner NOT identified (not the W1-new test files, not test_rate_limiting alone — needs the dedicated isolation hunt). Same class as the 2026-06-24 `test_clip_counts_requires_auth` entry (engine/app-state leak). Pre-existing: test_health untouched since main; only newly visible because pytest-randomly wasn't installed locally until today. | SEV3 (flake landmine — a future CI seed can hit it) | 📋 Open | Dedicated isolation hunt: bisect the seed-20260702 module order ahead of test_health; likely a settings/storage/engine global leak. Consider pinning `-p randomly --randomly-seed=<fixed>` in CI until fixed. |
| 2026-07-02 | Deploying PR #41 (migrations 0041–0043) to prod | **Deploy failed at "Run migrations": 0041 emitted `CREATE TYPE render_status_enum` (DuplicateObject) despite `create_type=False`** — the kwarg was on generic `sa.Enum`, where it is dialect-dependent; AND the deploy's migration step runs with the VM's own `/root/.local` Python packages (unpinned SQLAlchemy/alembic — version skew vs CI's pinned stack, which is why CI's fresh-DB bootstrap passed). Prod safe: transaction rolled back (DB stayed at 0040), failure was before the image swap, /health green on the prior image. | SEV1 (deploy pipeline blocked; no prod impact) | ✅ Fixed 2026-07-02 (hotfix PR) | 0041 rewritten with dialect `postgresql.ENUM(create_type=False)` for both enums + explicit `checkfirst=True` create for `summary_status_enum`; proven on real PG (0001→0043, down/re-up) + offline `--sql` render + squawk clean. FOLLOW-UP to promote: pin the VM migration runner to the image's deps (run `alembic upgrade` inside the app container, not host Python) — tracked as a residual under Issue 352. Also: deploy log warns "No BACKUP_R2_BUCKET configured — migrating WITHOUT safety dump" (Issue 256 operator step still pending). |
| 2026-07-02 | Building Issue 352 Batch K (CaptionStylePanel SSE follow) | **Styled re-render of an already-rendered clip is a worker no-op:** `POST /clips/{id}/render` (routers/clips.py ~:425-509) persists the new `style_preset` but never resets `render_status`, and the render task (worker/tasks.py ~:1373) skips any clip with `render_status=done AND render_uri` — emits `done` without applying the style. "Render with style" on a done clip silently changes nothing. | SEV2 (silent UX lie on the review surface) | 📋 Open | Promote to issues triage: the render endpoint should reset `render_status`/`render_uri` (or version the render) when a new style is submitted for a done clip; add a regression test on the re-render path. |
