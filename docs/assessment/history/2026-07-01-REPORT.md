# CreatorClip — Production Assessment

**Date:** 2026-07-01  ·  **Commit:** `f70a857` (branch `fix/retain-source-video-for-render`)  ·  **LOC:** ~52.9k non-test source (py/ts/tsx/css, excl. tests/node_modules/dist/venv)  ·  **Tests:** pytest ran to completion @ **79.06%** line coverage; ruff/mypy **regressed off baseline** (see Layer 0)

> **Method note (per user's directive this run):** every best-practice, SDK, library, and security claim below was verified by a per-module subagent against **current official documentation** (Anthropic, Stripe, YouTube/Google, React Router, `limits`, AssemblyAI, SQLAlchemy) with the source URL + check-date recorded in the module file — not from memory. Findings whose basis could not be doc-verified were dropped or marked `(unverified)` / `(needs-runtime-confirmation)`.

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

**0 BLOCKER · 6 SEV1 · ~54 SEV2 · large cleanup tail.** No launch-fatal defect: per-creator isolation holds on **every** creator-scoped query (app-level `WHERE creator_id` traced + Postgres RLS backstop verified across all modules; the one gap — chat — is a defense-in-depth hole with no live leak), money/media Celery tasks are idempotent on stable keys, tokens always via `decrypt()`, and no virality promise anywhere. The app works end-to-end. **Two of the prior run's three SEV1s are now fixed** (notify blank-email templates; queue-path `aset_owner` SSE-404) and a third was **mitigated** (slowapi sync-Redis loop-block → Issue 312 landed, now a SEV2 pin-hygiene residual).

CONDITIONAL is gated on: **(1) two Layer-0 gate regressions** — ruff 0→21 and mypy 0→2 — which violate CLAUDE.md's Phase-4 "no regression vs baseline" close-out rule; and **(2) six SEV1s**, of which three are the ones that should not ship to a *paid* beta: **billing's Stripe retry config is a silent no-op** (checkout gets 0 retries), the **React SPA has no error boundary** (any render throw blanks the whole app), and an **uncounted 15-conn-per-replica event-log DB pool** sits outside the PgBouncer connection budget (scale axis A). The other three SEV1s (chat BYPASSRLS defense-in-depth, worker blocking-Resend, improvement `pause_turn`) are real but not beta-fatal.

---

## Layer 0 — deterministic gates (from `_machine.json`)

| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | **21** | 0 | ❌ regressed (all 21 in `tests/`+`scripts/`: import-sort, unused imports, nested-`with`; 9 auto-fixable) |
| mypy | **2** | 0 | ❌ regressed (both in **production `routers/`** — `None` passed where `str` expected) |
| coverage | **79.06 %** | 75.2 % | ✅ (+3.86) |
| bandit | high 0 / med 0 | 0 / 0 | ✅ |
| pip-audit | 6 | 0 | ⚠️ **local-venv drift only** — `requirements.txt` pins jinja2==3.1.6 / msgpack==1.2.1 (fixed); the 6 hits are the venv's stale jinja2 3.1.2 + dev-only pip/pytest. CI-authoritative (correct pins + `pyproject.toml` ignore-list) ≈ 0. |
| freshness | 33/33 days | 90-day threshold | ✅ |

**The two mypy errors are genuine production None-safety bugs (elevate to SEV2):**
- `routers/thumbnails.py:227` — `list[str | None]` passed to `analyze_thumbnail_patterns(list[str])` (a `None` image URL reaches the vision call).
- `routers/insights.py:564` — `video_title: str | None` passed to `_build_analysis_prompt(video_title: str)`.

**Top untested load-bearing paths (carry-forward):** ffmpeg render shell-outs + worker R2 paths remain mocked (coverage is line-count, not behavior, on those); no fresh Locust run on this commit for the pool-math axis.

---

## Layer 1 — module register (ranked, doc-verified)

**Tally across 16 assessed units: 0 BLOCKER · 6 SEV1 · ~54 SEV2 · ~42 cleanup + 21 ruff.**

### SEV1 (6)
| # | Module | Location | Issue (doc-verified) | Backed fix |
|---|---|---|---|---|
| 1 | billing | `billing/stripe_client.py:34` | `stripe.max_network_retries = 3` is a **no-op** under the v8 `StripeClient` (module globals ignored; unset → resolves to **0** retries — traced in SDK source & confirmed vs Stripe "No global config" docs). Checkout + reconcile run with 0 retries; a transient blip → 502 to the user. Idempotency keys already present, so retries are safe. | Delete line 34; pass `max_network_retries=3` to the `StripeClient(...)` constructor. Add a test asserting the client's resolved retry count is 3. |
| 2 | frontend | `src/App.tsx:33-69` (+ `main.tsx:8`) | **No route `errorElement`/`ErrorBoundary` and no `onUncaughtError` on `createRoot`.** Per React Router v7 + React 19 docs (cited in-file, 2026-07-01), a render throw in any page unmounts the root → **blank SPA, no recovery**. Confirmed React 19.2.6 / RR 7.18. | Add a root `errorElement` on `RootLayout` rendering a branded fallback via `useRouteError()` with reload/back affordance; add `onUncaughtError`/`onRecoverableError` for telemetry. |
| 3 | _root_infra | `event_log.py:70-78` | A second DB engine (`pool_size=5,max_overflow=10` = **15 conns/replica**) to `logs_database_url` which **defaults to `DATABASE_URL`** (same PG/PgBouncer). It's on the **hot path** (`main.py:374` records on every non-skipped request) but is **absent from the per-pod budget** in `db.py:35-38` (scale axis A). | Pin it small (`pool_size=2,max_overflow=3` — telemetry is best-effort), and add it to the `total_db_connections` inequality in `docs/DEPLOYMENT.md`. (needs-load-confirmation vs the 25-conn PgBouncer sidecar sizing.) |
| 4 | chat | `worker/tasks.py:4364` (+ `chat/runner.py:53`, `chat/tools.py:529`) | Pro chat agentic loop runs under **BYPASSRLS `AdminSessionLocal` with no `creator_id` GUC** → RLS is *not* a backstop on the app's most prompt-injection-exposed surface. Isolation is correct **today** (every executor has an explicit `WHERE creator_id`), but one forgotten filter in a future tool = silent cross-tenant leak. No live leak. | Run the chat turn on app-role `AsyncSessionLocal` with `session.info["creator_id"]=cid` set before the first query; extend `alembic 0010._TENANT_TABLES` to the chat-reachable child tables; regression test that a creator-B session cannot reach a creator-A id. |
| 5 | worker | `worker/tasks.py:4626` | Blocking `resend.Emails.send` (**no timeout** — verified none in `notify/mailer.py` or config) invoked directly in `async def _send_notification_async` **while holding 1 of only 4 admin DB conns** (session open :4519→commit :4660). Fan-out-heavy (per-creator lifecycle + Beat sweeps). A Resend stall blocks the worker loop AND can exhaust the admin pool. Sibling `list_recent_paid_sessions` IS offloaded — inconsistency, not a constraint. *(Severity reconciled: the `notify` agent rated this SEV2 as prefork-bounded; kept SEV1 for the no-timeout + admin-conn-hold = scale axes B+E.)* | Commit the delivery/in-app rows first to release the DB conn, then `await asyncio.to_thread(mailer.send, …)`; add a `RESEND_TIMEOUT_S` (~10s) passed to the SDK. |
| 6 | improvement | `improvement/brief.py:132-218` | `web_search` enabled (:96) but **`stop_reason == "pause_turn"` is never handled** on either the `.create` or streaming path (verified vs Anthropic server-tools docs). On a paused search turn both return the last *text* block — the "let me search…" preamble — so the user silently gets a truncated brief. `warn_if_truncated` only catches `max_tokens`. Worsened by `web_search_20260209` with no `max_uses`. | Loop `while stop_reason=="pause_turn"`: re-send assistant content with the same `tools`, cap ~5; for streaming use `stream_message` (returns full Message). Add `max_uses`. Add a pause_turn test. |

### SEV2 — lead finding per module (full lists in `docs/assessment/modules/<module>.md`)
| Module | SEV2 | Lead finding (doc-verified) |
|---|---|---|
| routers | 8 | Unauthenticated `POST /api/activity` splats client `**safe_extra` into `log_event` → `TypeError`/`KeyError` 500 + log-injection (`activity.py:57-64`, still present). |
| _root_infra | 7 | `ENV: str` free-string gate — a typo (`ENV=prod`) silently disables **every** prod fail-fast (secret checks, `/docs` lockdown, HSTS). Fix: `ENV: Literal[...]`. Plus: unpinned `limits` transitive dep underneath the Issue-312 limiter mitigation (a silent bump could disable rate limiting); JWT secret no min-length; MultiFernet rebuilt per `decrypt()`. |
| worker | 7 | Broker `visibility_timeout=3600` decoupled from `CELERY_SOFT_TIME_LIMIT_S`; the load-bearing `soft < hard < visibility` invariant lives only in a comment → redelivery/duplicate risk if soft-limit is raised. |
| clip_engine | 4 | `ranking.py:127` idempotency guard reads creator-scoped `clips` by `video_id` with **no `creator_id`** predicate (defense-in-depth; worker path); post-snap `end_s` can exceed source duration → render failure. |
| improvement+ | 4 | (combined file) `improvement` inert sub-floor cache marker; `upload_intel.best_upload_windows` filters malformed rows **after** the top-N slice → under-fills; `optimal_gap_hours` ignores week wraparound. |
| youtube | 3 | `videos.insert` charged **100 units against the shared read pool**, but Google bills it to a separate 100-calls/day bucket at 1 unit/call (verified developers.google.com/youtube/v3/determine_quota_cost, 2026-07-01) → uploads prematurely exhaust interactive read/analytics quota. |
| preference | 3 | `features.py:22-34` NaN-guards only `dna_match`; a NaN in any of the other 6 features fails the whole sklearn retrain and poisons the predict-time rerank sort. |
| ingestion | 3 | `transcribe.py:250` — AssemblyAI `status=error` still returns empty segments as a charged "success" (no refund, garbage clips); verified **NOT** fixed by Issue 334 against the AssemblyAI SDK docs. |
| chat | 3 | Token usage logged with the generic `ANTHROPIC_MODEL` instead of the actually-invoked `ANTHROPIC_MODEL_CHAT`/`_INTAKE` → cost dashboards misattribute per-model spend. |
| billing | 2 | Stripe `Idempotency-Key` is the bare `intent_id`, **account-scoped** across all creators (collision/cross-tenant idempotency risk). |
| knowledge | 2 | Untrusted transcript/title text placed in the **system role** in the older builders (`titles`/`thumbnails`/`hooks`) — violates the module's own `UNTRUSTED_CONTENT_POLICY` + Anthropic prompt-injection guidance; the newer clip builders correctly `wrap_untrusted(...)` in the user turn (inconsistent). Also: 1h cache marker inert below the 1,024-tok floor when DNA brief is empty (new creators get zero cache benefit + 2× write premium). |
| notify | 2 | Same no-timeout blocking Resend send (SEV1 in `worker`); prior blank-email SEV1 confirmed **FIXED** (caller supplies vars + `StrictUndefined`). |
| frontend | 3 | `VideoTable.act()` raw `fetch` POST with no try/catch → button stuck `busy` on a network blip. |
| youtube/mypy | — | (Layer-0) `routers/thumbnails.py:227` + `routers/insights.py:564` None-safety type errors. |
| dna | 1 | `embeddings.py:31` tenacity `@retry` has no `retry=` predicate → retries permanent Voyage errors (auth/bad-request) 3×; scope to transient exceptions. |

**Module verdicts:** all 16 units **NEEDS-WORK** except `analysis` = **clean** and `upload_intel`/`dna` near-clean. **0 modules ship a cross-tenant leak.**

---

## Layer 2 — scale checklist (`scale-checklist.md`)

| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | **NEW SEV1:** `event_log.py` uncounted 15-conn/replica pool to the same DB, on the request hot path, outside the PgBouncer budget. Primary 15+5/pod under PgBouncer txn mode stands; no fresh Locust on this commit. |
| B Async loop hygiene | ⚠️ | Prior slowapi sync-Redis SEV1 **mitigated** (Issue 312: `socket_timeout` + async path). Residual: worker blocking-Resend SEV1 (prefork-bounded); unpinned `limits` transitive dep (SEV2) threatens the mitigation. All `task.delay` sites offloaded. |
| C Celery idempotency | ⚠️ | Money/media tasks idempotent on stable keys (UNIQUE + advisory-lock). Open: `visibility_timeout` decoupled from soft-limit; advisory-lock unlock-without-rollback sites; `generate_and_rank_clips` check-then-insert race. |
| D Tenant isolation | ✅ (1 ⚠️) | Every creator-scoped query traced to `WHERE creator_id`; RLS backstop verified. **One defense-in-depth gap:** chat runs BYPASSRLS with no GUC (SEV1) — correct today, no live leak. |
| E Backpressure | ⚠️ | **Billing 0-retry (SEV1)**; no timeout on Resend; AssemblyAI silent-empty-success; youtube quota under-count. Timeouts present on most other external clients. |
| F Rate limit / quota | ⚠️ | Per-creator limits confirmed; limiter mitigation depends on the **unpinned `limits`** dep; per-creator usage quota still a pre-launch TODO. |
| G Observability | ⚠️ | Inert cache markers + misleading "clears 1024 floor" comments (knowledge); chat token log misattributes model; thumbnail vision `None`-URL path (mypy). Structured logs + token logging otherwise present. |
| H Migration & pgvector | ✅ | New migrations 0038 (RLS on `improvement_briefs`/`creator_insights`) + 0039 (retain source video / split `audio_uri`) landed; HNSW/IVFFlat index intact. Online-safety not re-audited this run — carry-forward ✅. |
| I Secrets / deletion | ⚠️ | `decrypt()` safe; MultiFernet rotation works. Open (`_root_infra` cluster): `ENV` free-string gate, JWT secret no min-length, MultiFernet rebuilt per-call. |

**Per the checklist's own rule:** no open BLOCKER → not **NO**; axes A–F lack fresh load evidence / carry SEV1s → not **YES**. ⇒ **CONDITIONAL.**

---

## Diff vs previous report (2026-06-24, `a503ade`)

### Fixed / mitigated
- ✅ **SEV1 notify** blank-subject / host-less emails — caller now supplies the vars + `StrictUndefined` (verified in-file).
- ✅ **SEV1 e2e** `POST /videos/{id}/queue` missing `aset_owner` → live-progress SSE 404 — routers agent confirms **fixed**.
- ✅ **SEV1 → SEV2 _root_infra** slowapi sync-Redis loop-block — Issue 312 mitigation landed; residual is now the unpinned `limits` transitive pin.
- ✅ coverage 76.71 % → **79.06 %**.

### New this run
- 🔴 **SEV1 billing** — `stripe.max_network_retries` module-global no-op (0 retries) — re-severitied up after SDK-source verification (was a SEV2 "no-op" note before).
- 🔴 **SEV1 _root_infra** — uncounted `event_log` connection pool (axis A) — newly surfaced.
- 🔴 **SEV1 improvement** — `web_search` `pause_turn` unhandled — newly surfaced against live server-tools docs.
- 🔴 **SEV1 frontend** — no root ErrorBoundary (RR v7 / React 19).
- 🟡 **SEV2 knowledge** — untrusted transcript/title in the **system role** (prompt-injection posture) in older builders.
- 🟠 **Layer-0 regressions** — ruff 0→21 (tests/scripts), mypy 0→2 (production routers None-safety).

### Reaffirmed (carry-forward)
- 🔴 **SEV1 chat** BYPASSRLS defense-in-depth gap (no live leak); 🔴 **SEV1 worker** blocking-Resend; youtube quota under-count; AssemblyAI silent-empty; Stripe idempotency-key scoping; `ENV` free-string gate.

### Regressed
- ruff (0→21) and mypy (0→2) gates — the only true regressions; both violate the CLAUDE.md close-out "no regression vs baseline" rule.

---

## Top 8 actions, in order

1. **Restore the Layer-0 gates to green** — `ruff check . --fix` (9 auto-fix, the rest trivial test/script hygiene) and fix the 2 mypy None-safety errors (`routers/thumbnails.py:227`, `routers/insights.py:564`) — guard/`str(... or "")` the nullable args. Cheapest, unblocks the close-out rule.
2. **Fix the Stripe retry no-op (billing SEV1)** — pass `max_network_retries=3` to the `StripeClient` constructor before taking money in beta; add a resolved-retry-count test.
3. **Add the root ErrorBoundary (frontend SEV1)** — a route `errorElement` + `onUncaughtError`; one render throw currently blanks the whole app.
4. **Pin the event-log pool small + budget it (_root_infra SEV1)** — `pool_size=2,max_overflow=3` and add it to the `docs/DEPLOYMENT.md` connection inequality (axis A).
5. **Handle `web_search` pause_turn (improvement SEV1)** — continuation loop + `max_uses`; add a pause_turn test.
6. **De-risk the worker Resend send (worker SEV1)** — commit rows first to release the admin conn, then `to_thread` the send with a `RESEND_TIMEOUT_S`.
7. **Give chat an RLS backstop (chat SEV1)** — run the turn on the app role with the `creator_id` GUC + extend `_TENANT_TABLES`; keep the explicit filters.
8. **Pin `limits==<resolved>` + fix the youtube upload-quota bucket + move untrusted transcript text out of the system role (knowledge)** — three SEV2s that each remove a silent-failure or injection foothold.

After actions 1–4 land (the gate regressions + the three paid-beta SEV1s) and a fresh Locust run confirms axis A/B under the beta's replica count, the verdict flips to **YES for the ≤100-user beta**; 5–8 + the SEV2 tail are scheduled hardening, not beta blockers.
