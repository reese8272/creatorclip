# CreatorClip — Production Assessment (ready-pass close-out)

**Date:** 2026-07-29  ·  **Commit:** `w3/ready-pass-closeout` @ post-merge of the two assess-fix batches (prod runs `a50b332` = Deploys 1+2; the 8 assess-fixes ship as Deploy 3)  ·  **Tests:** backend 2338 passed / 0 failed · frontend 285 / 0 · build + eval green  ·  **Method:** Layer 0 gates + 10-module delta sweep over everything the 2026-07-29 ready-pass touched (untouched modules carry their 2026-07-20 post-fix findings) + same-day fix wave for every new SEV1/SEV2.

## VERDICT: PRODUCTION-READY — **YES for the locked v1 scope (≤100-user private beta)**

**0 BLOCKER · 0 SEV1 (1 found → fixed same day) · 7 new SEV2s found → ALL fixed same day · remainder = carried/accepted residuals with owners.** The core creator loop is now complete end-to-end for the first time: upload → clips → review → apply AI titles → trim/clean re-render → schedule → private YouTube publish → outcomes → retrain, with honest error states (source-expired cards, structured 409 codes) at every dead-end the July incidents exposed. Beta blockers remaining are operator-only: **#26** (OAuth test users, ~5 min) → **#28** (friend smoke), plus **#282** (uptime monitor — the 31-hour silent outage found this morning is the closing argument).

---

## Layer 0 — deterministic gates (from `_machine.json`, this tree)

| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff / ruff-format | 0 | 0 | ✅ |
| mypy | 0 | 0 | ✅ |
| coverage | 83.51 % | 75.2 floor | ✅ (**+3.8** vs 2026-07-20; ~90 new tests across the pass) |
| module floors | crypto 100 / limiter 100 / auth 93.3 / preference 91.4 | **ratcheted 2026-07-29:** 99 / 99 / 91 / 0 | ✅ (clip_engine/preference floors parked on the Issue-368 measurement quirk) |
| bandit | high 0 / med 0 | 0 | ✅ |
| pip-audit | **0** | 0 | ✅ (**first fully-green local run** — the 20 findings were venv drift, fixed by venv upgrade) |
| visual regression | **GATING + green** | was `continue-on-error` | ✅ (Issue 272 closed; baselines from ubuntu-latest run 30482627526) |

## Layer 1 — register (ranked; cleanup-grade items live in the module files)

| Sev | Module | Location | Issue | Status / backed fix |
|---|---|---|---|---|
| ~~SEV1~~ | frontend | ClipPlayer.tsx:26 | Filmstrip mapped drags against `end_s - start_s` while the backend validates/cuts against the setup origin — every saved trim + trim-render on setup clips systematically offset | ✅ **FIXED** f9c2aff (+ display/filmstrip now share one `clipDur`; aria-valuemax test pins it) |
| ~~SEV2~~ | frontend | CleanedPreviewConfirm + useCleanedUriPoll | Stale clean-poll cache latch on repeat trims within the 30s staleTime — confirm could no-op against the previous artifact | ✅ FIXED 1fa6abb (removeQueries on confirm + staleTime 0) |
| ~~SEV2~~ | frontend | ClipPlayer/Editor `<video>` | Player kept playing the OLD media after a confirmed swap (key/src unchanged) | ✅ FIXED 3efff42 (key on `${id}:${render_uri}`) |
| ~~SEV2~~ | frontend | components/ui/modal.tsx | No WAI-ARIA dialog contract on the newly load-bearing publish dialog | ✅ FIXED c012b21 (role/aria-modal/labelledby, focus in/restore, Escape) |
| ~~SEV2~~ | routers | review.py save-trim bounds | Missing the one-frame right-edge tolerance its sibling /trim-render allows — drag-to-end rounding 422'd | ✅ FIXED ec2c47a (shared `_clip_duration_s` + MIN_KEEP_SEGMENT_S tolerance) |
| ~~SEV2~~ | chat | routers/creators.py identity_chat | Newly-billed intake route was the ONLY LLM route without `require_flag("llm_generation")` + `require_budget` — the kill switch couldn't stop intake spend | ✅ FIXED b4ea33d (gates stacked like siblings; 503/429 tests) |
| ~~SEV2~~ | chat | chat/intake.py:299 | Billed at hardcoded Sonnet rates while the model is configurable | ✅ FIXED 5e028f6 (shared `billing.ledger.model_rates`, never-under-bill Opus fallback; runner migrated too) |
| ~~SEV2~~ | knowledge→chat | chat/tools.py:500 | `_suggest_clip_titles` discarded producer usage — an unbilled nested Sonnet call (the intake defect class) | ✅ FIXED 0979005 (billed like the clips.py caller; both callers now pinned in the AST guard) |
| SEV2 | worker | tasks.py:511 | build_signals enqueues generate_clips unconditionally on task REDELIVERY (double-LLM-spend residual; the new guard covers broker failure only) | OPEN — carried; needs a redelivery dedup (e.g. existing-clips probe before enqueue); fold into the next worker pass |
| SEV2 | routers | clips.py (carried ×3) | In-request LLM on /clips/generate; create_summary budget-parity; per-feedback retrain enqueue | OPEN — carried from 2026-07-20 with designs in routers.md |
| SEV2 | clip_engine | reframe.py sendcmd | Unverified on a real ffmpeg build — but feature-gated OFF (`ACTIVE_SPEAKER_REFRAME_ENABLED=False`) | OPEN — gated; verify before any flag flip |
| SEV2 | _root_infra | slowapi sync-Redis | Accepted beta residual (~100ms worst case); upgrade trigger documented | ACCEPTED (DECISIONS) |

Module verdicts: routers, worker, billing, chat, knowledge, clip_engine, frontend, _root_infra, notify, deploy_ci — **all clean or clean-after-fixes**. Untouched modules (dna, preference, youtube, ingestion, upload_intel, improvement, analysis) carry their 2026-07-20 post-fix clean state.

Follow-up candidates filed this run: routers/clips.py title/caption/explain callers still pass hardcoded Sonnet rates (now trivially fixable via `model_rates` — fold into the next routers touch); the empty-dashboard visual-baseline misnomer (Issue-272 note / OFF_COURSE); the AST guard covers LLM call sites, not producer-callers (manual registration documented in-file).

## Layer 2 — scale checklist

Unchanged from 2026-07-20 (the pass added no scale-relevant topology): **beta axes all ✅** on the recorded Locust evidence (50 users: 4,327 req, 0 failures, p99 180 ms; 300 users: clean saturation, per-creator shedding). The 10k/GKE track stays descoped per DECISIONS 2026-06-26. Improvements since: the enqueue-DRY helper + session-split hardening improve pool hygiene (axis B); LLM cost control (axis on spend) now covers every call site, enforced by CI.

## What YES does and does not mean

**YES:** the codebase, pipeline, and single-VM topology are production-ready for the ≤100-user private beta, and — new since 2026-07-20 — the creator-facing loop is *feature-complete* through publish with honest failure states, billing covers every LLM call site, and every CI job including visual regression is gating.

**Not covered (tracked, not code gates):** #26 OAuth test users → #28 friend smoke (the Stage-A capstone), #282 uptime monitoring (beta-critical after the outage), #29 Google verification (public launch only), the carried SEV2s and cleanup tail above, 10k scale.

## Diff vs 2026-07-20-postfix-REPORT.md

**New & fixed this run:** 1 SEV1 + 7 SEV2s (table above) — all same-day.
**Closed since:** Issue 272 (visual gating); Issue 362 residual (SPA source-expired card); the cached-token billing class (3 unbilled sites + 1h multiplier, with a repo-wide CI guard); "Save trim broken for most clips" (latent since Issue 339); the cleaned-preview `s3://` player bug; self-hosted fonts (GDPR); advisory-lock leak; the last open-session LLM holds; test-suite event-loop noise (7 lines → 0, real poisoner found); ~7 OFF_COURSE flake/infra rows.
**New process evidence:** prod outage 2026-07-28→29 (~31 h, fully silent — no monitoring fired); recovery verified same day; #24/#25 flipped GREEN with live evidence; #282 elevated to beta-critical.
**Regressed:** nothing — every 2026-07-20 fix was re-verified in place by the delta sweep.

**Path to beta:** Deploy 3 (this branch) → #26 test users → #28 friend smoke + 48 h window → invite.

*History snapshot: `docs/assessment/history/2026-07-29-REPORT.md`.*
