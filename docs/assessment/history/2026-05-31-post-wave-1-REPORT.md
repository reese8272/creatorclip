# CreatorClip — Production Assessment

**Date:** 2026-05-31 (post Wave-1 hotfix batch — Hotfixes A/B + Issues 89/90/91/98)  ·  **Commit:** `74431e7` + uncommitted Wave-1 changes  ·  **Tests:** 523 passed, 1 skipped, 89 deselected (default lane); integration lane has 4 new arc tests

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

No open BLOCKER and no cross-tenant leak in any module. The Wave-1 hotfix batch closed BOTH SEV1s from the 2026-05-30 register (the `worker/progress.py` SSE cap TTL drift via EXPIRE-on-every-INCR, and the `billing/refund.py` app-role-under-RLS silent no-op via `AdminSessionLocal()`). One new SEV1 surfaced in this round's thorough billing walk — **`billing/stripe_client.py:65` calls the synchronous Stripe SDK inside an `async def` checkout route**, blocking the FastAPI event loop for every 300–800ms Stripe round-trip. What keeps the verdict CONDITIONAL rather than YES: that new SEV1 (mechanical 1-line fix), 31 SEV2s (down from 27 last cycle — the 4 net-new are the stripe module-level client landmine, the dna state-machine ownership split, the DATABASE_MIGRATION_URL `.env.example` gap, and a routers cleanup), and scale axes A/E/C still lack the Locust-behind-PgBouncer **load evidence** that only a real run can produce. Module verdicts: **dna ✅ clean · improvement ✅ clean · 9 NEEDS-WORK · 0 BLOCKER · 0 cross-tenant leak**.

---

## Layer 0 — deterministic gates (from `_machine.json`; Python 3.12 + Redis)
| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | 0 issues | 0 | ✅ |
| mypy | 0 errors | 0 | ✅ |
| coverage | not run locally this cycle (CI authoritative; last 70.59%) | 69.54% floor | ✅ no regression expected — Wave 1 added +10 tests / +0 production behavior unmocked |
| bandit (SAST) | not run locally (CI verified) | 0 / 0 | ✅ |
| pip-audit | not run locally (CI verified) | 0 | ✅ |
| freshness | both skills 2d | <90d | ✅ |

All five lanes on the latest pushed CI ran green; Wave-1 changes are uncommitted in the working tree and have NOT yet exercised CI. Full local pytest default lane: **523 passed / 1 skipped / 89 deselected** — net +14 tests vs the 2026-05-30 baseline of 509 (Wave-1 added 14 new across `tests/test_progress.py`, `tests/test_billing_refund.py`, `tests/test_billing.py`, `tests/test_videos_upload_streaming.py`, `tests/test_static.py`, `tests/test_dna.py`, plus 4 integration-lane tests).

Top untested load-bearing code: no new gap surfaced this cycle. Carry-forward target: the LightGBM round-trip for `preference/model.py:46`'s `_ALLOWED_CLASSES` — a sklearn/lightgbm/numpy module-path move would silently disable personalization via `load_latest` swallowing `UnpicklingError`. Test exists for the LogisticRegression branch only.

## Layer 1 — module register (ranked)

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| **SEV1** | billing | `billing/stripe_client.py:65` | `_STRIPE.checkout.sessions.create(params)` is the SYNCHRONOUS Stripe SDK (urllib3 under the hood) called directly inside an `async def` route (`routers/billing.py:84` `checkout`). Each checkout HTTP round-trip (300–800ms p95) blocks the FastAPI event loop, capping concurrent checkouts to one per worker process AND dragging unrelated request latency. NEW finding this cycle — thorough billing walk surfaced what prior passes missed. | Either (a) swap to `stripe.AsyncStripeClient` (single-line); or (b) wrap the call in `await asyncio.to_thread(_STRIPE.checkout.sessions.create, params)`. Add a regression test asserting the route returns under a synthetic 500ms Stripe latency without serializing concurrent requests. |
| SEV2 | billing | `billing/refund.py:57-71` | Refund idempotency is still a read-then-write on `pack_id` (`SELECT MinutePack.id WHERE pack_id = 'refund:<vid>'` → INSERT-if-None). `models.py:507` has NO UNIQUE constraint on `pack_id` (only `stripe_session_id` is unique). Two concurrent `on_failure` callbacks for the same `video_id` would both pass the SELECT and both INSERT a refund → double-credit. Hotfix B closed the RLS no-op SEV1 but NOT this race. Carry-forward. | Alembic migration: `CREATE UNIQUE INDEX minute_packs_refund_key ON minute_packs (pack_id) WHERE reason = 'refund'` (partial index — avoids colliding with non-refund packs like `"trial"`/`"starter"`). Drop the read-then-write guard, let `grant_minutes`'s existing `IntegrityError` catch path no-op the duplicate. Concurrent-refund integration test. |
| SEV2 | billing | `billing/stripe_client.py:20` | `_STRIPE = stripe.StripeClient(settings.STRIPE_SECRET_KEY)` is constructed at import time even in dev where the key defaults to `""`. `Settings._require_prod_secrets` only fails-fast when `ENV == "production"` — a misconfigured `ENV=staging` deploy with no key silently binds an empty client; failure surfaces only at first `/checkout`. NEW finding this cycle. | Lazy-init via `functools.lru_cache` returning the client only when `STRIPE_SECRET_KEY` is set; raise `RuntimeError("Stripe not configured")` from the accessor. OR extend `_require_prod_secrets` to fail-fast for `ENV == "staging"` too. |
| SEV2 | billing | `billing/refund.py` | Refund triggers purely on Celery `on_failure` invocation without re-checking the video's terminal state. If `on_failure` ever fires on a still-completable task (broker replay during chain reschedule), the `MinuteDeduction` row exists and the refund proceeds, double-crediting against an eventual successful run. (needs-runtime-confirmation that the chain truly cannot fire `on_failure` on a succeeded task.) | Gate refund on `video.render_status IN ('failed', 'errored')` (single extra SELECT inside the same session), OR document explicitly in DECISIONS and rely on the pack_id UNIQUE partial-index fix above. |
| SEV2 | worker | `worker/progress.py:134-146` | `_async_client()` rebuilds the singleton on loop-mismatch but **still does not close the old client first**. In pytest's per-test loops a fresh `aredis.Redis` is constructed every test, the previous one is abandoned with its pool half-bound to a dead loop → `Event loop is closed` GC noise. Socket leak (needs-runtime-confirmation via `lsof`). | In the rebuild branch, best-effort close the old client via `old_loop.call_soon_threadsafe(lambda: asyncio.ensure_future(old.aclose()))` OR expose an `await arebind()` helper for tests. |
| SEV2 | worker | `worker/progress.py:149-164` | `aemit` exception handler **still** nulls out `_AIO`/`_AIO_LOOP` globals on *any* exception, not just `ConnectionError`/`TimeoutError`. Under concurrent emits + a Redis brown-out, every concurrent failure rebuilds the pool, worsening the brown-out (pathological churn). Pool-rebuild abandons in-flight `aread_since` reads → visible SSE UX flicker. | `except (redis.exceptions.ConnectionError, redis.exceptions.TimeoutError):` resets globals; `except Exception:` leaves singleton intact for transient XADD failures. Pin with `asyncio.Lock()` if multi-loop concurrency ever lands. |
| SEV2 | worker | `worker/progress.py:190-208` | `aread_since` uses XREAD with `block=5000`; each blocked read holds **one Redis connection for the full block duration**. redis-py async pool default `max_connections=50`. ~100 concurrent SSE consumers (inside the stated 200-creator scale) exhaust the pool → emits/reads queue or fail. Progress stream becomes a hard cap on web concurrency. (needs-runtime-confirmation under load.) | Bound the async client's pool explicitly: `aredis.from_url(..., max_connections=settings.REDIS_MAX_CONNECTIONS)` with default 200. Separate redis clients for SSE reads vs writes so a wedged reader pool doesn't starve emits. Document the math in `docs/DEPLOYMENT.md`. |
| SEV2 | worker | `worker/tasks.py:415-496` | `_render_clip_async` idempotency guard protects sequential redelivery-after-success but **not concurrent**: with `acks_late` + `reject_on_worker_lost`, two workers can both read `pending`, both flip to `running`, both encode + upload to the same R2 key. Wasted double encode/upload (no corruption, identical bytes). Carry-forward. | `select(Clip).where(...).with_for_update()` on the Clip row in the opening session, re-check `render_status` under lock before flipping to `running`. |
| SEV2 | worker | `worker/tasks.py:300-339` | `_ingest_async` not a clean no-op on redelivery-after-success: re-`probe_duration_s` + `extract_audio_wav` over already-derived WAV. No corruption (billing UNIQUE(video_id) idempotent), but wastes ffmpeg + R2 round-trips. Carry-forward. | Short-circuit when `source_uri` already points at the derived audio key OR gate on `ingest_status == done`. |
| SEV2 | worker | `worker/tasks.py:737-769` | `_poll_clip_outcomes_async` does NOT break on YouTube quota exhaustion; per-outcome failures swallowed by `except Exception: continue`. Quota-out creator walks whole candidate set firing doomed calls. Bounded by 10-day `cutoff_created` but wasteful. Carry-forward. | Catch `QuotaExhaustedError` explicitly inside the inner loop and `break` (mirror analytics-refresh at `tasks.py:1019-1025`); commit partial progress first. |
| SEV2 | worker | `worker/anthropic_stream.py:57-75` | If network drops mid-stream, the Anthropic SDK raises inside `for event in stream:` and propagates up to `_build_dna_async`'s outer try/except — but mid-stream tokens already delivered to UI are followed by an undefined gap before the terminal `error` emit fires (must unwind `to_thread` → outer try/except → `aemit`). | Wrap streaming call inside `stream_and_emit` with `try/except: sync_emit(task_id, "error", message="stream interrupted"); raise` so the terminal emit lands on the SAME Redis call path as the deltas. Caller's emit still fires as backup. |
| SEV2 | worker | `worker/anthropic_stream.py:72-84` | `usage_dict` casts every field via `getattr(usage, ..., 0)` — `0` as default silently hides a schema change in a future SDK bump (Issue 84) where a field is renamed. `cache_read=0` would under-report savings without raising. | Distinguish absent from zero — return `None` when attribute missing; log a one-time warning the first time a previously-present field returns None so SDK-bump regression is loud. |
| SEV2 | worker | `worker/progress.py:74-85` | `_serialize` does `json.dumps(fields, default=str)` with no schema/allowlist. Today's call sites are safe (counts, version ints, data-gate messages); structural unverified trust boundary for the next contributor. | Per-event-type `EventPayload(BaseModel)` at the emit boundary. Cheapest interim: emit-level guard that logs+drops any field whose stringified length exceeds N or matches a token-shape regex (`^sk-ant-`, `^ya29\.`, base64-long-string). |
| SEV2 | worker | `worker/tasks.py:661-666` | `_emit("error", message=str(exc))` for `ValueError` raw-passes the exception arg into the SSE stream. Today's raisers are safe data-gate messages; structural trust boundary unverified for the next library throwing `ValueError`. | Allowlist of known-safe shapes (`"… not found"`, `"… is empty"`) and fall back to a generic `"validation failed"` for anything else. |
| SEV2 | routers | `routers/tasks.py:131-138` | Ownership check is an enumeration oracle: 404 (`owner is None`) vs 403 (`owner != creator.id`) lets an authed client distinguish "task never existed" from "exists but belongs to another creator". In-code comment at :133-135 says "don't leak whether the task ever existed" — the implementation contradicts the comment. Prompt-confirmed: Wave 1 did NOT touch this. | Return `404 "Unknown task"` for BOTH branches; log the distinction server-side if needed. Regression test asserting creator B sees 404 (not 403) on creator A's task_id. |
| SEV2 | routers | `routers/tasks.py:140` + `worker/progress.py:204` | Client-supplied `Last-Event-ID` header is forwarded raw into XREAD with no validation; any non-`<ms>-<seq>` value triggers `redis.exceptions.ResponseError`, which `aread_since` doesn't catch → mid-stream 500 + noisy log on every malformed reconnect. The `finally:` does release the slot (verified). | At the top of `_event_stream` after acquiring the slot, validate `last_event_id` against `^\d+-\d+$` (or empty); on mismatch reset `cursor = "0-0"`. Cap header length at ≤64 chars before parsing. |
| SEV2 | routers | `videos.py:77-82`, `clips.py:118-123`, `upload_intel.py:37-40` | Unbounded `list(result.scalars())` with no LIMIT/pagination on `GET /videos`, `GET /videos/{video_id}/clips`, `GET /me/upload-intel`. With Issue 87's catalog sync bulk-loading hundreds–thousands of rows per creator, the videos endpoint is most acute — even after Wave 1's `source_uri IS NOT NULL` filter, an established creator still serializes the entire list per dashboard hit. | Keyset pagination `?limit=&before=` with hard cap 100 on `list_videos` and `list_clips`; bound `AudienceActivity` read by the documented 168-row max (7 days × 24 hours). |
| SEV2 | _root_infra | `observability.py:224-241` | Celery `_bind_request_id` / `_record_task_and_clear` use single module-level ContextVar — safe only under prefork. A future `--pool=gevent/threads` migration silently breaks log correlation. Carry-forward. | Assert `app.conf.worker_pool == "prefork"` at worker startup; OR key task start off `task.request.id` via per-task dict guarded by `task_postrun` removal. |
| SEV2 | _root_infra | `Dockerfile:1-34` | Image still runs as root — every process (uvicorn, celery, ffmpeg subprocesses) has UID 0 + write access to whole app tree. An RCE via a media-processing dep would have full container privilege. Defense-in-depth gap. Carry-forward. | `RUN useradd --create-home --uid 1000 app && chown -R app:app /app /root/.local` then `USER app` after `COPY . .`. Verify Celery + ffmpeg still write their state/temp dirs as `app`. |
| SEV2 | _root_infra | `Dockerfile:34` | Default CMD still ships `uvicorn … --reload`. Forgotten prod override starts uvicorn in dev mode (filesystem watcher, single worker, no graceful shutdown). Carry-forward. | Default to `gunicorn -k uvicorn.workers.UvicornWorker -w 4 main:app --bind 0.0.0.0:8000`; move `--reload` to `docker-compose.yml` where dev belongs. |
| SEV2 | youtube | `youtube/oauth.py:290` | `get_valid_access_token` still has no Redis-down degradation. `redis_client.set(lock_key, ..., nx=True)` raises `redis.RedisError` → unhandled 500 on every analytics fetch and every Celery task. Graceful-degrade posture defeated by a single Redis blip. Carry-forward. (needs-runtime-confirmation under real Redis failover.) | Wrap `set()` in `try/except redis.RedisError`; fall back to lockless refresh (Google tolerates rare double-refresh; DB write is idempotent on `creator_id`) with a warning log, OR raise `HTTPException(503, ...)`. Regression test mocking `redis_client.set` → `ConnectionError`. |
| SEV2 | youtube | `docs/COMPLIANCE.md:21,47-50,111` | Analytics retention/refresh cadence + max-staleness purge **still TBD** (Issue 75b still open). Daily Beat refresh overwrites `fetched_at` but no deletion sweep — per-creator `VideoMetrics`/`RetentionCurve`/`AudienceActivity`/`Demographics` rows persist indefinitely. **Largest remaining compliance gap; most likely cause of an audit finding during OAuth app verification.** Carry-forward. | Confirm Google's required staleness window, record in COMPLIANCE.md §2, add `YOUTUBE_ANALYTICS_MAX_STALENESS_DAYS` setting (conservative default 30d), add daily Beat sweep `DELETE`ing rows whose `fetched_at < now() - max_staleness`. |
| SEV2 | ingestion | `ingestion/transcribe.py:116-123,137-138` | Deepgram normalizer still uses hard-key indexing (`u["start"]`, `u["end"]`, `u["transcript"]`); a missing timestamp raises `KeyError` and burns a Celery retry instead of skipping the partial item. WhisperX + AssemblyAI normalizers already safe. Carry-forward — Wave 1 did NOT address. | Switch list comprehensions in `_normalize_deepgram` to `.get("start")`/`.get("end")` and skip any utterance/word where either is `None`. Default text via `u.get("transcript", "")`. |
| SEV2 | ingestion | `ingestion/transcribe.py:43-60` | `_guard_audio_size` swallows `OSError` and silently returns when file is missing/unreadable; downstream SDKs error cryptically (whisperx) or upload 0 bytes and succeed with empty transcript (AssemblyAI) — both burn per-job budget on guaranteed-empty pipeline run AND (under Issue 57) trigger automatic refund for a cause we could have detected upfront. Carry-forward. | In `except OSError`, raise `FileNotFoundError(f"audio not found: {audio_path}")` so caller's retry/refund pathway sees a clear terminal error. |
| SEV2 | clip_engine | `clip_engine/ranking.py:139` | `dna_match=c.get("score")` seeded to composite score and never refined; preference feature vector receives a duplicate of its own label-generating signal as an "independent" DNA-fit feature (collinear). Cuts against rubric 4 ("ranking is against THIS creator's DNA, not a generic score"). Carry-forward. | Have `score_candidates` return BOTH the DNA-only fit (Claude `score` before signal blend) and the composite. Persist DNA-only fit to `dna_match`. Unit test asserting `dna_match ≠ composite` on the DNA path, `dna_match is None` on cold-start. |
| SEV2 | clip_engine | `clip_engine/candidates.py:113` | Candidate windows never deduped/merged for overlap. `find_peaks` only enforces spacing between *peaks*; the backward setup scan can pull two adjacent peaks 39s apart to the SAME silence-end boundary inside the WINDOW_S=75s lookback, producing two near-identical clips. Cuts against principle #9 "One idea per Short". Carry-forward. | After chronological sort, IoU-merge pass: for each candidate compute IoU against the previous kept; if IoU > 0.5, drop the lower-prominence one. Eval scenario with two peaks 35s apart sharing a silence boundary. |
| SEV2 | dna | `dna/brief.py:130-151` | Streaming brief path does ~2k synchronous Redis XADDs per build on a worker threadpool slot. Bounded latency but consumes one threadpool slot per active streaming build. At `worker_concurrency=N`, only N concurrent DNA builds can stream. (needs-runtime-confirmation under target concurrency.) | Acceptable for v1 (DNA builds are low-frequency). If queue depth grows, batch deltas (every K tokens or K ms) to cut Redis round-trips ~10×. |
| SEV2 | dna | `dna/brief.py:153-157` | Asymmetric `# type: ignore[arg-type]` on `.create()` call but NOT on `stream_and_emit` call. SDK signatures differ in all versions; the asymmetry will fail mypy the day the SDK bump (Issue 84) tightens the streaming stub. Carry-forward. | Mirror the ignore onto the streaming call, OR narrow `_build_request`'s return type to SDK TypedDicts (`list[TextBlockParam]`, `list[MessageParam]`) once Issue 84 lands. |
| SEV2 | dna | `dna/brief.py:134` | Function-local `from worker.anthropic_stream import stream_and_emit` makes `worker.*` a runtime dep of `dna.brief` (layering smell — `dna/` is otherwise free of `worker/` imports). Defends against import cycle. Carry-forward. | Accept as lesser evil for v1; add one-line comment explaining WHY it's function-local so a future cleanup doesn't "fix" it back to a top-level import that breaks worker startup. |
| SEV2 | preference | `preference/model.py:46` | `_ALLOWED_CLASSES` LightGBM branch hand-derived, coupled to internal module paths (`lightgbm.sklearn`, `lightgbm.basic`, `numpy._core.multiarray.scalar`). Any library upgrade that relocates one of these silently makes every legitimate `from_bytes` raise `UnpicklingError` → `load_latest` swallows into silent DNA fallback — personalization disappears with no alert. Existing round-trip tests only exercise LogisticRegression. Carry-forward. | `tests/test_preference.py::test_scorer_round_trips_lightgbm` that forces the LGBM branch (call `fit` with `threshold=1`) and asserts `from_bytes(scorer.to_bytes()).predict_score` matches the original. Catches a future module-path move at build time, not in production. Optional: emit a "preference_deserialize_failed" metric for the silent-degrade observability. |
| SEV2 | preference | `preference/_scorer_cache.py:23` | In-process LRU bound is entry-count (default `PREFERENCE_SCORER_CACHE_SIZE=128`), not bytes; a LightGBM booster ~few MB × 128 entries × workers × replicas = hundreds of MB resident per pod. Bounded today by `prefetch_multiplier=1`. Carry-forward. | Lower default to ~32, OR gate cap on bytes via `sys.getsizeof` estimate at `put` time, OR document the per-entry × concurrency × replicas math in the docstring. |
| SEV2 | upload_intel | `upload_intel/timing.py:54-55` | `optimal_gap_hours` still does NOT have the bounds/coercion guard `best_upload_windows` got in Issue 75d — sorts on raw `r.activity_index`, computes `r.day_of_week * 24 + r.hour` on unvalidated attrs. A malformed row that `best_upload_windows` drops is silently consumed here, corrupting the response. Carry-forward. | Filter+coerce rows first (mirror the windows guard); return `None` if <2 valid rows. Regression test feeding one bad row and asserting gap equals gap over valid rows alone. |

Plus **34 cleanup** items spread across all 11 modules — typing gaps (`dict[str, object]` annotations, untyped `_creator_key(request)` and `decode_session_token` returns), DRY extractions (3 duplicated 402 raises in `billing/ledger.py`; `_clip_response` hand-mapped dict that duplicates `ClipOut`; `signal_array` rebuilt once per candidate in `scoring.py`), docstring contracts on the new emit wire-shape, render keyframe timeout scoped tighter than the full encode budget, lifespan-coupling registry, `DATABASE_MIGRATION_URL` missing from `.env.example` (NEW this cycle — operator silently single-roles through `DATABASE_URL`), magic constant `12` for missing hour-level analytics. Per-finding detail with `file:line` in `docs/assessment/modules/*.md`.

Module verdicts: **dna ✅ clean · improvement ✅ clean · 9 NEEDS-WORK · 0 BLOCKER · 0 cross-tenant leak**.

## Layer 2 — scale checklist (`scale-checklist.md`)
| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | psycopg3 `prepare_threshold=None` + pool 15+5 + admin pool 5+10 + `pool_pre_ping` + `pool_recycle=1800` verified in `db.py:33,47-50,58-62`; per-pod ceiling 35 vs documented 25-conn PgBouncer sidecar — verify sidecar budget covers BOTH engines. `worker/progress.py:188-208` async Redis pool default 50 is in request-concurrency critical path (SEV2). **Locust-behind-PgBouncer evidence still pending** — single most-load-bearing CONDITIONAL→YES action. *(unchanged)* |
| B Async loop hygiene | ⚠️ | **REGRESSION**: NEW SEV1 surfaced — `billing/stripe_client.py:65` calls sync Stripe SDK inside async checkout route. Issue 78d/78a closed every prior async-loop offender (clip_engine candidates+features, all sync LLM/upload/transcription/Voyage off the loop) — the stripe path was overlooked. *(was ✅ — now ⚠️ until 1-line `asyncio.to_thread`/AsyncStripeClient swap)* |
| C Celery idempotency | ⚠️ | Issue 76 advisory lock + partial UNIQUE on `creator_dna.build_job_id` closed the prior `build_dna` concurrent double-pay (SEV1 → resolved). Residual: render double-encode (SEV2), ingest re-do (SEV2), `poll_clip_outcomes` no quota break (SEV2), refund pack_id race (SEV2 — Hotfix B closed only the RLS gap, not the read-then-write race). *(unchanged ⚠️)* |
| D Tenant isolation | ✅ | RLS migration `0010_rls_policies` deployed; `db.py:119-148` listener emits `set_config('app.creator_id', :cid, true)` on every transaction whose session has `creator_id` set. **Wave-1 Hotfix B** closed the `billing/refund.py` carve-out — refund now opens `AdminSessionLocal()` (BYPASSRLS) like the rest of the worker surface. Structural per-creator isolation now invariant at DB layer with NO known carve-outs. *(unchanged ✅; subscribed leak surface fully closed)* |
| E Backpressure | ⚠️ | Issue 76 closed the four prior E SEV2s (Retry-After on 429, ffmpeg timeout, SDK transcription timeout, streaming upload). Residual: `oauth.py:290` Redis-down → unhandled 500 (SEV2), Anthropic mid-stream interrupt has no first-class terminal emit (SEV2), analytics retention purge still open (SEV2 — largest compliance gap). *(unchanged)* |
| F Rate limit / quota | ✅ | **PROMOTED from ⚠️**: **Wave-1 Hotfix A** closed the SSE cap TTL drift SEV1. EXPIRE-on-every-INCR now refreshes the per-creator concurrent-cap key on every acquire; cap silently-bypassed scenario eliminated. Quota TZ FIXED (`youtube/quota.py:56-57` `ZoneInfo("America/Los_Angeles")`). slowapi per-creator + spend gate verified. Not load-tested. *(was ⚠️ — now ✅)* |
| G Observability | ✅ | request-id ContextVar + ASGI middleware + JSON logs (no PII/token in any new emit), `/metrics` bearer-token + prod fail-safe. API→Celery correlation via signals (`weak=False`). Residual SEV2: ContextVar correlation safe only under prefork (carry-forward). *(unchanged)* |
| H Migration / pgvector | ✅ | HNSW index (0006) + FK indexes + partial UNIQUE on `confirmed` (0005) + `final` marker (0007) + RLS (0010) + pack_id width (0011) + creator_identity (0012) all verified at HEAD. All migrations use `CREATE INDEX CONCURRENTLY` in autocommit. **Backups + PITR restore-test still unverified.** *(unchanged)* |
| I Secrets / deletion | ✅ | MultiFernet rotation + decrypt + typed `TokenDecryptError` (`crypto.py:13-43`); JWT identity (`auth.py:31-55`); account-deletion (Issue 19/36); Stripe prod fail-fast + `/metrics` prod fail-safe; `/docs` off in prod; ALLOWED_ORIGINS locked. *(unchanged)* |

## Diff vs previous report (2026-05-30 post Issue 86, commit `bbfa3c8`)

**Fixed & verified this cycle (BOTH prior SEV1s + 0 SEV2s closed by Wave 1; SEV1 4→2→1 trajectory continues downward):**
- ✅ SEV1 **`worker/progress.py:214-232` SSE concurrent-cap TTL drift** (Hotfix A): `client.expire(key, _STREAM_TTL_SECONDS)` now fires on every INCR (verified at `worker/progress.py:232`). The previously-flagged steady-state cap bypass (creator holds ≥1 streams past 3600s → counter TTL elapses → next INCR resets to 1 → cap silently bypassed) can no longer occur. Pinned by 2 new regression tests in `tests/test_progress.py`.
- ✅ SEV1 **`billing/refund.py:41` app-role session under prod RLS** (Hotfix B): now `AdminSessionLocal()` (BYPASSRLS) — matches the rest of the worker surface. Refund credits will actually land once the prod RLS role split flips. Source-inspect invariant + runtime-factory-spy test pin the choice in `tests/test_billing_refund.py`.
- ✅ SEV2 (functional bug, not in prior register) **silent failed uploads on low balance** (Issue 89): new `check_balance_for_minutes` mirrors `deduct_for_video`'s predicate; wired into `/videos/upload` AFTER `probe_duration_s` with concrete "needs N you have M" copy. **Deviation from AC**: NOT wired into `/clips/render` because render doesn't deduct — deviation captured in `docs/DECISIONS.md`. 4 unit tests + 1 router-level integration test.
- ✅ SEV2 (UX bug, not in prior register) **catalog rows polluting `/videos` list** (Issue 90): `Video.source_uri.isnot(None)` filter. `source_uri IS NULL` documented in `docs/SOT.md` as the canonical catalog-only marker.
- ✅ SEV2 (UX bug, not in prior register) **"Clips ready" miscounts unrendered clips** (Issue 91): frontend filter `c.render_status === 'done'`; card relabeled "Clips rendered"; also fixed an unrelated unwrapping bug (`.length` on `{clips: [...]}` wrapper).
- ✅ SEV2 (state-machine bug, not in prior register) **DNA dashboard banner sticky after confirm** (Issue 98): root cause was `dna/profile.py::create_draft` never advancing `onboarding_state`. Fix: bump `connected → dna_pending` so `confirm_draft`'s existing `dna_pending → active` branch reaches `active`. 3 unit tests + 4 integration tests pin the full arc and idempotency/non-regression.

**New this run (Issue 89-channel + previously unflagged):**
- 🆕 SEV1 `billing/stripe_client.py:65` — sync Stripe SDK in async `/checkout` route (thorough billing walk surfaced what prior passes missed).
- 🆕 SEV2 `billing/stripe_client.py:20` — module-level Stripe client construction with empty key in dev/staging silently binds a landmine.
- 🆕 SEV2 `billing/refund.py` — refund triggers on `on_failure` without re-checking terminal video state (needs-runtime-confirmation).
- 🆕 cleanup `_root_infra` — `DATABASE_MIGRATION_URL` declared in `Settings` but missing from `.env.example` — operator silently single-roles through `DATABASE_URL`.
- 🆕 cleanup `dna/profile.py` — state-machine ownership split between `create_draft` (`connected → dna_pending`) and `worker/tasks.py:629` (`awaiting_data → dna_pending`). Correct under identity-map sharing but non-obvious; easy to break in a refactor.

**Carry-forward (unchanged this cycle — fix opportunities still open):**
- SEV2 `routers/tasks.py:131-138` 404/403 enumeration oracle.
- SEV2 `routers/tasks.py:140` unvalidated `Last-Event-ID` → 500 on malformed reconnect.
- SEV2 `worker/progress.py:134-146` rebuild-without-aclose on loop mismatch (test-context pool leak).
- SEV2 `worker/progress.py:149-164` aemit churn on any exception (not just connection errors).
- SEV2 `worker/progress.py:188-208` blocking XREAD with default redis-py pool of 50.
- SEV2 `worker/anthropic_stream.py:57-75` no terminal emit on mid-stream interrupt.
- SEV2 `worker/anthropic_stream.py:72-84` silent-zero usage defaults hide SDK schema drift.
- SEV2 `worker/progress.py:74-85` + `worker/tasks.py:661-666` unschema'd emit payloads + raw ValueError leak path.
- SEV2 `worker/tasks.py` carry-forwards (render double-encode, ingest re-do, poll quota break, refund double-credit race).
- SEV2 routers unbounded list endpoints on `/videos`, `/clips`, `/upload-intel`.
- SEV2 clip_engine `dna_match` collinear seed; candidate overlap dedup missing.
- SEV2 dna brief streaming sync XADD volume + asymmetric type-ignore + function-local import.
- SEV2 preference LightGBM round-trip not in CI; scorer cache entry-count not byte-bounded.
- SEV2 youtube oauth Redis-down → 500; analytics retention purge still open (largest compliance gap).
- SEV2 ingestion Deepgram hard-key indexing; `_guard_audio_size` silent OSError swallow.
- SEV2 upload_intel `optimal_gap_hours` missing Issue-75d guard.
- SEV2 _root_infra Celery ContextVar prefork-only safety; Dockerfile root user + `--reload` default.

**Counts (Layer 1, all modules):**

| Severity | This run (2026-05-31) | Prior (2026-05-30) |
|---|---|---|
| BLOCKER | 0 | 0 |
| SEV1 | 1 | 2 |
| SEV2 | 31 | 27 |
| cleanup | 34 | 28 |

SEV1 trajectory: 4 → 2 → 1 across the last three cycles (Issue 76 closed 4 prior; Issue 86 surfaced 2; Wave 1 closed both, billing-stripe walk surfaced 1). The 4 net-new SEV2s are: stripe module-level client landmine, refund-without-terminal-state-check, state-machine ownership split (dna), and the operational `.env.example` gap. All Wave-1 changes are mechanical and bounded.

## Top 5 actions, in order

1. **`billing/stripe_client.py:65` — fix the sync-Stripe-in-async SEV1** (1-line `asyncio.to_thread` wrap or single-line swap to `stripe.AsyncStripeClient`). Add a regression test asserting `/checkout` returns under a synthetic 500ms Stripe latency without serializing concurrent requests. **Same shape as Issue 78d — same fix recipe.**

2. **Activate RLS on prod (now structurally unblocked by Wave-1 Hotfix B)** — the manual workflow at `.github/workflows/activate-rls.yml`. (a) Generate two passwords with `openssl rand -hex 24`; (b) add `POSTGRES_APP_PASSWORD` and `POSTGRES_MIGRATE_PASSWORD` to repo Secrets; (c) Trigger `Activate RLS (Issue 79)` workflow with `dry_run=true`, verify printed SQL + .env plan; (d) re-run with `dry_run=false`. Workflow rolls back automatically on failed verification. Adds **`DATABASE_MIGRATION_URL` to `.env.example` first** so the runbook reader has a complete checklist (NEW cleanup this cycle).

3. **Add `CREATE UNIQUE INDEX … WHERE reason = 'refund'`** to `minute_packs` (Alembic 0013), drop the read-then-write refund guard, let `grant_minutes`'s existing `IntegrityError` path no-op duplicates. Closes the only billing SEV2 a misbehaving concurrency can exploit AND tightens the refund flow's narrative.

4. **Locust-behind-PgBouncer load test (`tests/perf/`)** on a real staging cluster — converts scale axes A/C/E from ⚠️ to ✅ and is the **single gating action** to move the overall verdict from CONDITIONAL to YES. Pool math (axis A), Celery idempotency under at-least-once delivery (axis C), and backpressure under quota exhaustion (axis E) all need actual concurrency evidence that reading cannot produce.

5. **Compliance — analytics retention purge** (still-open Issue 75b): confirm Google's required staleness window, record in `docs/COMPLIANCE.md` §2, add `YOUTUBE_ANALYTICS_MAX_STALENESS_DAYS=30` setting + daily Beat sweep deleting stale `VideoMetrics`/`RetentionCurve`/`AudienceActivity`/`Demographics` rows. **Largest remaining compliance gap and the most likely cause of an audit finding during OAuth app verification.**
