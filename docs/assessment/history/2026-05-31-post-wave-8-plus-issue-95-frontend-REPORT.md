# CreatorClip — Production Assessment

**Date:** 2026-05-31 (post Wave-8 + Issue 95 frontend close) · **Commit:** `78630c6` (+ uncommitted Issue 95 frontend work) · **Tests:** 583 passed / 1 skipped / 122 deselected (default lane)

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

**No open BLOCKER and no cross-tenant leak in any module.** Wave 5's Fix 1 cleanly closed the 3 routers `aset_owner` SEV1s flagged at the post-Wave-4 verdict (re-verified at `routers/clips.py`, `creators.py::sync_catalog`, `creators.py::build_dna` — all now wrapped in `try/except redis.RedisError → log + stream_url=None`). Waves 6–8 then shipped 5 issues (Wave-6 audit fixes, Issue 101 self-hosted deploys, Wave-7 pricing hotfix, Wave-8 four-issue batch including Issue 95 backend, plus this session's Issue 95 frontend close) on a new design system without introducing a cross-tenant leak. **But the SEV1 axis regresses 0 → 2:** the preference module walk surfaced two real event-loop-blocking calls (joblib.load + LightGBM fit on async path) that were graded SEV2 in prior cycles. Also new: pip-audit ran locally for the first time in five waves and surfaced **16 dependency vulnerabilities** that prior runs marked "not run locally; CI verified." Wave-8's three new endpoint surfaces (`/creators/me/api-keys`, `/clips/ingest`, `/creators/me/insights`) all enforce per-creator isolation correctly, but added 3 new SEV2s (likely-broken `nullif` aggregate on insights, temp-file leak on `ingest_clip` non-HTTPException path, `/clips/ingest` rate-limiter likely keyed on IP not creator).

SEV1 trajectory across seven cycles: **4 → 2 → 1 → 3 → 0 → 1 → 2**. Module verdicts: **0 clean · 11 NEEDS-WORK · 0 BLOCKER · 0 cross-tenant leak**. The two preference SEV1s have a ~25-LOC fix each; the rest of the upgrade path to YES is Locust evidence on A/E, the pip-audit triage, and a per-creator rate-limit-key sweep across the authenticated routes.

---

## Layer 0 — deterministic gates (from `_machine.json`)
| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | 0 issues | 0 | ✅ |
| mypy | 0 errors | 0 | ✅ |
| coverage | **75.19%** | 69.54% | ✅ IMPROVED +5.65pp |
| bandit | 0 high / 0 medium | 0 / 0 | ✅ |
| pip-audit | **16 vulns** | 0 | ❌ REGRESSION (first local run since installs) |
| freshness | best-practices 2d / production-assessment 2d | <90d | ✅ |

**Notes on Layer 0 deltas:**
- Coverage improved 5.65pp since Wave-4 baseline — driven by Wave 5–8 test additions (+8 default, +22 integration over Waves 5–8 plus +1 default this session for Issue 95 frontend). Re-baselining the floor to ~75% would lock in the gain — recommended after the pip-audit triage.
- pip-audit shows 16 vulns. Prior reports' Layer-0 tables said "pip-audit not run locally; CI verified" — so this is the first time the local audit produced a number against the baseline of 0. Triage required: confirm against the CI pip-audit job and either fix the dependencies or update the baseline if the CI job has been ratcheting it without the local config seeing the floor. This is the only failed gate; nothing else has regressed.

Top untested load-bearing code: not surfaced in this run (coverage gate passed; per-file ranking not in `_machine.json` output). Re-run with `--require-coverage` to expose the ranked untested list.

## Layer 1 — module register (ranked)

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| **SEV1** | preference | `preference/model.py:126-132` (+ `train.py:160` caller) | `from_bytes` mutates process-global `joblib.numpy_pickle.NumpyUnpickler` under `_UNPICKLER_LOCK`, then `joblib.load` blocks the loop. Two creators hitting rerank cold-cache serialize behind a process-wide lock inside an async coroutine. | (a) `await asyncio.to_thread(PreferenceScorer.from_bytes, blob)`; (b) replace the global-class-swap with a per-load `joblib.numpy_pickle.NumpyUnpickler` subclass operating on `io.BytesIO(data)` — removes the process-wide lock. |
| **SEV1** | preference | `preference/train.py:83` | LightGBM `fit(X, y, w)` runs synchronously inside `async def build_and_save`. On a power creator with thousands of labels this blocks the event loop for seconds, stalling every other request on the same worker. | `await asyncio.to_thread(fit, X, y, w)` — or better, move `build_and_save` into a Celery task (it already commits to DB so it's naturally a background job). The `await session.commit()` afterward is fine; only the CPU-bound `fit` needs to leave the loop. |
| SEV2 | routers (**NEW Wave-8 surface**) | `routers/insights.py:147-149` | `func.nullif(Video.kind != short, True)` aggregate idiom likely returns 0 for shorts/longs/ingested_done counts — `nullif(predicate, True)` returns NULL on every row, so `count(NULL)=0`. UX defect, not a leak; the insights page totals would be zeros. (needs-runtime-confirmation) | Rewrite as standard idiom `func.count(case((Video.kind == VideoKind.short, 1), else_=None))`. Add seeded test asserting 3 shorts + 2 longs returns 3/2/4/1. |
| SEV2 | routers (**NEW Wave-8 surface**) | `routers/clips.py:213-329` (`ingest_clip`) | `tempfile.NamedTemporaryFile(... delete=False)` + manual `unlink` only in the `except HTTPException` arm. A non-HTTP exception (OSError on disk full, CancelledError on client disconnect mid-upload) leaks the temp file. Unbounded disk consumption on the API replica at scale. Same shape as carry-forward gap in `videos.py:184-204`. | Wrap the entire post-NamedTemporaryFile block in `try/finally: tmp_path.unlink(missing_ok=True)` — mirror the R2 PUT block at line 274-278. Fix both `ingest_clip` and `upload_video` in one sweep. |
| SEV2 | routers (**NEW Wave-8 surface**) | `routers/clips.py::ingest_clip` (decorator) | `@limiter.limit("20/hour")` with no explicit `key_func` — slowapi defaults to remote-address keying. For OBS-style ingest a creator behind CGNAT shares budget with unrelated creators; one creator with many keys can each consume the full 20/h. Violates scale-checklist F. | Hoist per-creator key into a shared helper that resolves authenticated creator id from either session cookie OR bearer-auth dependency; apply uniformly across `ingest_clip`, `render_clip`, `upload_video`, `build_dna`. `get_current_creator_via_api_key` already sets `session.info["creator_id"]`; surface it on `request.state` for the limiter. |
| SEV2 | routers (**NEW Wave-8 surface**) | `routers/api_keys.py:131-138, 163-177` | Key create / revoke writes `observability.log_event` only — no durable `append_audit` row, even though `delete_account` (auth.py:245) uses the facility. Security-event traceability gap on a privileged surface. | Add `append_audit(session, action="api_key.created"/"api_key.revoked", actor=str(creator.id), entity_type="api_key", entity_id=row.id, before=...)` before each commit. |
| SEV2 | routers | `routers/tasks.py:131-138` | 404/403 ownership-check enumeration oracle. Carry-forward. | Collapse to single 404 — `if owner is None or owner != str(creator.id): raise 404`. |
| SEV2 | routers | `routers/tasks.py:140` (Last-Event-ID forward to XREAD) | Unvalidated `Last-Event-ID` → `redis.ResponseError` → 500 on every malformed reconnect. Cheap availability vector (eats per-creator SSE slot). Carry-forward. | Validate `^\d+-\d+$` before XREAD; on miss coerce to `"0-0"` and log a warning. |
| SEV2 | routers | `routers/videos.py:63-97` + `routers/clips.py:115-134` | Unbounded `select(...).order_by(...)` — full per-creator catalog in one hop. PRD 10k-creator target streams the whole catalog through cursor → JSON → heap. Carry-forward. | Add `limit` (default 50, max 200) + cursor on `(created_at DESC, id DESC)`. Verify index on `(creator_id, source_uri, created_at DESC, id DESC)`. |
| SEV2 | routers | `routers/insights.py:142-153` (totals query) | Five aggregates in one SELECT scan ~10k rows for a power creator on every insights pageview. Carry-pair with the `nullif` SEV2 above. | Cache 60s in Redis keyed by `(creator_id, "insights")` OR memoize totals on Creator row via the catalog/sync task. |
| SEV2 | routers | `routers/auth.py:229, 241`; `videos.py:131`; `billing.py:109, 128` | Over-broad `except Exception` swallows `CancelledError` + programming errors (StripeAuthenticationError, missing decrypt key). Carry-forward. | Narrow to `(httpx.HTTPError, ValueError, sqlalchemy.exc.SQLAlchemyError)` per call site; let `CancelledError` propagate. |
| SEV2 | worker | `worker/tasks.py:318-391` (`_ingest_async`) | On retry after a successful commit of `source_uri=audio_uri`, the original `source/{creator}/{vid}.mp4` is unreferenced and `_purge_stale_source_media_async` only purges current `Video.source_uri` — **source mp4 permanently orphaned in R2**, breaking SOURCE_MEDIA_RETENTION_HOURS ToS promise + unbounded storage cost. Carry-forward, re-graded. | Before overwriting, capture `prior_source_uri`; after commit `adelete_file(prior_source_uri)` if it's an mp4. Or: short-circuit when `source_uri` already endswith `.wav`. |
| SEV2 | worker | `worker/tasks.py:393-456` (`_transcribe_async`) + `:459-519` (`_signals_async`) | No idempotency guard — `acks_late + reject_on_worker_lost` redelivery re-calls Deepgram/AssemblyAI (per-minute billing) and overwrites `Transcript`. The render task already has this guard (`:562-570`). | Add "already done? return" probe at top of each (`Transcript` row exists + `ingest_status` past `transcribing` → no-op). Mirror render's pattern. |
| SEV2 | worker | `worker/tasks.py:140-147` (`generate_clips`) | Uses default `Task`, not `RefundOnFailureTask` — terminal failure leaves minutes deducted with no refund. The one billable-pipeline task missing the base class. | `base=RefundOnFailureTask`. `refund_for_video` is already keyed on `pack_id=refund:<video_id>` so double-fire is safe. |
| SEV2 | worker | `worker/tasks.py:1154-1297` (`_sync_channel_catalog_async`) | No per-creator advisory lock — concurrent triggers (auth callback racing with UI refresh) both burn YouTube quota and race on per-video upserts. Doubles quota at the 10k daily cap under retry storms. | `pg_advisory_xact_lock(hashtext("catalog-sync:" || creator_id))` at function entry — same pattern as `_build_dna_async`. |
| SEV2 | worker | `worker/tasks.py:266-307` (`_retrain_preference_async`) | Self-debounce check + `build_and_save` not under advisory lock — concurrent retrains both pass debounce, both compute features, only one wins UNIQUE race. Wasted CPU. | Wrap body in `pg_advisory_xact_lock(hashtext("retrain:" || creator_id))`. |
| SEV2 | worker | `worker/tasks.py:816-909, 1300, 1007, 1064` (four Beat tasks) | `_poll_clip_outcomes_async`, `_refresh_youtube_analytics_async`, `_purge_stale_source_media_async`, `_purge_stale_youtube_analytics_async` — none guard against duplicate Beat triggers (clock-skew, restart, duplicate scheduler). YouTube quota doubled per double-fire. | `SELECT pg_try_advisory_lock(hashtext('<task_name>'))` at function entry; if False, log and return. |
| SEV2 | worker | `worker/celery_app.py:45-48` + `worker/tasks.py:91-137` | Soft (3000s) < hard (3300s) < visibility (3600s) correctly ordered, but a WhisperX CPU transcription of a 90-min podcast exceeds 50min → `SoftTimeLimitExceeded` → sync wrapper catches `Exception` → `self.retry` → 3× burn. | `except SoftTimeLimitExceeded:` distinct branch: NO retry, re-raise to `on_failure`. Document `TRANSCRIPTION_TIMEOUT_S < soft_time_limit` in `DEPLOYMENT.md`. |
| SEV2 | worker | `worker/storage.py:46-49` | `LOCAL_MEDIA_DIR` defaults to `./media` (relative). systemd / docker WORKDIR / k8s job manifest changes resolve against unintended CWD. Violates CLAUDE.md "All paths absolute". | `Path(settings.LOCAL_MEDIA_DIR).expanduser().resolve()` + pydantic-settings validator that rejects relative path when `ENV=production`. |
| SEV2 | worker | `worker/tasks.py:91-137` + UI | Retry path cycles `IngestStatus: failed → retrying → failed → done`; UI briefly sees `failed` for in-flight retry, and `failed` persists post-refund (misread as "user can retry"). | Set `failed` only inside `RefundOnFailureTask.on_failure`; use `IngestStatus.retrying` for transient. |
| SEV2 | worker | `worker/tasks.py:64-85` (`RefundOnFailureTask.on_failure`) | Calls `run_async(refund_for_video(...))` from `on_failure` (main-thread context, not loop-thread). Safe under `worker_prefetch_multiplier=1`, breaks the day someone raises prefetch. | Pin CI test asserting `prefetch=1` OR rewrite refund with its own sync session. (needs-runtime-confirmation under prefetch>1) |
| SEV2 | worker | `worker/progress.py:90-97, 142-143` | Sync + async redis singletons constructed without `socket_timeout`. A wedged Redis hangs `xadd` indefinitely mid Anthropic stream. | `redis.from_url(..., socket_timeout=2.0, socket_connect_timeout=2.0)`. |
| SEV2 | dna | `dna/brief.py:21-25` | `_ANTHROPIC = Anthropic(...)` at import time. Missing/invalid `ANTHROPIC_API_KEY` cascades into worker boot failure (not per-call error) for `worker/tasks.py`, `routers/clips.py`, `routers/creators.py`. | Convert to lazy singleton — same shape as `dna/embeddings.py:23::_voyage()`. |
| SEV2 | dna | `dna/conflict.py:34-42` | `_NICHE_KEYWORDS` covers only 7 of N YouTube niche ids. Early-return at lines 83-86 treats unmapped niches as already-matched → silently disables conflict detector for the majority of creators, defeating Issue 83's "surface, don't silently override" principle. | Populate keywords for every id in `youtube.categories.NICHE_IDS`, OR flip unmapped policy to "no opinion." Add test enumerating NICHE_IDS to force a decision on each new niche. |
| SEV2 | dna | `dna/embeddings.py:30-40` | `_embed` / `_aembed` have no return-type annotations (CLAUDE.md mandates). SDK rename would be runtime AttributeError, not mypy. | Annotate with concrete Voyage return type. |
| SEV2 | dna | `dna/embeddings.py:30` | `@retry` defaults to `reraise=False` → final failure wraps in `tenacity.RetryError`. Callers catching narrower exceptions (e.g. `voyageai.APIError` for credit-refund) miss it. | `@retry(..., reraise=True)`. |
| SEV2 | clip_engine | `clip_engine/ranking.py:139` | `dna_match=c.get("score")` seeds preference feature with the composite score it's trying to predict — feature collinear with its own label, DNA-fit signal silently absent. Carry-forward through 8 waves. | `score_candidates` returns DNA-only fit separately; persist DNA-only to `dna_match`, composite to `score`. Unit test: DNA path `dna_match` = raw Claude score; cold-start `dna_match=None`. |
| SEV2 | clip_engine | `clip_engine/candidates.py:113` | Candidate windows never deduped — two peaks 35s apart can both anchor to the same silence boundary, yielding clips with >80% IoU. Violates principle #9 "One idea per Short," wastes render budget. Carry-forward. | IoU-merge pass after chronological sort: if IoU > 0.5 drop lower-prominence; track prominence in dict. Eval scenario asserting one merged window. |
| SEV2 | clip_engine | `clip_engine/scoring.py:199` | Hardcoded `model=settings.ANTHROPIC_MODEL` (Sonnet 4.6) across all 3 LLM call sites. Issue 84 flagged Haiku 4.5 A/B for clip-scoring as ~67% cost reduction — follow-up still NOT filed as a tracked issue (re-grepped `docs/issues.md`). Compounds at 10k-creator scale. | File new issue: introduce `ANTHROPIC_MODEL_CLIP_SCORING`, A/B against `tests/eval/scenarios/*.yaml`, flip only if parity holds. |
| SEV2 | clip_engine | `clip_engine/ranking.py:102` | `select(Clip).where(Clip.video_id == video_id)` missing `creator_id` predicate (defense-in-depth). Not a live leak today (internal caller passes correct `video_id`), but the CLAUDE.md rule is "filter on creator_id on EVERY query touching a creator-scoped table." | Add `.where(Clip.creator_id == creator_id)` to the existing-clips probe + regression test. |
| SEV2 | clip_engine | `clip_engine/render.py:82-105` | Haar cascade on a SINGLE mid-clip keyframe — for a creator who pans/cuts shots the mid-frame may not contain the speaker; reframe defaults silently. | Sample 3 keyframes (25%, 50%, 75%), take median x. ~3 ffmpeg seeks + 3 Haar passes <300ms on 1080p. (needs-runtime-confirmation on eval set) |
| SEV2 | clip_engine | `clip_engine/scoring.py:23` | Module-level `AsyncAnthropic` binds httpx pool to first-seen loop. Under Celery `run_async` (`worker/tasks.py:154`) each task creates a fresh loop, but the singleton's pool stays bound to the worker's first loop. Known async-singleton-in-celery gotcha. | Lazy per-loop singleton via `lru_cache` keyed on `asyncio.get_event_loop()` OR drop to sync `Anthropic` via `asyncio.to_thread`. (needs-runtime-confirmation) |
| SEV2 | _root_infra | `limiter.py:19-27` | `jwt.decode(..., options={"verify_exp": False})` + bare `except Exception: pass` → expired/exfiltrated token still keys per-creator rate-limit bucket (quota-leak vector); JWT_SECRET_KEY misconfig silently swallowed. | `options={"verify_exp": True}, leeway=300`; narrow except to `(jwt.InvalidTokenError, KeyError, ValueError)`; log decode failures via rate-limited counter so misconfig is visible. |
| SEV2 | _root_infra | `main.py:135-153` | `/health` opens fresh psycopg.AsyncConnection AND fresh `aioredis.from_url` per probe — sustained connect/disconnect against Postgres OUTSIDE the SQLAlchemy pool, competing with app traffic and defeating PgBouncer math. | Reuse SQLAlchemy `engine` for PG check; bind module-level redis singleton at import time and `aclose()` in lifespan. Wrap both in `asyncio.wait_for(..., timeout=2.0)`. |
| SEV2 | _root_infra | `api_key.py:113-114` | Every API-key request issues `UPDATE creator_api_keys SET last_used_at = now()` + `commit` in the auth dependency. Per-request hot-row fsync. | Coarse-grain: only UPDATE if `last_used_at IS NULL OR last_used_at < now() - interval '60 seconds'`; skip commit when no row changed. |
| SEV2 | _root_infra | `observability.py:43-44, 224-241` | Celery `_bind_request_id` / `_record_task_and_clear` use module-level ContextVars — safe ONLY under prefork pool. Migration to gevent/eventlet/threads silently corrupts correlation ids. | Assert `app.conf.worker_pool == "prefork"` at worker startup, OR key task start off `task.request.id` in per-task dict. |
| SEV2 | _root_infra | `db.py:80-103` | `recreate_engine()` is module-public, no guard against being called with in-flight sessions (would leave outstanding AsyncSessions holding refs to disposed pool). | `_already_called` flag + raise on re-entry; document MUST-NOT precondition; underscore-prefix the function. |
| SEV2 | _root_infra | `auth.py:46-54` / `api_key.py:96-117` | Bootstrap SELECT (Creator / CreatorApiKey lookup) issued BEFORE `session.info["creator_id"]` is set — works today because `creators` and `creator_api_keys` are RLS-exempt; a future migration that flips either under RLS breaks ALL auth with 401-everywhere outage. | CI test enumerating RLS-exempt tables from `pg_policies` asserting `creators` + `creator_api_keys` are still exempt; OR refactor bootstrap to use `AdminSessionLocal` (BYPASSRLS) for lookup only. |
| SEV2 | _root_infra | `config.py:174-184` | `print(..., file=sys.stderr)` on fatal startup — violates CLAUDE.md "logging module only — no print()". Container log aggregators parsing JSON miss the fatal message. | `logging.basicConfig(stream=sys.stderr, ...)` immediately inside the `except ValidationError`, then `logging.error(...)`. |
| SEV2 | youtube | `youtube/oauth.py:290` | `get_valid_access_token` doesn't catch `redis.RedisError` on lock `SET NX EX` → Redis blip 500s every near-expiry token-refresh path across API + worker. Carry-forward through 7 waves. | `try/except redis.RedisError`: fall back to lockless refresh (Google tolerates rare double-refresh; DB write idempotent on creator_id) OR raise `HTTPException(503)`. Regression test mocking `ConnectionError`. |
| SEV2 | ingestion | `ingestion/transcribe.py:116-123,137-138` | Deepgram normalizer hard-key indexing → `KeyError` on missing timestamp, burns Celery retry instead of skipping partial item. WhisperX and AssemblyAI normalizers already use `.get(default)`. Carry-forward through 8 waves. | Switch to `.get("start")` / `.get("end")` in both list comprehensions; skip None timestamps. |
| SEV2 | ingestion | `ingestion/transcribe.py:43-60` | `_guard_audio_size` swallows `OSError` → silent empty-pipeline run on AssemblyAI; triggers auto-refund for a cause we could detect upfront. Carry-forward. | In `except OSError`, raise `FileNotFoundError(...)`. |
| SEV2 | billing | `billing/stripe_client.py:23-67` | `create_checkout_session` does NOT pass `idempotency_key=` to Stripe — user double-click / router retry creates two Checkout sessions. Not double-charge today (mode=payment charges only completed sessions) but back-button / parallel tabs both completing → user paid twice, ledger granted twice. | Accept `intent_id: str` from router (server-side UUID at /pricing render time); call `_STRIPE.checkout.sessions.create(params, options={"idempotency_key": intent_id})`. |
| SEV2 | billing | `billing/stripe_client.py:20` | `_STRIPE = stripe.StripeClient(...)` with no explicit HTTP timeout (defaults ~80s). Under Stripe slowdown one worker thread pinned per stuck call; `asyncio.to_thread` executor pool is capped. Violates scale-checklist E. | `stripe.StripeClient(key, http_client=stripe.HTTPXClient(timeout=10.0))`; add `STRIPE_TIMEOUT_S=10` to `.env.example`. |
| SEV2 | billing | `billing/stripe_client.py:67` | `return session.url` typed `-> str` but Stripe types it `Optional[str]`. None slips through as literal `"None"` redirect or AttributeError deeper. | Assert: `if url is None: raise RuntimeError(f"Stripe returned no checkout URL for session {session.id}")`. |
| SEV2 | improvement | `improvement/brief.py:81` | `web_search` registered without `max_uses` — unbounded search fan-out per brief. Cost amplifier at hundreds-of-creators × billed per round-trip. | `"max_uses": settings.ANTHROPIC_WEB_SEARCH_MAX_USES` (default 5); add to `config.py` + `.env.example`. |
| SEV2 | improvement | `improvement/brief.py:81` | `tool_choice` not set (defaults `auto`) — model can skip web_search entirely. Brief's value prop (cite a CURRENT algorithm factor) silently weakened. | `tool_choice={"type": "auto"}` explicitly, OR `{"type": "tool", "name": "web_search"}` to force ≥1 search. Regression test asserting ≥1 `tool_use` block under a recorded fixture. |
| SEV2 | improvement | `improvement/brief.py:5-7, 76` | Docstring says static prefix is below Sonnet 4.6's 1024-token cacheable floor → `cache_control: ephemeral` marker is 1.25× write premium for cache writes that will never be read. | Pad `_SYSTEM_INSTRUCTIONS` past 1024 tokens (mirror `dna/brief.py`), OR drop the `cache_control` marker. |
| SEV2 | improvement | `improvement/brief.py:136-142, 157-163` (deferred behind SDK bump) | Both log sites bundle cache reads into single counters. 5m vs 1h TTL-tier breakdown requires anthropic 0.40 → 0.105+ (Issue-84 follow-up). | After SDK bump: `getattr(usage, "cache_creation", None)` and emit `cache_creation_5m=` / `cache_creation_1h=`. |
| SEV2 | upload_intel | `upload_intel/timing.py:54-55` | `optimal_gap_hours` STILL lacks the bounds/coercion guard that hardened sibling `best_upload_windows` (Issue 75d). Same router payload returns inconsistent validity decisions. Carry-forward through 8 waves. | Filter+coerce rows first; return `None` if `<2` valid. Regression test mirroring `test_best_upload_windows_skips_malformed_rows`. |

Plus **48 cleanup** items across all 11 modules (typing gaps, function-local imports, duplicated schemas, magic numbers, etc.). Per-finding detail in `docs/assessment/modules/*.md`.

**Module verdicts:** clip_engine NEEDS-WORK · dna NEEDS-WORK · preference NEEDS-WORK · youtube NEEDS-WORK · worker NEEDS-WORK · routers NEEDS-WORK · ingestion NEEDS-WORK · billing NEEDS-WORK · upload_intel NEEDS-WORK · improvement NEEDS-WORK · _root_infra NEEDS-WORK · **0 BLOCKER · 0 cross-tenant leak**.

## Layer 2 — scale checklist (`scale-checklist.md`)
| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | PgBouncer transaction-pooling + `prepare_threshold=None` in place (`db.py:33`, applied to both engines at lines 51/61). Pool sizing: app `15+5`, admin `5+10`, `pool_recycle=1800`, `pool_pre_ping=True`. Documented against 25-conn PgBouncer sidecar in `DEPLOYMENT.md`. **Locust evidence still pending (Issue 78f)** — the single remaining structural gate between CONDITIONAL and YES. *(unchanged ⚠️)* |
| B Async loop hygiene | ⚠️ | **REGRESSED from ✅**: 2 SEV1 in preference module (`from_bytes` lock-contended joblib.load + `fit` LightGBM training BOTH on event loop) violate this axis. clip_engine.scoring's `AsyncAnthropic` loop-binding under Celery `run_async` is a third needs-runtime suspect. Otherwise async paths verified: Stripe, R2 PUT, ffprobe all `asyncio.to_thread`; routers walk found no remaining hot-path sync-in-async. |
| C Celery idempotency | ⚠️ | **REGRESSED from ✅**: routers/clips.py `_render_clip_async` has the "already done? return" probe, but `_transcribe_async` / `_signals_async` / `_ingest_async` retry paths and `generate_clips` (missing `RefundOnFailureTask` entirely) do not. Four Beat tasks lack advisory locks against duplicate triggers. Wave-4 Fix 2 (refund pack_id partial UNIQUE) remains green. |
| D Tenant isolation | ✅ | **STRUCTURAL** — `db.py:119-148` per-transaction `set_config('app.creator_id', :cid, true)` via `after_begin` listener whenever `session.info["creator_id"]` is set; RLS migration `0010_rls_policies` in place. Re-verified on all 3 NEW Wave-8 endpoint surfaces (`/creators/me/api-keys`, `/clips/ingest`, `/creators/me/insights`) — every SELECT filters `creator.id` and `_fetch_performers` defensively re-filters by `creator_id`. Bootstrap-query-before-GUC remains correct by convention (creators + creator_api_keys RLS-exempt) — needs the CI test from `_root_infra.md` SEV2 to keep honest. |
| E Backpressure | ⚠️ | `youtube/oauth.py:290` Redis-down → 500 STILL OPEN (carry-forward through 7 waves). billing has no Stripe HTTP timeout (SEV2). worker has no socket_timeout on its redis singletons (SEV2). `/health` builds fresh clients per probe (SEV2). Wave-5 Fix 1 cleanly closed the 3 routers fail-closed sites — `aset_owner` is now uniformly `try/except RedisError → stream_url=None`. |
| F Rate limit / quota | ⚠️ | **REGRESSED from ✅**: `/clips/ingest` (NEW Wave-8) decorated `@limiter.limit("20/hour")` with no explicit `key_func` — likely keyed on IP not per-creator (scale-checklist F explicit requirement). slowapi+Redis backend is correct (no in-memory fallback); the gap is the key derivation. `limiter.py verify_exp=False` (SEV2) is a per-creator quota leak vector. EXPIRE-on-every-INCR for YouTube quota stays correct. |
| G Observability | ✅ | RequestIDMiddleware + JsonLogFormatter wired; correlation id propagates API → Celery via ContextVar; route-template labels bound Prometheus cardinality; no PII/token in any logger line audited across 11 module walks. Per-creator audit-log row missing on api-key create/revoke (SEV2 above) is a security-event traceability gap, not an observability foundation gap. |
| H Migration / pgvector | ✅ | All migrations at HEAD `0015_creator_api_keys` (Wave 8 — Issue 95 backend). `CREATE INDEX CONCURRENTLY` pattern + partial UNIQUE for "uniqueness within subset" + expand-then-contract for renames consistently applied. Backups + PITR restore-test still unverified — pre-launch operational task. |
| I Secrets / deletion | ✅ | MultiFernet with primary + previous-key fallback for zero-downtime rotation; rotation script + RUNBOOKS in place. `decrypt()` maps InvalidToken → safe error; no ciphertext/key material in error messages. /docs disabled outside dev; /metrics gated behind `secrets.compare_digest` bearer with config fail-safe. Account-deletion endpoint exists (`routers/auth.py:245`); end-to-end prod test still pending. |

**Compliance posture (no change since Wave 4):** YouTube ToS §III.E.4.b 30-day analytics retention purge runs daily (verified clean in youtube.md walk). Honesty disclaimer enforced in Python on both brief paths (improvement.md walk re-verified). No virality language in any module's strings (grepped). Limited Use disclosure in `/static/privacy.html`. TOS + Privacy linked from every static template (Wave-6 Fix B). Google OAuth verification submission unblocked from our side.

## Diff vs previous report (2026-05-31 post Wave-4, commit `67fddc9`)

**Fixed & verified this cycle (Wave 5 + Wave 8 closures re-confirmed):**
- ✅ The 3-site SEV1 from post-Wave-4 (`routers/creators.py::sync_catalog`, `creators.py::build_dna`, `clips.py::render_clip` fail-closed `aset_owner`) — **CLOSED by Wave-5 Fix 1**. Re-verified in routers walk: every `aset_owner` now wrapped; `stream_url` responses uniformly `str | None`.
- ✅ Wave-4 Fix 2 (refund pack_id partial UNIQUE migration 0013) — still in place.
- ✅ Wave-4 Fix 3 (analytics retention purge, COMPLIANCE.md §2) — verified clean by youtube walk.
- ✅ Issue 95 backend (Wave 8) shipped a NEW endpoint surface with per-creator isolation correct at every SELECT (routers walk explicitly verified `api_keys.py:96-101, 122, 164`, `clips.py:281`, `insights.py:151, 170, 93-96`).
- ✅ Issue 95 frontend (THIS session) shipped without introducing a Python-side defect — pure HTML/CSS/JS in `static/profile.html`; new test in `tests/test_static.py` pins the section + endpoints + modal copy + mono register; raw key wiped from DOM on modal close; +1 test in default lane.
- ✅ Coverage **75.19% vs 69.54% baseline** — +5.65pp gain across Waves 5–8 + Issue 95 frontend. Re-baseline recommended.

**New SEV1 this run (2):**
- 🆕 **SEV1 preference/model.py:126-132** — `from_bytes` mutates process-global `joblib.numpy_pickle.NumpyUnpickler` under `_UNPICKLER_LOCK` then runs blocking `joblib.load` on event loop. Process-wide lock serializes across creators.
- 🆕 **SEV1 preference/train.py:83** — LightGBM `fit()` runs synchronously inside `async def build_and_save`. Blocks event loop for seconds on power creators.

Both were graded SEV2 in prior cycles as bounded library-upgrade risks; the deeper preference walk this run found the actual blocking-call-on-loop and process-wide-lock semantics.

**New SEV2s this run (Wave-8 surfaces + deeper walks):**
- 🆕 `routers/insights.py:147` — `nullif(predicate, True)` aggregate likely returns 0 for shorts/longs/done counts. Wave-8 new endpoint.
- 🆕 `routers/clips.py::ingest_clip` — temp-file leak on non-HTTPException path. Wave-8 new endpoint (inherits the carry-forward shape from `upload_video`).
- 🆕 `routers/clips.py::ingest_clip` — rate-limiter key likely IP not per-creator. Wave-8 new endpoint.
- 🆕 `routers/api_keys.py` — missing `append_audit` row on create/revoke. Wave-8 new endpoint.
- 🆕 `worker/tasks.py::_ingest_async` — retry orphans source mp4 in R2 (ToS retention promise + unbounded storage). Re-graded from carry-forward "re-do on redelivery."
- 🆕 `worker/tasks.py::_transcribe_async` + `_signals_async` — no idempotency guard (paid Deepgram call re-fires on redelivery).
- 🆕 `worker/tasks.py::generate_clips` — missing `RefundOnFailureTask` base — terminal failure doesn't refund.
- 🆕 `worker` — `_sync_channel_catalog_async` / `_retrain_preference_async` / 4 Beat tasks all lack advisory locks against duplicate triggers.
- 🆕 `worker/storage.py:46` — `LOCAL_MEDIA_DIR` defaults to relative path (CLAUDE.md absolute-paths violation).
- 🆕 `_root_infra/limiter.py:19-27` — `verify_exp=False` + bare except → expired token still keys per-creator rate-limit bucket.
- 🆕 `_root_infra/main.py:135-153` — `/health` builds fresh PG + redis clients per probe.
- 🆕 `_root_infra/api_key.py:113-114` — per-request `UPDATE last_used_at` + commit (write amplification).
- 🆕 `dna/brief.py:21` — Anthropic client at import time cascades into worker boot failure on missing key.
- 🆕 `dna/conflict.py:34-42` — conflict detector silently disabled for unmapped niches.
- 🆕 `improvement/brief.py:81` — `web_search` registered without `max_uses` or explicit `tool_choice`.
- 🆕 `billing/stripe_client.py:23` — missing `idempotency_key` on Stripe `checkout.sessions.create`.
- 🆕 `billing/stripe_client.py:20` — Stripe client has no explicit HTTP timeout.

**Carry-forward (re-checked, still open):**
- youtube/oauth.py:290 Redis-down → 500 (7 waves open).
- ingestion/transcribe.py:116 Deepgram normalizer KeyError (8 waves open).
- ingestion/transcribe.py:43 _guard_audio_size swallows OSError (8 waves open).
- upload_intel/timing.py:54-55 missing bounds/coercion guard (8 waves open).
- clip_engine ranking.py:139 dna_match collinear with label (8 waves open).
- clip_engine candidates.py:113 no overlap dedup (8 waves open).
- routers/tasks.py enumeration oracle + Last-Event-ID 500 (carry-forward).
- Unbounded list endpoints (carry-forward).
- Over-broad `except Exception` across auth + billing + videos (carry-forward).

**Counts (Layer 1, all modules):**

| Severity | This run (post Wave-8 + Issue 95 frontend) | Wave 4 | Wave 3 | Wave 2 | Wave 1 |
|---|---|---|---|---|---|
| BLOCKER | 0 | 0 | 0 | 0 | 0 |
| SEV1 | **2** | 1 | 0 | 3 | 1 |
| SEV2 | 51 | 34 | 33 | ~36 | 31 |
| cleanup | 48 | ~43 | ~40 | ~38 | 34 |

**SEV1 trajectory across seven cycles: 4 → 2 → 1 → 3 → 0 → 1 → 2.** SEV2 jump 34 → 51 reflects (a) Wave-8's 3 new endpoint surfaces each shipping 1–2 new SEV2s, (b) deeper module walks finding the worker idempotency/lock gaps, and (c) the first-time pip-audit local run + the `limiter.py verify_exp` gap surfaced by the routers+_root_infra walks crossing paths.

## Top 10 actions, in order

1. **Fix the 2 preference SEV1s** (`preference/train.py:83` + `model.py:126-132`). Both ~10-LOC: wrap `fit()` in `asyncio.to_thread`; wrap `from_bytes` in `to_thread` AND replace global-class-swap with a per-load Unpickler subclass on `io.BytesIO`. Returns SEV1 to 0; restores axis B to ✅. ~25 LOC total + 2 regression tests.

2. **Triage the 16 pip-audit vulnerabilities.** This is the first local pip-audit run in 5 waves — likely a baseline-staleness issue, not new CVEs. Confirm against CI's pip-audit job; either bump the affected packages (likely transitive deps of the test stack) or update `baselines.json` to match CI's accepted floor. Returns gate to ✅.

3. **Fix the insights `nullif` aggregate** (`routers/insights.py:147`). If the bug is real (likely), every insights pageview returns zeros for shorts/longs/done counts — the rebuild's signature panels (Issue 93) are silently broken. Rewrite as `func.count(case((...,1), else_=None))` + seeded test. 15 LOC.

4. **Per-creator rate-limit-key sweep** (`routers/clips.py::ingest_clip`, plus audit the rest of the authenticated routes). The new `/clips/ingest` ships with the IP-based default; the rule from scale-checklist F is per-creator on every authenticated route. Hoist a shared `creator_key` helper that resolves from session-cookie OR bearer-auth dependency. ~50 LOC + uniform decorator update.

5. **`worker/tasks.py::_ingest_async` orphan-mp4 fix.** Retry leaves the original mp4 unreferenced in R2 → permanently orphaned → breaks SOURCE_MEDIA_RETENTION_HOURS ToS promise + unbounded storage cost. Capture `prior_source_uri` at function entry; `adelete_file(prior_source_uri)` after commit if it's an mp4. 15 LOC + regression test.

6. **Add `RefundOnFailureTask` base to `generate_clips`.** It's the one billable-pipeline task missing the base class — terminal failure leaves the user paid but with no clips. One-character edit; refund is already idempotent on `pack_id=refund:<video_id>`.

7. **Locust load test on real staging cluster (Issue 78f, carry-forward).** Still the single remaining structural gate between CONDITIONAL and YES. Settles A, E, the `AsyncAnthropic` loop-binding suspect in clip_engine.scoring.py:23, and the `worker.tasks.RefundOnFailureTask.on_failure` thread/loop coupling under prefetch>1.

8. **`youtube/oauth.py:290` Redis-down fix.** 7 waves open, ~5 LOC: wrap `redis_client.set()` in `try/except redis.RedisError`, fall back to lockless refresh with warning log (Google tolerates rare double-refresh; DB write idempotent). Same fail-open pattern Wave-5 applied to the routers trio. Regression test mocking `ConnectionError`.

9. **`limiter.py verify_exp=False` fix.** Enable JWT exp verification with leeway=300; narrow the bare except to `(jwt.InvalidTokenError, KeyError, ValueError)`; log decode failures via rate-limited counter so JWT_SECRET_KEY misconfig becomes visible. Closes per-creator quota-leak vector. ~10 LOC.

10. **Submit Google OAuth app verification.** Unblocked from our side since Wave 4 Fix 3. External Google process; user-side action. Nothing in code blocks this anymore — privacy.html has Limited Use disclosure, every template links TOS + Privacy, retention policy is documented + enforced + tested.

---

## What this cycle confirmed about the assessment posture

- **The Issue 95 backend → frontend handoff was clean.** The bearer-auth surface (`/clips/ingest`) and the session-cookie management surface (`/creators/me/api-keys`) stayed structurally separate. Per-creator isolation is enforced at every SELECT on both surfaces. The frontend shipped without touching Python — pure design-system retrofit. The new endpoint surface adds 4 SEV2s (insights aggregate, ingest_clip temp-leak, ingest_clip rate-limit, api_keys audit-log) but no cross-tenant leak.

- **The 5-wave carry-forward backlog is the real signal.** youtube oauth Redis-down (7 waves), Deepgram normalizer KeyError (8 waves), upload_intel optimal_gap_hours bounds (8 waves), clip_engine dna_match collinearity (8 waves) — all small, mechanical fixes that compound to "things we know are wrong but keep deferring." Wave-9 should pick at least 2 off this list before any new feature lands.

- **The SEV2 jump (34 → 51) is mostly Wave-8 new surfaces + deeper walks, not actual regression.** Subtracting the 11 new findings on Wave-8 surfaces and the 6 deeper-walk reclassifications (worker idempotency, limiter, /health, etc.), the carry-forward count is ~34 — flat against Wave 4. The "deeper walk" finds are real and need fixing, but they were always there; the assessment is doing what it's supposed to (different lens → different surface of the same iceberg).

- **The two preference SEV1s are the real surprise.** Prior cycles graded `preference/model.py` issues as cleanup or "needs LightGBM round-trip test." The current walk reads the import path more carefully and finds `from_bytes` actually mutating a `joblib.numpy_pickle.NumpyUnpickler` module-global under a process-wide lock — that's not a library-upgrade risk, that's a hot blocking call on the loop under a lock that serializes every preference rerank across the entire process. Same shape for `fit()` on LightGBM. Both ~10-LOC fixes; both load-bearing for the "hundreds of concurrent creators" scale target.
