# CreatorClip — Production Assessment

**Date:** 2026-06-09  ·  **Commit:** `da36382`  ·  **LOC:** ~56,400 (py/js/html/css, excl. venv)  ·  **Tests:** 958 collected (127 deselected); coverage run green

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

Both gating items from the 2026-06-08 run are FIXED (the `index.html` runaway-poll BLOCKER and the billing-webhook RLS SEV1), and all Layer-0 gates are green with coverage up to 75.93%. But this deeper pass surfaced **7 new SEV1s** across 5 modules: two real frontend ships-broken defects (a dead non-catalog analysis path and unescaped third-party/LLM content reaching `innerHTML` — stored XSS), an unratelimited in-request multimodal LLM endpoint with a fail-open cache (uncapped cost hole), creator email logged in `expire_trials` (PII invariant violation), chapter generation deterministically truncating on 1h+ videos (`max_tokens=512`), inert cache markers below Sonnet 4.6's 2048-token floor in titles/thumbnails, and a `ttl: "1h"` cache param riding the anthropic 0.40.0 pin without its required beta header. Conditional on those 7 plus the still-deferred Locust 300-user run.

---

## Layer 0 — deterministic gates (from `_machine.json`)

| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | 0 issues | 0 | ✅ |
| mypy | 0 errors | 0 | ✅ |
| coverage | 75.93 % | 75.20 % | ✅ (+0.73) |
| bandit | high 0 / med 0 | high 0 / med 0 | ✅ |
| pip-audit | 0 vulns | 0 | ✅ |
| freshness | 11 / 11 days | 90-day threshold | ✅ |

**Top uncovered load-bearing paths (carry-forward):** ffmpeg render shell-outs (`render_cleaned_clip_file`, worker R2-upload paths) are mocked in tests; all `static/` JS has no test harness — notably the two frontend SEV1s below live exactly in that untested surface.

---

## Layer 1 — module register (ranked)

**Tally across 14 modules: 0 BLOCKER · 7 SEV1 · 63 SEV2 · 71 cleanup**

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| **SEV1** | static_frontend | `index.html:681`, `insights.html:441,573,604-605` | XSS sinks: YouTube video titles (third-party-controlled via `/videos/link`) and LLM analysis output (prompt-injectable via transcripts) interpolated into `innerHTML` unescaped — :604-605 is stored XSS on every saved-insights page load. | Extract the existing `_esc()` (analysis.html:793) into `static/util.js`; escape every interpolation or build nodes with `textContent`. Regression test with `<img src=x onerror=alert(1)>` title. |
| **SEV1** | static_frontend | `analysis.html:708` | `getElementById('youtube_url')` — element doesn't exist (input is `url-input`); uncaught TypeError kills the whole non-catalog-video analysis path before stream subscription. The Issue-125 "Ingest this video" CTA has never worked. | Build URL from in-scope `urlRaw` (`'/?yt=' + encodeURIComponent(urlRaw)`); add a smoke test for the non-catalog path. |
| **SEV1** | routers | `thumbnails.py:54-145` | `GET /creators/me/thumbnail-patterns` has NO rate limit yet runs a Claude multimodal call (≤10 images) in-request; 24h Redis cache is fail-open — Redis degraded ⇒ every request is a billed vision call; no single-flight ⇒ N concurrent first-hits fire N LLM calls. | Add `@limiter.limit("10/hour", key_func=creator_key)` + per-creator single-flight lock (`SET NX thumbnail_patterns_lock:{id}`, 10s TTL). |
| **SEV1** | worker | `tasks.py:1683-1686` | `_expire_trials_async` logs creator EMAIL in `trial_expired_zero_balance` — direct violation of the no-PII-in-logs invariant. | Drop `email` from the SELECT (:1675) and the log line; `creator=%s trial_ends_at=%s` suffices. One line. |
| **SEV1** | knowledge | `chapters.py:209` | `max_tokens=512` deterministically truncates chapter JSON for ~1h+ videos (≈20+ chapters + duplicated `description_block` > cap); all 3 Celery retries fail identically — the product's core input class. | Raise to 2000 AND drop `description_block` from the schema (`parse_chapters` already rebuilds it in Python); test with 60 boundaries. |
| **SEV1** | knowledge | `titles.py:120-133`, `thumbnails.py:201-216` | `cache_control` markers sit on a ~1,200–1,500-token prefix — below Sonnet 4.6's **2048**-token floor (docs/DECISIONS.md:2283 records 1024, which is the Sonnet **4.5** floor). 1.25× write premium every call, zero reads — same inert-marker class the Issue-135 audit fixed in hooks/chapters. | Runtime-confirm via `usage.cache_read_input_tokens` on back-to-back calls; if inert, remove markers (hooks.py:174-179 pattern) or grow the prefix past 2048; correct DECISIONS.md. |
| **SEV1** | clip_engine | `scoring.py:237-247` | DNA scoring sends `cache_control: {ttl: "1h"}` through `anthropic==0.40.0`, which lacks the typing AND never sends the `extended-cache-ttl-2025-04-11` beta header; if the API gates `ttl` on that header, every DNA scoring call 400s → total DNA-scoring outage, invisible to fixtures. (needs-runtime-confirmation) | Upgrade the `anthropic` pin (also retires two `type: ignore`s); interim `extra_headers={"anthropic-beta": "extended-cache-ttl-2025-04-11"}`; recorded-fixture test on exact request shape. |
| SEV2-top | youtube | `data_api.py:84`, `analytics.py:42` | `consume(cost)` runs once but the retry loop fires up to 4 real Google requests — local counter undercounts, real quota drains, Beat refresh 403s creators we think have budget (ToS §4). | Move `await consume(cost)` inside the retry loop before each `.get(...)`; test: two 429s + one 200 ⇒ counter += 3. |
| SEV2-top | youtube | `oauth.py:312-332` | Redis fail-open self-destructs: on Redis outage the except arm proceeds without lock but the `finally` calls `redis_client.eval(...)` against the same dead Redis — the RedisError replaces the successful return; refresh 500s anyway. | Track `lock_held` set only on successful SET; release only if held, wrapped in try/except RedisError. Regression test with both `set` and `eval` raising. |
| SEV2-top | billing | `stripe_client.py:34` | `stripe.max_network_retries = 3` is provably inert for `StripeClient` (verified against stripe 11.4.0) — all Checkout calls run with ZERO network retries. | Pass `max_network_retries=3` to the `StripeClient` constructor; delete the inert global. Safe: `intent_id` Idempotency-Key dedupes at Stripe. |
| SEV2-top | billing | `ledger.py:152` | `deduct_for_video` raises `HTTPException(402)` inside a Celery context — caught by generic retry ⇒ 3 full re-download/re-extract cycles, then terminal failure with the "purchase a pack" copy lost. | Raise domain `InsufficientBalanceError`; routers map to 402; `ingest_video` treats as non-retryable so refund fires immediately. |
| SEV2-top | ingestion | `transcribe.py:200` | AssemblyAI `transcript.status` never checked — a failed hosted transcription silently yields an empty "successful" transcript, bypassing retry AND the terminal-failure refund. | `if transcript.status == aai.TranscriptStatus.error: raise RuntimeError(...)` so the worker retry/refund chain engages. |
| SEV2-top | worker | `tasks.py:415,1376,1523,1638,1874,1980` | Six session-scoped `pg_try_advisory_lock` sites can return a pooled connection with the lock still held (unlock fails / PendingRollback) — every later Beat tick skips until recycle. | `PoolEvents.reset` listener emitting `pg_advisory_unlock_all()` on both engines, OR migrate to `pg_advisory_xact_lock` (pattern at tasks.py:1139). |
| SEV2-top | _root_infra | `config.py:237` | All production fail-fast validators gated on free-string `ENV == "production"` — `ENV=prod` typo silently disables Stripe-secret enforcement, `/metrics` fail-safe, and path checks at once. | `ENV: Literal["development","test","staging","production"]` — pydantic rejects anything else at boot; sweep other ENV compares (main.py:89). |
| SEV2-top | routers | `auth.py:232-236` | Decrypted Google refresh token sent to the revoke endpoint as a URL **query parameter** — recorded by proxies/egress logs. Google documents revocation as form-encoded POST. | `client.post(url, data={"token": ...}, headers={"Content-Type": "application/x-www-form-urlencoded"})`. |
| SEV2 | clip_engine | 8 findings | generate check-then-insert race (double-insert + double-bill), libass path escaping, post-snap `end_s` > duration, bare `json.loads` on scoring response, import-time AsyncAnthropic loop binding, `_in_window` boundary undercount, caption word dup, 120s cleaned-render timeout. | See `docs/assessment/modules/clip_engine.md`. |
| SEV2 | static_frontend | 9 findings | JS-in-attribute escape bug (profile), camelCase/snake_case `registerTask` mismatch (clean/edit renders never tracked), pricing page broken for both signed-in AND anonymous users, unused htmx CDN script w/o SRI on 5 authed pages, privacy policy promises a deletion UI that doesn't exist, contradictory early-access pricing page (+ no honesty disclaimer), silent feedback-vote failures. | See `static_frontend.md`. |
| SEV2 | worker | 7 more | soft-timeout leaves `ingest_status=running` forever (spinner + refund w/o retryability), purge lock released before work, session+lock held across YouTube loops (unbounded refresh sweep), ffmpeg orphan on soft-timeout (now timeout-bounded), clean/edit ownership not re-verified, re-renders unbilled/undocumented, thumb-Redis loop binding. | See `worker.md`. |
| SEV2 | _root_infra / dna / analysis / youtube / others | 30 more | slowapi sync Redis on the event loop, backend-conditional prod secrets unchecked, JWT secret length floor, MultiFernet rebuilt per call (carried), upload-gap week-wrap bug (dna + upload_intel), VOYAGE_API_KEY silent prod skip, streamed analysis never carries the honesty disclaimer (masked by static copy), activity telemetry always "anonymous" + log-injection 500s, link/upload double-submit 500, LLM held in request path on /clips/generate, uncapped catalog pagination, joblib unpinned (RCE-allowlist enforcement point), 50-id silent truncation, sequential identity fetch, nullable cache-token log drop. | Per module file. |
| Cleanup | (71 items) | various | Typing tightness, DRY (12× enqueue block, 4× escape helpers, 6× logout copies, dup retry loops), dead code (`_DISCLAIMER`, htmx, constants), fonts-CDN GDPR, client N+1, hardcoded Haiku model ids. | Per module file. |

**Module verdicts:**

| Module | Verdict | B | S1 | S2 | C |
|---|---|---|---|---|---|
| static_frontend | **NEEDS-WORK** | 0 | **2** | 9 | 7 |
| knowledge | **NEEDS-WORK** | 0 | **2** | 1 | 7 |
| clip_engine | **NEEDS-WORK** | 0 | **1** | 8 | 6 |
| worker | **NEEDS-WORK** | 0 | **1** | 8 | 5 |
| routers | **NEEDS-WORK** | 0 | **1** | 5 | 6 |
| _root_infra | NEEDS-WORK | 0 | 0 | 7 | 7 |
| youtube | NEEDS-WORK | 0 | 0 | 6 | 3 |
| analysis | NEEDS-WORK | 0 | 0 | 5 | 3 |
| dna | NEEDS-WORK | 0 | 0 | 4 | 7 |
| ingestion | NEEDS-WORK | 0 | 0 | 4 | 5 |
| billing | NEEDS-WORK | 0 | 0 | 3 | 3 |
| improvement | NEEDS-WORK | 0 | 0 | 1 | 4 |
| preference | NEEDS-WORK | 0 | 0 | 1 | 4 |
| upload_intel | NEEDS-WORK | 0 | 0 | 1 | 4 |

---

## Layer 2 — scale checklist

| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | _root_infra re-verified pool math 15+5 app / 5+10 admin against the DEPLOYMENT.md inequality; `prepare_threshold=None` for PgBouncer txn mode; `pool_pre_ping` + `pool_recycle=1800`. **Locust 300-user run still not executed** (needs load evidence). |
| B Async loop hygiene | ⚠️ | Regressed from ✅: slowapi 0.1.9 does a **sync** Redis round-trip inside async middleware on every rate-limited route (limiter.py:80, mitigate with `?socket_timeout=0.1`); `/videos/{id}/clips/generate` still awaits an Anthropic call in the request path (routers/clips.py:100-139). All `task.delay` sites remain correctly wrapped in `to_thread`. |
| C Celery idempotency | ⚠️ | Billing webhook fast-path FIXED (session.info stamped at billing.py:215, verified). But: `generate_and_rank_clips` check-then-insert race double-inserts + double-bills (no UNIQUE on `(video_id, rank)`), 6 advisory-lock sites can leak locks onto pooled connections, ledger 402-in-worker burns 3 full reprocess retries. |
| D Tenant isolation | ✅ | Routers agent re-traced every query — all creator-scoped, SSE gated by Redis owner key, no cross-tenant leak; RLS infrastructure intact; preference/dna/billing verified `WHERE creator_id` line-by-line. Defense-in-depth gaps (worker clean/edit ownership re-check, `sync_video_analytics` guard) tracked as SEV2s, not leaks. |
| E Backpressure | ⚠️ | Timeouts on every external client confirmed. But Stripe runs at provably 0 network retries (inert global), the youtube Redis fail-open destroys its own success in `finally`, quota undercounts per retry, and Locust is still pending. |
| F Rate limit / quota | ❌ | The thumbnail-patterns GET is axis-F's exact failure mode live: no limiter, fail-open cache, billed multimodal LLM in-request (SEV1). `/auth/login`+`/auth/callback` also unlimited. Per-creator usage quotas before LLM/render jobs remain a pre-launch TODO (CLAUDE.md). |
| G Observability | ⚠️ | Structured logs + token logging on all Anthropic calls; but `expire_trials` logs creator email (SEV1 — violates "never PII"), and nullable cache-token fields can silently drop the mandatory token line (improvement, needs-runtime-confirmation). |
| H Migration & pgvector safety | ✅ | Alembic auto-applied on deploy; pgvector index intact; creator-scoped indexes confirmed in migration 0001; UNIQUE keys on money paths verified (models.py:603/627, migration 0013). |
| I Secrets / deletion | ⚠️ | Fernet+MultiFernet rotation works; tokens read via `decrypt()` everywhere (verified). But the refresh token transits Google revocation as a URL query param (SEV2), the privacy policy promises a deletion UI that doesn't exist in profile.html (OAuth-verification reviewers check this), and the rotation runbook is still a pre-launch TODO. |

---

## Diff vs previous report (2026-06-08)

### Fixed since last run
- ✅ **BLOCKER** static_frontend runaway `_pollTimer` — FIXED (index.html:894-953: bounded ticks, exponential backoff, hidden-tab pause, stuck-warning surface; re-verified).
- ✅ **SEV1** billing webhook idempotency fast-path dead under RLS — FIXED (`session.info["creator_id"]` stamped before the idempotency query, billing.py:215; re-verified by the routers agent).
- ✅ The 2026-06-08 UX cluster landed: empty-state envelopes on `/videos`, `/clips`, `/insights/saved`; onboarding aggregation on `/auth/me` + `/creators/me`; `_registerInFlightIngests` dedupe.
- ✅ improvement streaming empty-text SEV2 — finding invalidated (guard exists in `worker/anthropic_stream.py:89-90`).
- ✅ youtube stale-`type: ignore` cleanup — invalidated (mypy `--warn-unused-ignores` proves they're required).

### New SEV1s introduced/surfaced (7)
- 🔴 static_frontend XSS sinks (`index.html:681`, `insights.html:573/604-605`) — incl. stored XSS via saved insights.
- 🔴 static_frontend `analysis.html:708` dead element id — non-catalog analysis path has never worked.
- 🔴 routers `thumbnails.py:54` unratelimited in-request multimodal LLM with fail-open cache.
- 🔴 worker `tasks.py:1683` creator email in `expire_trials` log (new code, Issue 126).
- 🔴 knowledge `chapters.py:209` `max_tokens=512` truncation on 1h+ videos.
- 🔴 knowledge titles/thumbnails inert cache markers below the Sonnet 4.6 2048-token floor (DECISIONS.md records the wrong floor).
- 🔴 clip_engine `scoring.py:237` `ttl:"1h"` on anthropic 0.40.0 without the beta header (escalated from SEV2; needs-runtime-confirmation).

### Regressed
- 🟡 Axis B ✅→⚠️ (slowapi sync-Redis-on-loop surfaced; clips/generate LLM-in-request now counted here).
- 🟡 Axis F ✅→❌ (thumbnail-patterns cost hole).
- 🟡 preference and upload_intel lost their "clean" verdicts (1 new SEV2 each: unpinned joblib at the RCE-allowlist enforcement point; circular-week gap bug).

### Carry-forward unchanged
- ⚠️ Locust 300-user run still not executed → axes A and E remain ⚠️.
- ⚠️ `TOKEN_ENCRYPTION_KEY` rotation runbook still a pre-launch TODO.
- ⚠️ ~20 SEV2s re-verified unfixed from 2026-06-07/08 (advisory-lock cluster, crypto MultiFernet, quota undercount, dna week-wrap, ingestion audio trio, Stripe idempotency-key scoping, routers enqueue DRY).

---

## Top 10 actions, in order

1. **Escape every `innerHTML` interpolation** (`index.html:681`, `insights.html:441/573/604-605`) — shared `escapeHtml()` in `static/util.js`; regression test with a hostile title. SEV1, stored XSS. ~30 LOC.
2. **`analysis.html:708`** — build the ingest URL from `urlRaw`; un-break the non-catalog analysis path (dead since Issue 125). SEV1. ~3 LOC.
3. **`routers/thumbnails.py:54`** — `@limiter.limit("10/hour")` + single-flight `SET NX` lock around the multimodal call. SEV1; closes axis F's ❌. ~15 LOC.
4. **`worker/tasks.py:1683`** — drop creator email from the `expire_trials` SELECT + log line. SEV1, PII invariant. 1 line.
5. **`knowledge/chapters.py:209`** — `max_tokens=2000` + drop `description_block` from the model output (Python already rebuilds it). SEV1 for the core long-form input class. ~10 LOC + test.
6. **Upgrade the `anthropic` pin** (retires the clip_engine `ttl` SEV1 risk AND the 0.40-era `type: ignore`s/getattr hazards in dna/improvement/analysis); runtime-confirm titles/thumbnails cache reads and remove inert markers; correct the Sonnet 4.6 floor (2048) in DECISIONS.md:2283. Two SEV1s + several SEV2 hazards in one dependency PR.
7. **`youtube` quota + fail-open pair** — `consume(cost)` per retry attempt; `lock_held` guard so the Redis outage path actually fails open. 2 SEV2s that bite exactly when Google/Redis degrade. ~20 LOC + 2 tests.
8. **`billing` pair** — `max_network_retries=3` into the `StripeClient` constructor (inert global verified); `InsufficientBalanceError` domain exception so a worker-side 402 stops burning 3 full reprocess retries. ~25 LOC.
9. **Worker advisory-lock hygiene** — `PoolEvents.reset` listener emitting `pg_advisory_unlock_all()` on both engines (one fix closes all six leak sites) + set `ingest_status=failed` in the `SoftTimeLimitExceeded` branches. ~25 LOC.
10. **Run Locust at 300 users** (tests/perf/) to close axes A + E with load evidence — the standing gap since Issue 112; reading cannot settle pool pressure or the slowapi-on-loop magnitude.

After 1–9 land and #10 produces clean evidence, the verdict flips to **PRODUCTION-READY: YES** subject to the rotation runbook + per-creator usage quotas (pre-launch checklist items, not code defects).
