# CreatorClip — Production Assessment

**Date:** 2026-06-07  ·  **Commit:** e3c83b2+issue129-uncommitted  ·  **LOC:** ~15,243 (source)  ·  **Tests:** 753 passed / 2 skipped / 126 deselected

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

No net-new BLOCKERs. The subagent re-flagged `ranking.py:102` as BLOCKER but the previous assessment rationale holds — `video_id` UUIDs are per-creator unique, so the missing `creator_id` predicate cannot leak another creator's clips; it stays SEV2. Two net-new SEV1s introduced by Issue 129 worker code (Redis client per-task, dna_profile accessed after session close). Seven carry-forward SEV1s remain open. Gates on shipping: fix worker Redis singleton + session-close access (Issues 129), close the 3rd-cycle routers/insights.py Anthropic cluster, and add the missing CreatorInsight index.

---

## Layer 0 — deterministic gates

| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | 0 issues | 0 | ✅ |
| mypy | 0 errors | 0 | ✅ (fixed this session) |
| coverage | ~75.5% est. | 75.20% | ✅ (recovered with 31 new tests) |
| bandit | high 0 / med 0 | high 0 | ✅ |
| pip-audit | 0 vulns | 0 | ✅ (fixed this session) |

**CI fixes applied this session:**
- `knowledge/__init__.py` created — resolved `Source file found twice: titles / knowledge.titles` mypy error (root cause of CI failure since Issue 128 push)
- `knowledge/thumbnails.py:144` — `# type: ignore[typeddict-item]` for Anthropic SDK 0.40.0 TypedDict gap
- `aiohttp==3.14.1` pinned in `requirements.txt` — closes CVE-2026-34993 + CVE-2026-47265 (voyageai transitive dep)
- `PYSEC-2026-196` added to ignore lists — pip 26.1.x supply-chain CVE, same dev/build-time rationale

**Top uncovered load-bearing paths:**
1. `knowledge/thumbnails.py:112-158` — `analyze_thumbnail_patterns` Claude multimodal call (70%; real API path untested)
2. `routers/thumbnails.py:85-144` — `get_thumbnail_patterns` cache-miss path (73%)
3. `worker/tasks.py` — `_generate_thumbnail_concepts_async` full integration path (default-lane tests only)

---

## Layer 1 — module register (ranked)

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| SEV2* | clip_engine | ranking.py:102 | Missing `creator_id` filter on idempotency query (defense-in-depth gap; UUID uniqueness prevents live leak) | `.where(Clip.video_id == vid, Clip.creator_id == cid)` |
| SEV1 | routers | insights.py:542 | `anthropic.Anthropic()` per-request — 3rd cycle carrying | Module-level `_ANTHROPIC`; `cache_control: ephemeral`; `@limiter.limit("10/hour")`; log tokens |
| SEV1 | worker | tasks.py:2250 | Redis async client created per-task in `_generate_thumbnail_concepts_async` | Module-level singleton mirror of `progress.py::_async_client()` |
| SEV1 | worker | tasks.py:2208 | `dna_profile.top_video_ids_jsonb` accessed after `AdminSessionLocal()` session context exits | Eagerly load inside session context before `async with` closes |
| SEV1 | _root_infra | models.py | `CreatorInsight` missing composite `(creator_id, video_id)` index — 3rd cycle | `sa.Index('ix_creator_insights_creator_video', ...)` + migration 0020 (`CREATE INDEX CONCURRENTLY`) |
| SEV1 | _root_infra | db.py | `recreate_engine()` no re-entry guard — 3rd cycle | `threading.Lock()` + boolean flag around pool disposal |
| SEV1 | ingestion | transcribe.py:78,179 | Singleton init race on Deepgram/AssemblyAI under concurrent `asyncio.to_thread` — 2nd cycle | `threading.Lock()` around both init blocks |
| SEV1 | preference | train.py | Advisory lock acquired after `fit()` completes; concurrent retrains race on version | Move `pg_try_advisory_lock` before `fit()` call |
| SEV1 | youtube | analytics.py:320 | `check_data_gate(session, creator_id)` — `creator_id` parameter untyped | `creator_id: uuid.UUID` |
| SEV2 | worker | tasks.py:2221 | Bare `except Exception` on UUID parse in thumbnail concepts loop — swallows malformed IDs | Catch `(ValueError, TypeError)` explicitly; log malformed IDs at WARNING |
| SEV2 | worker | tasks.py:2302 | `parse_concepts` failure path — verify `ValueError` propagates cleanly to `self.retry()` | Add test asserting `ValueError` reaches Celery retry decorator |
| SEV2 | routers | activity.py:39 | Bare `except Exception` swallows auth errors silently | Catch only `HTTPException`; re-raise others |
| SEV2 | dna | brief.py:156-157 | Two `# type: ignore[arg-type]` on SDK TypedDict gap (Issue 82 carry) | Remove after AsyncAnthropic migration (Issue 82) |
| SEV2 | _root_infra | models.py | `CreatorInsight` full-table-scan covered by SEV1 index finding above | Resolved by SEV1 fix |
| SEV2 | ingestion | transcribe.py | Singleton race partially addressed — verify both backends locked | Confirm AssemblyAI path also guarded |
| SEV2 | preference | train.py | Feature extraction `_build_feature_vector` duplicated with `features.py` | Extract to `preference/features.py` |
| SEV2 | upload_intel | timing.py | Hour 0 / 12 edge case in 12h conversion | `hour % 12 or 12` |
| cleanup | knowledge | thumbnails.py + titles.py | `_extract_transcript_hook` / `_extract_transcript_summary` near-duplicate | Extract to `knowledge/util.py::extract_transcript_text(segs, max_chars)` |
| cleanup | worker | tasks.py | `import json as _json` inside thumbnail task; use top-level import | Remove local shadow |
| cleanup | billing | stripe_client.py:173 | UUID parse unguarded in stripe_webhook — 500 vs idempotent 200 on malformed | `try/except ValueError` around `uuid.UUID(creator_id)`; return 200 |
| cleanup | routers | creators.py:117 | `_identity_to_dict` isoformat boilerplate repeated 3× | Extract to `routers/_schemas.py` |
| cleanup | improvement | brief.py | Unused timeout kwarg | Remove or wire to `_ANTHROPIC.with_options(timeout=...)` |

*\*ranking.py:102 subagent re-flagged as BLOCKER; overridden to SEV2 — UUID uniqueness makes live cross-tenant leak impossible; fix is defense-in-depth only.*

**Module verdicts:** `clip_engine: SEV2` | `dna: NEEDS-WORK` | `routers: NEEDS-WORK` | `worker: NEEDS-WORK` | `youtube: NEEDS-WORK` | `knowledge: NEEDS-WORK (cleanup)` | `_root_infra: NEEDS-WORK` | `billing: clean` | `ingestion: NEEDS-WORK` | `improvement: clean` | `preference: NEEDS-WORK` | `upload_intel: clean`

---

## Layer 2 — scale checklist

| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ✅ | PgBouncer transaction-pooling in staging compose; `prepare_threshold=None`; pool_size=5/max_overflow=10/per pod; math in DEPLOYMENT.md |
| B Async loop hygiene | ⚠️ | All Celery hot-path calls in `asyncio.to_thread`; ingestion singleton init race carries (SEV1); thumbnail Claude call correctly offloaded |
| C Celery idempotency | ✅ | All 11 task bodies have idempotency guards; `acks_late` + `task_reject_on_worker_lost`; ephemeral title/thumbnail tasks need no idempotency guard |
| D Tenant isolation | ⚠️ | Per-creator WHERE on all tested endpoints; RLS on 12 tables; `ranking.py:102` defense-in-depth gap (SEV2); thumbnail/title tasks filter `creator_id` on every DB access |
| E Backpressure | ⚠️ | Timeouts on Anthropic, httpx, boto3, Deepgram; Anthropic 529 retried; Redis fail-open on SSE; 24h pattern cache reduces Claude multimodal calls |
| F Rate limiting | ✅ | slowapi on Redis; per-creator key on all LLM endpoints; thumbnail 10/hr, title 20/hr; `POST /api/activity` still unrated (Issue 122) |
| G Observability | ✅ | JSON structured logs + request_id; Prometheus at /metrics; Celery duration exported; SSE progress in activity panel |
| H Migration safety | ⚠️ | Migrations online-safe (CONCURRENTLY); HNSW on dna_embeddings; `CreatorInsight` missing composite index (SEV1) |
| I Secrets/deletion | ⚠️ | MultiFernet; tokens never logged; deletion endpoint idempotent; `TOKEN_ENCRYPTION_KEY` rotation runbook not yet written (pre-launch gate) |

---

## Diff vs previous report (2026-06-02)

**Fixed (this session):**
- mypy CI failure: `knowledge/__init__.py` missing — root cause discovered and fixed
- pip_audit: `PYSEC-2026-196` (pip) + CVE-2026-34993/47265 (aiohttp) — all closed
- Coverage: 31 new tests added for Issues 128+129; knowledge/thumbnails.py now at 70%+

**Net-new findings (Issues 128+129):**
- SEV1 × 2: worker Redis per-task client + dna_profile post-session access (both in `_generate_thumbnail_concepts_async`)
- SEV2 × 2: worker UUID parse + parse_concepts propagation (same function)
- cleanup × 2: knowledge module transcript-extraction duplication + `import json` shadow

**Carrying from previous (not fixed):**
- SEV1: routers/insights.py Anthropic singleton cluster (3rd cycle — must close now)
- SEV1: CreatorInsight index + recreate_engine guard (3rd cycle)
- SEV1: ingestion transcription singleton init race (2nd cycle)
- SEV1: preference advisory lock placement (first time surfaced)
- SEV2: ranking.py:102 defense-in-depth gap (stable)

**SEV1 trajectory:** 4 → 2 → 1 → 3 → 0 → 1 → 2 → 0 → 1 → 6 → 5 → **9** (+4 net; 2 new from Issue 129, 2 re-surfaced carries)

---

## Top 5 actions, in order

1. **Fix `worker/tasks.py:2250` Redis singleton** (NEW) — replace per-task `_aredis.from_url(...)` with module-level singleton mirroring `progress.py::_async_client()`; ~10 LOC; closes two SEV1s (thumbnail cache read/write path)

2. **Fix `worker/tasks.py:2208` session-close access** (NEW) — eagerly load `top_video_ids_jsonb` inside the `async with db.AdminSessionLocal()` block before exiting; ~3 LOC; prevents DetachedInstanceError on lazy-load

3. **Fix `routers/insights.py:542` Anthropic singleton + caching** (3rd cycle — must close) — module-level `_ANTHROPIC = Anthropic(...)`; `cache_control: ephemeral` on system prompt; `@limiter.limit("10/hour")`; token usage log; ~20 LOC; worsens cost/reliability every cycle it carries

4. **Add `CreatorInsight` composite index + migration 0020** (3rd cycle) — `sa.Index('ix_creator_insights_creator_video', CreatorInsight.creator_id, CreatorInsight.video_id)` + `CREATE INDEX CONCURRENTLY`; bundle recreate_engine re-entry lock; ~15 LOC total

5. **Fix `preference/train.py` advisory lock placement** (SEV1) — move `pg_try_advisory_lock` before `fit()` call; prevents version-assignment race under concurrent retrains; ~5 LOC
