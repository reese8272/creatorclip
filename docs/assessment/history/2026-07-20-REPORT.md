# CreatorClip — Production Assessment

**Date:** 2026-07-20  ·  **Commit:** `ca3305c` (branch `fix/drills-module-shadow`, working tree has only doc/artifact changes)  ·  **Tests:** full suite green @ **81.02%** line coverage (floor 75.2, prior 79.06)  ·  **Method:** Layer 0 deterministic gates + 16 parallel per-module subagents (each re-verified every 2026-07-01 finding against current code, then swept the ~19 days of changes since `f70a857`) + a dedicated triage of the user's four live-prod artifacts (`error.png`, `render loop.png`, `rendered-clip.png`, `autoclip.studio.har`).

## VERDICT: PRODUCTION-READY — **CONDITIONAL** (beta-gate: two live-prod symptoms need one SSH session to confirm/clear)

**1 BLOCKER-class live symptom (cause unconfirmed) · 4 SEV1 · ~37 SEV2 · ~50 cleanup.**

The headline good news: **all six SEV1s from 2026-07-01 are verified FIXED** — Stripe 0-retries (constructor `max_network_retries=3`), frontend root ErrorBoundary, uncounted event-log pool, chat BYPASSRLS (now `tenant_session` + RLS on chat tables, migrations 0040/0026), worker blocking-Resend (Issue 349: `to_thread` + `RESEND_TIMEOUT_S`), and improvement `pause_turn` (Issue 350, both paths, tested). Both Layer-0 gate regressions (ruff 21→0, mypy 2→0) are cleared, and coverage rose to 81.02%. `ingestion` and `notify` came back **clean**.

CONDITIONAL is gated on:
1. **The live sign-in failure** the user captured (`error.png`) — users who cannot sign in is beta-fatal *if persistent*. Code triage narrowed it to exactly two emitting sites in `routers/auth.py:277-292`; top candidates are a double-consumed OAuth code (slow callback + refresh/prefetch), a YouTube-API 403 at the login-time channels fetch, or DB/migration drift. **One log-grep on the VM is decisive** (see live-smoke section).
2. **The stuck "Rendering your clip…" spinner** (`render loop.png`) — a real code defect regardless of trigger: once a worker dies mid-encode the row is stuck `render_status='running'` **forever**, the render endpoint 409s, and the frontend treats 409 as in-progress. No stale-`running` recovery exists anywhere. (SEV1 #4 below.)
3. **Three new SEV1s** (spend-breaker bypass on the most expensive LLM route; API-key RLS false-402s in prod; prod-VM self-hosted runner executing PR code).

---

## Layer 0 — deterministic gates (from `_machine.json`)

| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | **0** | 0 | ✅ (prior run's 21 fixed) |
| mypy | **0** | 0 | ✅ (prior run's 2 production None-safety errors fixed) |
| coverage | **81.02 %** | 75.2 | ✅ (+1.96 vs prior run) |
| bandit | high 0 / med 0 | 0 / 0 | ✅ |
| pip-audit | 8 (venv) | 0 | ⚠️ **local-venv/dev drift only** — pillow 12.2.0 (8 PYSECs) is not in `requirements.txt` and never imported in source; remaining hits are dev-only pip/pytest. `pip-audit -r requirements.txt` → 1 finding, dev-only pytest (PYSEC-2026-1845). CI-authoritative ≈ clean. |
| freshness | 52/52 days | 90-day threshold | ✅ |

**Harness defect found this run (cleanup):** `run_layer0.py` deletes `_coverage.xml` at the end of `gate_coverage`, so the **per-module coverage-floor gate (Issue 269) has never actually run** — it always reports "coverage.xml not found". Fix: run `gate_module_coverage` before the unlink (or unlink after both gates).

---

## Live-smoke triage (user's prod artifacts, 2026-07-20) — full detail in `modules/_live_smoke_triage.md`

| # | Symptom | Severity | Best candidate (code-traced) | Decisive check |
|---|---|---|---|---|
| 1 | "We couldn't complete sign-in with Google" banner | **BLOCKER (needs-runtime-confirmation)** | Only 2 emit sites (`routers/auth.py:277-292`). Ranked: double-consumed auth code (`invalid_grant` — the callback does exchange+userinfo+channels+multi-write DB before redirecting, a wide double-request window) > YouTube-API 403 at login-time channels fetch > DB/migration/RLS failure in `_persist_oauth_grant`. State-cookie/SameSite and redirect-uri mismatch are **ruled out** (they produce raw JSON 400s, not this banner). | `docker compose logs app \| grep -Ei "oauth callback (exchange )?failed"` — the two log formats split HTTP-status vs exception-type causes immediately. |
| 2 | Spinner stuck at "Rendering your clip… (~30s)" | **SEV1 (code defect confirmed)** | Row stuck `render_status='running'`: worker SIGKILL (OOM/deploy) after `running` commits (`worker/tasks.py:1580,1595`) skips every `failed` write; redelivery waits ≥3600s `visibility_timeout` or never comes. Then `POST /clips/{id}/render` 409s (`routers/clips.py:450`) and `ClipPlayer.tsx:49` treats 409 as in-progress → **permanently unrecoverable without manual SQL**. | `SELECT id, render_status, updated_at FROM clips WHERE render_status='running'` — an hours-old row confirms. |
| 3 | Black frame at 0:00 in the player | SEV2 (likely benign) | `<video>` has `autoPlay` without `muted` (Chrome blocks), no `poster`, no `preload`, on a `bg-black` div (`ClipPlayer.tsx:68-76`). The ffmpeg cut path (`-ss` before `-i` + `-accurate_seek` + re-encode) is the correct pattern and can't emit keyframe black. | `ffmpeg -vf blackdetect` on the downloaded mp4. |
| 4 | Google Fonts blocked (HAR status 0 ×3) | SEV2 (CSP) + non-issue | Our CSP (`main.py:285-292`) is `default-src 'self'` with no `style-src`/`font-src`, and `CSP_EXTRA_SOURCES` defaults empty — **the SPA has silently used system-font fallback in prod since Issue 229**. The malformed `display=swap%CC%A6` Open Sans/Poppins URLs are **not in the repo or the bundle** — browser-extension-injected, non-issue. | `curl -sI https://autoclip.studio/app/ \| grep -i content-security-policy`. |

---

## Layer 1 — module register (ranked)

**Tally across 16 units: 0 code BLOCKER · 4 SEV1 · ~37 SEV2 · ~50 cleanup.** Module verdicts: `ingestion`, `notify` **clean**; `dna`, `analysis`/`upload_intel` near-clean; the rest NEEDS-WORK; none has a cross-tenant leak.

### SEV1 (4)
| # | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| 1 | routers | `routers/clips.py:221-227` | `POST /videos/{id}/clips/generate` — the **most expensive LLM route** (in-request per-candidate scoring) — has **neither `require_flag("llm_generation")` nor `require_budget`**, while every sibling LLM surface got both this cycle. The Issue-290 global spend breaker (which trips by flipping `llm_generation` off) and the per-creator cool-down **cannot stop the main burn path**; only balance check + 10/h rate limit bound it. | Add `dependencies=[Depends(require_flag("llm_generation")), Depends(require_budget)]` exactly like the three sibling clip LLM routes; test asserting 503 with the flag off. |
| 2 | _root_infra | `api_key.py:105-136` | API-key auth path missed the Issue-344 GUC fix, and Issue-352's 5-min `last_used_at` stamp throttle removed the commit that masked it: on every no-stamp request the transaction begins **before** `session.info["creator_id"]` is set → no `app.creator_id` GUC → under enforced prod RLS all tenant reads deny → `check_positive_balance` sees zero minute-packs and **falsely 402s funded creators** on `/clips/ingest` (works once per 5-min window, then 402s). Fails closed — correctness, not leak. Dev/unit tests can't catch it (single-role bypass). | Mirror `auth.py:157`: after setting `session.info`, execute `SELECT set_config('app.creator_id', :cid, true)`; add an `-m integration` test calling the dependency twice inside the stamp window. |
| 3 | deploy_ci | `scripts/setup-runner.sh:58,96` + `.github/workflows/ci.yml:33` | The self-hosted runner lives **on the prod VM**, is in the `docker` group (root-equivalent), owns `/opt/autoclip/.env` (all prod secrets) — and `ci.yml` triggers on `pull_request`. Any malicious transitive dep executing during a PR install step can read every secret and control prod containers. Bounded today (private solo repo) but a full-prod-compromise supply-chain path. | Restrict the prod-VM runner to deploy-track workflows; move PR CI to GitHub-hosted or a second runner off the VM (already the documented second-runner TODO); at minimum a runner user without docker-group or `.env` read. |
| 4 | worker + routers + frontend (live-smoke #2) | `worker/tasks.py:1580-1661`, `routers/clips.py:450`, `ClipPlayer.tsx:49` | **No stale-`running` recovery for renders** — worker death after `running` commits leaves the clip spinning forever; render endpoint 409s; frontend treats 409 as in-progress; only manual SQL unsticks it. This is the mechanism behind the user's `render loop.png`. | Beat sweep flipping `running` rows older than `CELERY_TIME_LIMIT + margin` to `failed` (idempotent, admin session), **or** allow re-render at `clips.py:450` when `updated_at` exceeds the hard limit. Secondarily (Issue 353 interaction): enqueue **before** nulling `render_uri` at `clips.py:484-496` so a broker failure can't destroy a watchable clip. |

### SEV2 — lead finding per module (full lists in `docs/assessment/modules/<module>.md`)
| Module | SEV2s | Lead finding |
|---|---|---|
| deploy_ci | 7 | `docker-compose.staging.yml:74,103` — staging loads the **prod `.env`**: DB/Redis are overridden (isolation real) but prod R2 creds (staging renders write into the prod media bucket), live Stripe key, prod Sentry/OTel, prod `TOKEN_ENCRYPTION_KEY` all bleed. Fix: explicit `environment:` overrides or `/opt/autoclip/.env.staging`. Also: `deploy.sh` `SendEnv=GHCR_TOKEN` almost certainly not accepted by default sshd (manual-fallback deploy broken); `rotate_token_key.py` puts both Fernet keys on argv (visible in `ps`/shell history); `backup_redis.sh` `source`s the prod `.env`; `activate-rls.yml` sanity step gaps. |
| routers | 5 | `/clips/generate` still runs the 30–120s LLM scoring pass **in-request** (carry-forward; every sibling is 202+Celery+SSE); `create_summary` check-then-insert race with no unique backstop → double ffmpeg renders; retrain enqueue after commit. |
| preference | 5 | `model.py:46-65` (carry-forward) LightGBM allowlist has no serialization round-trip test — dep/allowlist drift silently disables personalization for all mature creators via `load_latest`'s broad fallback; `efficacy.py` unbounded `load_labeled_clips`, CPU-bound `fit` on the loop. |
| worker | 4 | `tasks.py:5106-5118` — `send_notification` commits the dedupe row as `sent` **before** the send and swallows failures → a Resend blip permanently loses the email (no retry can pass the dedupe guard). Fix: status-aware dedupe + re-raise after marking failed. |
| clip_engine | 2 | `ranking.py:187-229` (carry-forward, now shared with worker) `persist_ranked_clips` check-then-insert with no unique constraint on `clips(video_id, rank)` → concurrent generation can double-insert the full clip set. Fix: unique index + `IntegrityError`→reload. |
| youtube | 2 | `publish.py:126-162` — residual duplicate-upload window: a raw httpx error escaping `_query_offset` triggers a full task retry that opens a **new** upload session. (All three prior SEV2s — insert-quota bucket, completed-upload misreport, scope stripping — verified FIXED.) |
| knowledge | 2 | `titles.py:239` + `hooks.py:243` — the two web-search **streaming** builders have no `pause_turn` continuation (thumbnails/improvement/chat all handle it) → paused turn yields partial/empty JSON and a failed task. |
| frontend | 2 | `Recap.tsx:51-55,92-96` — comment claims a poll for in-flight recap renders that **doesn't exist** → Redis-blip/stale state latches "Recap rendering…" until manual reload; plus remaining raw-fetch mutations without error affordances (Dashboard/Review). |
| _root_infra | 2 | (carry-forward) `TOKEN_ENCRYPTION_KEY` format not validated at boot (first-decrypt 500 instead of fail-fast); slowapi sync-Redis residual accepted-for-beta (`limits==5.8.0` now pinned — prior unpinned-dep SEV2 fixed). |
| billing | 1 | `spend_guard.py:328-337` — global trip-latch SETNX **before** `_flip_llm_flag`; a failed flip leaves the latch blocking retries ~1h while the global cap breach goes unenforced. (Stripe-retries SEV1 + idempotency-key scoping both verified FIXED.) |
| improvement | 1 | `brief.py:209-248` — `.create()` pause_turn loop bills only the **final round's** usage (streaming path accumulates correctly) → multi-round searches under-bill on that path. |
| chat | 1 | `runner.py:180-186` (carry-forward) — billing cost hardcoded to Sonnet rates + "sonnet-tier" label while `ANTHROPIC_MODEL_CHAT` is configurable. |
| dna | 1 | `builder.py:87-96` — `_optimal_upload_gap_h` near-duplicates `upload_intel.optimal_gap_hours` but lacks its Batch-J circular-week wrap + malformed-row guard → skews persisted upload rhythm. Fix: delegate. |
| ingestion / notify | 0 | Clean (all prior SEV2s verified FIXED — AssemblyAI error→refund chain, waveform filter, silence gate; Resend timeout, EMAIL_FROM fail-fast, Issue-246 sunset cap correct). |

---

## Layer 2 — scale checklist

| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ✅ | Prior SEV1 event-log pool fixed and budgeted; no fresh load test this commit (beta-scale OK; re-run Locust before any scale push). |
| B Async loop hygiene | ⚠️ | Resend now `to_thread`+timeout (fixed); slowapi sync-Redis residual bounded ~100ms worst-case, accepted for ≤100-user beta; `preference/efficacy.py` CPU-bound `fit` on the loop (SEV2). |
| C Celery idempotency | ⚠️ | Money/media tasks idempotent on stable keys; open: `persist_ranked_clips` + `create_summary` check-then-insert races (unique-index fixes specified); **no stale-`running` recovery (SEV1 #4)**; visibility-timeout coupling still comment-only. |
| D Tenant isolation | ✅ (1 ⚠️) | Chat BYPASSRLS SEV1 fixed (tenant_session + RLS, tested); RLS policies NULLIF-hardened (0045); **new api_key GUC gap fails closed** — a 402-correctness bug, not a leak (SEV1 #2). No cross-tenant leak anywhere. |
| E Backpressure | ⚠️ | Stripe retries fixed; Resend timeout fixed; open: youtube duplicate-upload retry window; no timeout on the render-path R2 download; login-time channels fetch shares the read quota (live-smoke candidate #2). |
| F Rate limit / quota | ⚠️ | Stacked limits + async storage + brief quota shipped; **but the main burn path bypasses the kill switch and budget guard entirely (SEV1 #1)** — the breaker is decorative until that lands. |
| G Observability | ⚠️ | Chat cost-model hardcode; improvement `.create` usage under-count; notify `provider_message_id` never populated; live-smoke shows good log lines exist but **nobody has tailed the VM logs** — symptoms 1–2 are one SSH away from root cause. |
| H Migration & pgvector | ✅ | 0040–0045 landed incl. NULLIF-hardened RLS; migration-lint now does downgrade round-trip + irreversibility detection (Issue 296); pgvector indexes intact. |
| I Secrets / deletion | ⚠️ | decrypt()/rotation runbook solid; open cluster is **deploy-side**: prod-VM runner (SEV1 #3), staging env bleed, keys on argv, `backup_redis.sh` sourcing prod .env. |

---

## Diff vs previous report (2026-07-01, `f70a857`)

### Fixed (verified in code this run)
- ✅ **All 6 prior SEV1s**: billing Stripe 0-retries; frontend root ErrorBoundary; _root_infra event-log pool; chat BYPASSRLS→tenant_session; worker blocking-Resend (Issue 349); improvement pause_turn (Issue 350).
- ✅ Layer-0 regressions: ruff 21→0, mypy 2→0 (incl. both production None-safety bugs); coverage 79.06→81.02.
- ✅ Prior SEV2s confirmed fixed: youtube ×3 (quota bucket, upload misreport, scope stripping); ingestion ×3 (AssemblyAI silent-empty now raises→refund, waveform, silence gate); knowledge ×2 (untrusted-in-system-role, inert cache marker); upload_intel ×2 (top-N filter order, week wraparound); billing idempotency-key scoping; dna Voyage retry predicate; _root_infra ENV Literal + JWT min-length + `limits==5.8.0` pin; frontend stuck-busy fetches; routers activity-splat.
- ✅ **Off-course log 2026-07-02 rollback no-op is FIXED at HEAD** (`${IMAGE_TAG:-latest}` + `:rollback` re-tag) — the OFF_COURSE_BUGS.md entry was stale-open (now updated).

### New this run
- 🔴 **BLOCKER-class live symptom** — prod Google sign-in failure (cause needs one log check; three ranked candidates).
- 🔴 **SEV1 routers** — `/clips/generate` bypasses kill switch + budget guard (spend breaker ineffective on the main burn path).
- 🔴 **SEV1 _root_infra** — API-key path missing GUC ⇒ prod-RLS false-402s within the 5-min stamp window (Issue 344 fix never reached `api_key.py`; Issue 352 unmasked it).
- 🔴 **SEV1 deploy_ci** — prod-VM self-hosted runner executes `pull_request` code with docker-group + prod `.env` access.
- 🔴 **SEV1 (live)** — stuck-`running` render with no recovery path (the `render loop.png` mechanism).
- 🟡 New SEV2 cluster in **deploy_ci** (staging prod-env bleed, deploy.sh SendEnv, keys-on-argv, backup sourcing .env) — first assessment of this surface.
- 🟡 worker `send_notification` commits-then-swallows (email loss); knowledge streaming pause_turn gap (titles/hooks); improvement usage under-count; Recap phantom poll; CSP blocks Google Fonts in prod.

### Carry-forward (still open)
- `/clips/generate` in-request LLM scoring; `persist_ranked_clips` race; preference LightGBM round-trip test; chat cost hardcode; TOKEN_ENCRYPTION_KEY boot validation; slowapi sync residual (accepted-for-beta).

---

## Top actions, in order

1. **SSH the VM once and run the four decisive checks** (`modules/_live_smoke_triage.md` §Cross-cutting) — the sign-in BLOCKER and the stuck-render trigger are both falsifiable from logs in minutes: `grep "OAuth callback"` on app logs; `SELECT … WHERE render_status='running'`; `docker compose ps`; `curl -sI …/app/ | grep -i content-security-policy`.
2. **Gate `/clips/generate`** with `require_flag("llm_generation")` + `require_budget` (SEV1 #1) — two lines + a test; the spend breaker is decorative without it.
3. **Port the Issue-344 GUC fix to `api_key.py`** (SEV1 #2) — one `set_config` call + an integration test; prod API-key users are intermittently 402ing right now if any exist.
4. **Add stale-`running` render recovery** (SEV1 #4) — Beat sweep or 409-override; also reorder Issue 353's null-then-enqueue.
5. **Move PR CI off the prod-VM runner** (SEV1 #3) — GitHub-hosted for `pull_request`, VM runner for deploy-track only.
6. **Set `CSP_EXTRA_SOURCES` on the VM** (or self-host the fonts) — prod has been on system-font fallback since Issue 229.
7. **Stop staging from bleeding prod env** — explicit overrides in `docker-compose.staging.yml` (R2 bucket, Stripe test key, empty Sentry/OTel).
8. **Fix the send_notification dedupe-before-send** (worker SEV2) and the two check-then-insert races (unique indexes specified in the module files).
9. File the SEV2 tail into `docs/issues.md` per the Wave×Lane×Batch process.

*History snapshot: `docs/assessment/history/2026-07-20-REPORT.md`.*
