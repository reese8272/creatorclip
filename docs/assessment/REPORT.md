# CreatorClip — Production Assessment

**Date:** 2026-05-30 (post Issue 86 — live SSE progress streaming)  ·  **Commit:** `bbfa3c8`  ·  **LOC:** ~11.4k Python (non-test, +3.6k since prior; new SSE/progress + identity + RLS + observability surface)  ·  **Tests:** 492 passed, 1 skipped, 85 deselected (default); +integration lane green on CI

## VERDICT: PRODUCTION-READY — **CONDITIONAL**

No open BLOCKER and no cross-tenant leak in any module. The entire prior SEV1 set is closed and verified: the `build_dna` concurrent double-pay (Issue 76 — `pg_advisory_xact_lock` + partial UNIQUE), the ingestion whole-file buffering and missing SDK transcription timeout (Issue 76 — streaming upload + `httpx.Timeout`), and the 18-endpoint `response_model` gap (now 18/18 closed; the new SSE endpoint is the sole, intentional exemption). Per-creator isolation is now **structural** at the DB layer via RLS migration `0010_rls_policies` rather than vigilant — promotes scale-axis D from ⚠️ to ✅. What keeps it CONDITIONAL rather than YES: two new SEV1s (one in the Issue-86 SSE concurrent-cap, one latent in `billing/refund.py` that will silently no-op once the prod RLS role split flips), 27 SEV2s, and scale axes A/C/E/F still lack the Locust-behind-PgBouncer **load evidence** that only a real run can produce.

---

## Layer 0 — deterministic gates (from `_machine.json`; Python 3.12 + Redis)
| Gate | Result | Baseline | Status |
|---|---|---|---|
| ruff | 0 issues | 0 | ✅ |
| mypy | 0 errors | 0 | ✅ (CI's pinned mypy `1.14.1` agrees after the `_AIO` narrowing fix at `worker/progress.py:147`) |
| coverage | unchanged from 70.59% (last full CI run; skipped in this local Layer 0 — `coverage.xml` absent locally) | 69.54% floor | ✅ no regression |
| bandit (SAST) | 0 high / 0 medium | 0 / 0 | ✅ (skipped locally — bandit not installed in `.venv`; CI `quality.yml` ran it green on this commit) |
| pip-audit | 0 vulns | 0 | ✅ (same — CI verified) |
| freshness | both skills 0d | <90d | ✅ |

All five lanes on this commit green on CI: **CI · Integration tests · Quality Gates · Docker publish · Deploy to production**. Coverage stayed at the 70.59% floor — Issue 86 added 24 new unit tests + 1 subprocess integration test that exactly cover the new surface (`tests/test_progress.py`, `tests/test_anthropic_stream.py`, `tests/test_tasks_sse.py`, `tests/test_worker_imports_integration.py`).

Top untested load-bearing code: nothing new flagged this cycle — every Issue-86 path landed with tests. Prior `clip_engine/scoring.py` boundary asserts and Anthropic prompt-cache placement remain unit-tested.

## Layer 1 — module register (ranked)

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| SEV1 | worker | `worker/progress.py:214-232` | `aacquire_slot` EXPIRE is only set on the INCR→1 transition; a creator holding N>0 SSE streams continuously past `_STREAM_TTL_SECONDS=3600s` has the counter TTL elapse → reset to 0 → live `arelease_slot` calls DECR into negatives → the per-creator concurrent-SSE cap is **silently bypassed** until the next missing-key INCR resets it. An authed creator can hold-open many more than 3 streams in a steady-state attack. | EXPIRE on every INCR (not just count==1) so the TTL is refreshed while the key is in use; OR replace the counter with a sorted-set of per-stream entries (`ZADD task:{cid}:streams <expire_ts> <stream_uuid>` + `ZREMRANGEBYSCORE` on each acquire) which has no negative state — the industry-standard concurrency-limit pattern. |
| SEV1 | billing | `billing/refund.py:41` | `refund_for_video` opens `db.AsyncSessionLocal()` (the **app-role** factory, RLS-gated since Issue 79) and never sets `session.info["creator_id"]`, so the `after_begin` listener at `db.py:119-148` does NOT emit `SET LOCAL app.creator_id`. Under prod RLS (`creatorclip_app` without BYPASSRLS, policy `creator_id = current_setting('app.creator_id', true)::uuid`), the `MinuteDeduction` SELECT returns no rows → every terminal-failure refund silently no-ops, and the existing integration test runs as the single-role dev user (BYPASSRLS) so it's blind. *(needs-runtime-confirmation: severity depends on whether the prod operator has flipped the role split; CLAUDE.md / DEPLOYMENT.md lists this as pending — so the fix must land before that switch.)* | Switch to `db.AdminSessionLocal()` to match the rest of the worker surface (refund is a system action, not a per-creator request); add a regression test running as the `creatorclip_app` role with RLS enforced asserting a refund actually credits. |
| SEV2 | worker | `worker/progress.py:235-240` | `arelease_slot` calls `DECR` with no guard against the counter being absent (TTL expired) or already 0; counter goes negative and stays negative until the next missing-key INCR resets it. | Clamp at 0 via a tiny Lua script (`if redis.call('DECR', KEYS[1]) < 0 then redis.call('SET', KEYS[1], 0) end`), or collapse with the SEV1 fix above into a sorted-set scheme. |
| SEV2 | worker | `worker/progress.py:130-164` | `_async_client()` rebuilds the singleton on loop-mismatch but does NOT close the old client first; in pytest's function-scoped loops every test abandons an aredis client with its connection pool half-bound to a dead loop. Sockets may leak to process exit; logs fill with `Event loop is closed` GC noise. | In the rebuild branch, schedule `await old.aclose()` (cleanest: expose an `await arebind()` helper that the test fixture / `aclose()` calls). Mark `(needs-runtime-confirmation)` on the socket-leak severity — an `lsof` would settle it. |
| SEV2 | worker | `worker/progress.py:149-164` | `aemit` correctly swallows all exceptions but on every failure nulls out `_AIO`/`_AIO_LOOP` globals without locking; under concurrent emits + a Redis brown-out, every concurrent failure rebuilds the pool, worsening the very condition the swallow was protecting against. | Reset only on `ConnectionError` / `TimeoutError` / `redis.exceptions.RedisError`; pin with an `asyncio.Lock` if multi-loop concerns ever surface. |
| SEV2 | worker | `worker/progress.py:188-208` | `aread_since` holds **one Redis connection for the full `block=block_ms`** XREAD; redis-py async pool default is `max_connections=50`. With ~100 concurrent SSE consumers (well inside Issue 86's stated 200-creator scale) the pool exhausts and new emits/reads queue or fail — the progress stream becomes a hard cap on web concurrency. *(needs-runtime-confirmation under load.)* | Bound the async client's pool explicitly (`aredis.from_url(..., max_connections=N)`) with `N >= MAX_CONCURRENT_SSE_PER_CREATOR × expected_concurrent_creators + headroom`; document the math in DEPLOYMENT.md alongside the PgBouncer math; use a separate redis client for SSE reads vs writes so a wedged reader doesn't starve emits. |
| SEV2 | worker | `worker/progress.py:74-85` + `worker/tasks.py:643` | `_serialize` passes every emitter field through `json.dumps(..., default=str)` with no schema/allowlist; `_emit("error", message=str(exc))` for `ValueError` raw-passes the exception arg into the SSE stream. Today's call sites are safe (data-gate messages like `"Creator … not found"`); the SEV2 is the unverified trust boundary for the next contributor. | Add per-event-type allowlists (e.g. `EventPayload(BaseModel)`) at the emit boundary; sanitize `ValueError` messages against an allowlist (`"… not found"`, `"… is empty"`, …) and fall back to a generic `"validation failed"` for anything else. |
| SEV2 | worker | `worker/anthropic_stream.py:57-75` | If the network drops mid-stream, the Anthropic SDK raises inside the `for event in stream:` loop and propagates up to `_build_dna_async`'s outer try/except — but mid-stream tokens already delivered to the UI are followed by an undefined gap before the caller's terminal `error` emit fires (must unwind `to_thread` → outer try/except → `aemit`). On a slow brown-out the SSE consumer's keepalive must hold for >100ms. | Wrap the streaming call inside `stream_and_emit` with `try/except: sync_emit(task_id, "error", message="stream interrupted"); raise` so the terminal event lands on the SAME Redis call path as the deltas (lower-latency than the multi-layer async unwind). Caller's emit still fires as backup. |
| SEV2 | worker | `worker/anthropic_stream.py:72-84` | `usage_dict` casts every field via `getattr(usage, ..., 0)` — safe today, but `0` as a default silently hides a schema change in a future SDK bump (Issue 84) where a field is renamed/moved. The "cache_read=0" log line would under-report savings without raising. | Distinguish absent from zero — return `None` when the attribute is missing; log a one-time warning the first time a previously-present field returns None so the SDK-bump regression is loud. |
| SEV2 | worker | `worker/tasks.py:547-556` | `_poll_clip_outcomes_async` does NOT break on YouTube quota exhaustion; per-outcome failures are swallowed by `except Exception: continue`, so a quota-out creator walks the whole candidate set firing doomed calls (COMPLIANCE §4). Bounded by the 10-day cap, but wasteful. **Carry-forward from prior cycle.** | Catch `QuotaExhaustedError` explicitly inside the inner loop and `break` (mirror analytics-refresh at `tasks.py:906-912`); commit partial progress first. |
| SEV2 | worker | `worker/tasks.py:416-474` | `_render_clip_async` idempotency guard protects sequential redelivery-after-success but NOT concurrent: two workers can both read `pending`, both flip to `running`, both encode + upload to the same R2 key. Wasted double-encode, not corruption. **Carry-forward.** | `select(Clip).where(...).with_for_update()` + re-check `render_status` under lock before flipping to `running`. |
| SEV2 | worker | `worker/tasks.py:278-317` | `_ingest_async` not a clean no-op on redelivery: re-extracts/re-uploads the already-derived WAV. No corruption (billing idempotent via UNIQUE(video_id)), but re-does ffmpeg work. **Carry-forward.** | Short-circuit when `source_uri` already points at the derived `audio/*.wav` key, or gate on `ingest_status==done`. |
| SEV2 | routers | `routers/tasks.py:131-138` | Ownership check is an enumeration oracle: 404 (`owner is None`) vs 403 (`owner != creator.id`) lets an authed client distinguish "task never existed" from "exists but belongs to another creator". The in-code comment at :133-135 says "Either way: don't leak whether the task ever existed" — but the implementation contradicts the comment. | Return `404 "Unknown task"` for BOTH branches; log the distinction server-side for debugging if needed. Add a regression test asserting creator B sees 404 (not 403) on creator A's task_id. |
| SEV2 | routers | `routers/tasks.py:140` + `worker/progress.py:204` | Client-supplied `Last-Event-ID` header is forwarded raw into `XREAD` with no validation; any non-`<ms>-<seq>` value triggers `redis.exceptions.ResponseError`, which `aread_since` doesn't catch → mid-stream 500 + noisy log on every malformed reconnect. | At the top of `_event_stream` after acquiring the slot, validate `last_event_id` matches `^\d+-\d+$` (or empty); on mismatch reset `cursor = "0-0"`. Optionally bound the header length (≤64 chars) before parsing. |
| SEV2 | routers | `routers/tasks.py:77` + `worker/progress.py:214-232` | `sse:count:{creator_id}` recovery window is up to 1 hour on abnormal process exit — if the API replica is `SIGKILL`'d / OOM-killed mid-stream, the `finally:` never runs and the counter stays inflated for the 3600s TTL. A creator at the cap when the pod crashes is locked out for up to an hour. | Lower the TTL on `sse:count:{creator_id}` to ~120-300s (an SSE stream silent for that long is dead anyway, and active streams re-INCR + re-set TTL on every reconnect — self-healing). |
| SEV2 | routers | `videos.py:65-66`, `clips.py:118-123`, `upload_intel.py:37-40` | Unbounded `list(result.scalars())` with no LIMIT/pagination on `GET /videos`, `GET /videos/{id}/clips`, audience-activity read → a creator with thousands of rows loads the whole set into memory + one response. **Carry-forward.** | Keyset or offset pagination (`?limit=&before=`) with a hard cap (100). |
| SEV2 | clip_engine | `clip_engine/ranking.py:139` | `dna_match=c.get("score")` is seeded to the composite score and never refined; the preference feature vector receives a duplicate of its own label-generating signal as an "independent" DNA-fit feature (collinear). **Carry-forward.** | Have `score_candidates` return the DNA-only fit (Claude `score` before signal blend); persist that to `dna_match`. Add a unit test asserting `dna_match ≠ composite`. |
| SEV2 | clip_engine | `clip_engine/candidates.py:113` | Candidate windows are never deduped/merged; two adjacent peaks' `setup_start_s` can pull near-identical windows (cuts against principle #9 "One idea per Short"). **Carry-forward.** | After the chronological sort, drop any candidate whose `[setup_start_s, end_s]` overlaps the previous kept candidate by >50% IoU (keep higher-prominence one); add an eval scenario with two close peaks. |
| SEV2 | dna | `dna/brief.py:130-151` | Streaming brief path does ~2k synchronous Redis XADDs per build on a worker threadpool slot. Bounded latency (<1ms per round-trip on LAN, not a loop stall thanks to `asyncio.to_thread`) but consumes one threadpool slot per active streaming build. *(needs-runtime-confirmation under target concurrency.)* | Acceptable for v1 (DNA builds are low-frequency). If `build_dna` queue depth grows under load, batch deltas (emit every K tokens or every K ms) to cut Redis round-trips ~10x. |
| SEV2 | dna | `dna/brief.py:153-157` | Asymmetric `# type: ignore[arg-type]` on `.create()` call but NOT on `stream_and_emit` call. SDK signatures of `.create` and `.stream` are not identical in all versions — adding the ignore to one but not the other will fail mypy the day the SDK is bumped (Issue 84). | Either add the same ignore to the streaming call site, or — better — narrow `_build_request`'s return type to the real SDK TypedDicts (`list[TextBlockParam]`, `list[MessageParam]`) once Issue 84 bumps anthropic past 0.40. |
| SEV2 | dna | `dna/brief.py:134` | `from worker.anthropic_stream import stream_and_emit` is function-local inside the `if task_id is not None:` branch — defensive against an import cycle (`worker.tasks` imports `dna.brief`), but it places `worker.*` as a runtime dependency of `dna.brief` (layering smell). | Accept as lesser evil for v1, but add a one-line comment explaining WHY the import is function-local so a future cleanup doesn't "fix" it back to a top-level import that breaks worker startup. |
| SEV2 | preference | `preference/model.py:46` | LightGBM branch of `_ALLOWED_CLASSES` is hand-derived and coupled to exact internal module paths (`lightgbm.sklearn`, `lightgbm.basic`, `numpy._core.multiarray.scalar`). Any library upgrade that relocates one of these silently makes every legitimate `from_bytes` raise `UnpicklingError`, which `load_latest:159-163` swallows into a silent DNA fallback. Existing round-trip tests only exercise the LogisticRegression branch. | Add `tests/test_preference.py::test_scorer_round_trips_lightgbm` that forces the LGBM branch (call `fit` with `threshold=1`) and asserts `from_bytes(scorer.to_bytes()).predict_score` matches the original. Catches a future module-path move at build time, not in production. |
| SEV2 | preference | `preference/_scorer_cache.py:23` | In-process LRU bound is entry-count (default `PREFERENCE_SCORER_CACHE_SIZE=128`), not bytes; a LightGBM booster with 100 trees + sklearn pipeline can be a few MB → 128 entries × N workers × N replicas → hundreds of MB resident per pod. Bounded today by `prefetch_multiplier=1`, but a future tuning that raises concurrency silently inflates footprint. | Lower the default to e.g. 32 to match expected concurrent-creator working set per worker, OR gate the cap on bytes using `sys.getsizeof` estimates, OR explicitly document the per-entry-size × concurrency × replicas pod-memory math in the docstring. |
| SEV2 | youtube | `youtube/oauth.py:285-326` | `get_valid_access_token` has no Redis-down degradation: `get_redis_client().set(...)` raises `redis.RedisError` → unhandled 500 on every analytics fetch in the Beat task. The module's graceful-degrade posture (quota → "try tomorrow", auth-error → drop row) is defeated by a Redis blip. *(needs-runtime-confirmation under real Redis failover.)* | Wrap the lock acquisition in `try / except redis.RedisError`; fall back to lockless refresh (Google tolerates double-refresh; DB write is idempotent on creator_id) OR raise `HTTPException(503, ...)`. Add test asserting a `RedisError` from `set()` does not surface as 500. |
| SEV2 | youtube | `docs/COMPLIANCE.md:21,47-50,111` | Analytics retention/refresh **cadence still TBD** (Issue 75b still open); refresh exists but no max-staleness purge → dead-grant metrics persist indefinitely. Largest remaining compliance gap. **Carry-forward.** | Confirm Google's required staleness window, record it in COMPLIANCE.md, add a `YOUTUBE_ANALYTICS_MAX_STALENESS_DAYS` setting, add a daily Beat sweep deleting rows whose `fetched_at` exceeds the window. |
| SEV2 | ingestion | `ingestion/transcribe.py:116-123,137-138` | Deepgram normalizer still uses hard-key indexing (`u["start"]`, `u["end"]`, `u["transcript"]`); a missing timestamp raises `KeyError` and burns a Celery retry instead of skipping the partial item. WhisperX + AssemblyAI normalizers are already safe. **Carry-forward (Deepgram half not addressed by Issue 76).** | Switch the two list comprehensions in `_normalize_deepgram` to `.get("start")`/`.get("end")` and skip any utterance/word where either is `None`. |
| SEV2 | ingestion | `ingestion/transcribe.py:43-60` | `_guard_audio_size` swallows `OSError` and silently returns when the file is missing/unreadable; downstream SDKs then either error cryptically (whisperx) or upload 0 bytes and succeed with an empty transcript (AssemblyAI) — both burn the per-job budget on a guaranteed-empty pipeline run. | In the `except OSError` branch, raise `FileNotFoundError(f"audio not found: {audio_path}")` so the caller's retry/refund pathway sees a clear terminal error. |
| SEV2 | billing | `billing/refund.py:50-56` | Refund idempotency guard is a read-then-write (`SELECT MinutePack.id WHERE pack_id = 'refund:<vid>'` → INSERT if None); `MinutePack.pack_id` has NO UNIQUE constraint, so two concurrent `refund_for_video` calls for the same video can both pass the SELECT and both insert → double-refund. The docstring claims the race is "not reachable" but a manual-recovery rerun racing with the automatic `on_failure` is reachable. | `CREATE UNIQUE INDEX CONCURRENTLY uq_minute_packs_refund_pack_id ON minute_packs (pack_id) WHERE reason = 'refund'`; wrap `grant_minutes` call in `try/except IntegrityError: return 0`. Add concurrent-refund integration test. |
| SEV2 | upload_intel | `upload_intel/timing.py:54-55` | `optimal_gap_hours` still did NOT get the bounds/coercion guard `best_upload_windows` got in Issue 75d — sorts on raw `r.activity_index`, computes `r.day_of_week * 24 + r.hour` on unvalidated attributes. Two functions on the SAME payload disagree on what a valid row is. **Carry-forward (not addressed this cycle).** | Filter+coerce rows first (mirror the windows guard); return None if <2 valid rows. |
| SEV2 | _root_infra | `observability.py:189-211` | Celery `_bind_request_id` / `_record_task_and_clear` use a single module-level ContextVar — correct ONLY under prefork. A future `--pool=gevent/threads` migration silently breaks log correlation. **Carry-forward.** | Assert `app.conf.worker_pool == "prefork"` at worker startup; or key task start off `task.request.id`. |
| SEV2 | _root_infra | `Dockerfile:1-34` | Image runs as root — every process (uvicorn, celery, ffmpeg subprocesses) has UID 0 + write access to the entire app tree. An RCE via a media-processing dependency would have full container privilege. Defense-in-depth gap even with k8s `securityContext: runAsNonRoot`. **NEW (surfaced this cycle).** | Add `RUN useradd --create-home --uid 1000 app && chown -R app:app /app /root/.local` then `USER app` after `COPY . .`. Verify Celery + ffmpeg still write their state/temp dirs as `app`. |
| SEV2 | _root_infra | `Dockerfile:34` | Default `CMD` ships `uvicorn … --reload` — a forgotten prod override starts uvicorn in dev mode (filesystem watcher, single worker, no graceful shutdown). **NEW (surfaced this cycle).** | Default to `gunicorn -k uvicorn.workers.UvicornWorker -w 4 main:app --bind 0.0.0.0:8000`; move `--reload` to `docker-compose.yml` where dev belongs. |

Plus **28 cleanup** items (typing gaps the mypy ratchet will catch, DRY extractions, docstring contracts on the new emit wire-shape, render keyframe timeout scoped tighter than the full encode budget, lifespan-coupling registry, magic constants in youtube/analytics.py and dna/conflict.py niche coverage gap, etc.) — full per-finding detail with `file:line` in `docs/assessment/modules/*.md`.

Module verdicts: **dna = clean · improvement = clean · 9 = NEEDS-WORK · 0 = BLOCKER · 0 = cross-tenant leak**.

## Layer 2 — scale checklist (`scale-checklist.md`)
| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | psycopg3 `prepare_threshold=None` + pool 15+5 + admin pool 5+10 + `pool_pre_ping` + `pool_recycle=1800` all verified in `db.py:33,47-50,58-62`; per-pod ceiling 35 vs the documented 25-conn PgBouncer sidecar — verify the sidecar budget covers BOTH engines. NEW: `worker/progress.py:188-208` async Redis pool default 50 is now in the request-concurrency critical path (SEV2). **Locust-behind-PgBouncer evidence still pending.** *(unchanged from prior — A remains the largest "needs load evidence" gap)* |
| B Async loop hygiene | ✅ | clip_engine SEV2 from prior pass RESOLVED (`extract_candidates` + feature computation now in `asyncio.to_thread` — `ranking.py:115`, `scoring.py:161`). All sync LLM/upload/transcription/Voyage off the loop. Residual: Stripe SDK is sync-only by upstream design — documented constraint, not a new flag. *(was ⚠️)* |
| C Celery idempotency | ⚠️ | Issue 76 advisory lock + partial UNIQUE on `creator_dna.build_job_id` closes the prior `build_dna` concurrent double-pay (SEV1 → resolved). Residual: SSE concurrent-cap TTL drift (SEV1, worker), render double-encode (SEV2), ingest re-do (SEV2), `poll_clip_outcomes` no quota break (SEV2). Direction is right; ⚠️ stays because a new SEV1 replaced an old one. |
| D Tenant isolation | ✅ | RLS migration `0010_rls_policies` deployed; `db.py:119-148` listener emits `set_config('app.creator_id', :cid, true)` on every transaction whose session has `creator_id` set. The database now refuses cross-tenant rows even if application code forgets the filter — **structural, not vigilant**. Caveat: `billing/refund.py` SEV1 above is the one place this currently bites — refund opens an app-role session WITHOUT setting `creator_id`, so under prod RLS the refund silently no-ops. Fix is mechanical (switch to `AdminSessionLocal()`). *(was ⚠️)* |
| E Backpressure | ⚠️ | Issue 76 closed the four prior E SEV2s (`Retry-After` honored on 429 — `analytics.py:58-60`, `data_api.py:101-103`; ffmpeg timeout — `ingest.py:62-69`; SDK transcription timeout — `transcribe.py:105,174`; streaming upload — `transcribe.py:101-107`). New: oauth.py Redis-down → unhandled 500 (SEV2), Anthropic stream mid-interrupt has no first-class terminal emit (SEV2), analytics retention purge still open (SEV2). |
| F Rate limit / quota | ⚠️ | Quota TZ FIXED (`youtube/quota.py:56-57` now keys by `ZoneInfo("America/Los_Angeles")` with regression test). slowapi per-creator + spend gate verified. New: aacquire_slot TTL drift (SEV1 cap-bypass — concurrency-limit shape rather than rate-limit but same axis). Not load-tested. |
| G Observability | ✅ | request-id ContextVar + ASGI middleware + JSON logs (no PII/token in any new emit), Prometheus golden signals at `/metrics` now bearer-token-gated + prod fail-safe (`config.py:142-147`, `main.py:110-123` — prior `/metrics` SEV2 closed). API→Celery correlation via signals (`weak=False`). Residual SEV2: ContextVar correlation safe only under prefork (carry-forward). *(unchanged — but the prior /metrics SEV2 is now closed.)* |
| H Migration / pgvector | ✅ | HNSW index (0006) + FK indexes + partial UNIQUE on `confirmed` (0005) + `final` marker (0007) + RLS (0010) + pack_id width (0011) + creator_identity (0012) all verified at HEAD. All migrations use `CREATE INDEX CONCURRENTLY` in autocommit blocks. **Backups + PITR restore-test still unverified.** *(unchanged)* |
| I Secrets / deletion | ✅ | MultiFernet rotation + decrypt + typed `TokenDecryptError` (`crypto.py:13-43`); JWT identity (`auth.py:31-55`); account-deletion (revoke + purge — Issue 19/36); Stripe prod fail-fast (`config.py:85-98`); `/docs` off in prod; ALLOWED_ORIGINS locked; `ENV=production` gates. *(unchanged)* |

## Diff vs previous report (2026-05-29 re-run, commit `f6c73ee`)

**Fixed & verified this cycle (5 of the 4 prior SEV1s + 7 SEV2s closed):**
- ✅ SEV1 **`build_dna` concurrent double-pay** (worker/dna): Issue 76 — `pg_advisory_xact_lock(hashtext(creator_id))` in the same session that re-checks `build_job_id`, partial UNIQUE on `creator_dna.build_job_id`, IntegrityError-as-idempotent-no-op around commit. Verified at `worker/tasks.py:526-543, 621-630`.
- ✅ SEV1 **ingestion Deepgram whole-file buffering** (ingestion): Issue 76 — `_transcribe_deepgram` now passes the file handle to `client.listen.rest.v("1").transcribe_file(...)`; httpx streams the upload (`transcribe.py:101-107`). RAM ceiling closed.
- ✅ SEV1 **no SDK-native transcription timeout** (ingestion): Issue 76 — `_http_timeout()` constructs `httpx.Timeout(TRANSCRIPTION_HTTP_TIMEOUT_S, connect=10.0)`, passed to Deepgram + set on AssemblyAI module init (`transcribe.py:105, 174`).
- ✅ SEV1 **18 endpoints missing `response_model`** (routers): every endpoint now declares `*Out` except the new `/tasks/{task_id}/events` (SSE — intentionally exempted at `tests/test_response_models.py:23` with Issue 86 citation).
- ✅ SEV2 **clip_engine in-request candidate CPU**: now `asyncio.to_thread` wrapped (`ranking.py:115`, `scoring.py:161`).
- ✅ SEV2 **youtube oauth lock-wait stale identity-map read**: `populate_existing=True` on the re-read (`oauth.py:309`).
- ✅ SEV2 **youtube quota UTC vs PT**: `ZoneInfo("America/Los_Angeles")` (`quota.py:56-57`).
- ✅ SEV2 **youtube ingest.py no ffmpeg timeout**: `subprocess.run(..., timeout=settings.FFMPEG_EXTRACT_TIMEOUT_S)` (`ingest.py:62-69`).
- ✅ SEV2 **youtube 429 Retry-After ignored**: `retry_after_seconds(resp)` (`errors.py:17`) used at `analytics.py:58-60` + `data_api.py:101-103`.
- ✅ SEV2 **billing non-keyed grant swallowing IntegrityError**: scoped re-raise (`ledger.py:89-95`).
- ✅ SEV2 **dna `_enrich_video` N+1**: now batched 3 IN-queries (`builder.py:142-189`).
- ✅ SEV2 **dna `rank_videos` unbounded fetch**: bounded by `DNA_MAX_CANDIDATE_VIDEOS=500` (`builder.py:118-119`).
- ✅ SEV2 **preference `load_latest` re-deserialize on every rerank**: per-(creator,version) LRU in new `preference/_scorer_cache.py` (Issue 78a).
- ✅ SEV2 **`/metrics` unauthenticated** (_root_infra): bearer-token gate + prod fail-safe (`main.py:110-123`, `config.py:142-147`).
- ✅ SEV2 **improvement-brief 120s blocking** (improvement): Issue 78d — Celery 202/poll. Module now **clean**.

**New this run (Issue 86 surface + previously-unflagged hardening gaps):**
- 🆕 SEV1 worker/progress.py SSE concurrent-cap TTL drift (cap silently bypassed at steady-state).
- 🆕 SEV1 billing/refund.py opens app-role RLS-gated session without `creator_id` — latent until prod role split flips.
- 🆕 SEV2 worker/progress.py: 4 new findings on the loop-aware singleton (no aclose on rebind, churn on Redis failure, blocking-XREAD pool sizing, unschema'd emit payloads).
- 🆕 SEV2 worker/anthropic_stream.py: mid-stream interrupt has no first-class terminal `error` emit; silent-zero usage defaults hide future SDK schema drift.
- 🆕 SEV2 routers/tasks.py: 404/403 enumeration oracle; unvalidated `Last-Event-ID` → 500 on malformed reconnect.
- 🆕 SEV2 _root_infra Dockerfile: image runs as root; default CMD ships `uvicorn --reload`.
- 🆕 SEV2 worker/tasks.py: `_emit("error", message=str(exc))` raw-passes ValueError args into SSE stream (no leak today; structural concern).

**Carry-forward (unchanged this cycle — fix opportunities still open):**
- SEV2 clip_engine dna_match seed duplicate-feature, candidate overlap dedup.
- SEV2 worker `_render_clip_async` concurrent double-encode, `_ingest_async` re-do on redelivery, `poll_clip_outcomes` no quota break.
- SEV2 upload_intel `optimal_gap_hours` missing 75d guard.
- SEV2 youtube analytics retention purge (Issue 75b — needs Google staleness window confirmed).
- SEV2 _root_infra Celery ContextVar prefork-only safety.
- SEV2 routers unbounded list endpoints on `/videos` / `/clips` / `/upload-intel`.

**Counts (Layer 1, all modules):**

| Severity | This run (2026-05-30) | Prior (2026-05-29) |
|---|---|---|
| BLOCKER | 0 | 0 |
| SEV1 | 2 | 4 |
| SEV2 | 27 | 23 |
| cleanup | 28 | 24 |

Net SEV1 trend: **−2** (4 prior closed; 2 new — one is the Issue-86 surface and one is the latent RLS misalignment in refund). SEV2 trend: **+4** (12 prior closed; 16 new, mostly the surface area of Issue 86's new code plus two Dockerfile hardening gaps that were always there but uncovered this cycle).

## Top 5 actions, in order

1. **Fix `aacquire_slot` TTL drift** (`worker/progress.py:214-232`, SEV1). One-line: `EXPIRE` on every INCR, not just count==1. Add an integration test that simulates a 1h-running stream + verifies the cap still holds after TTL renewal. **The only SEV1 that an attacker (or buggy client) can exploit today; prioritize.**
2. **Switch `refund_for_video` to `AdminSessionLocal()`** (`billing/refund.py:41`, SEV1). One-line change. Add a regression test running as the `creatorclip_app` role with RLS enforced to assert the refund actually credits. Land **before** the prod role split flips — flagged in CLAUDE.md as pending.
3. **Collapse the SSE 404/403 enumeration oracle** (`routers/tasks.py:131-138`, SEV2) **and** validate `Last-Event-ID` against `^\d+-\d+$` before XREAD (`routers/tasks.py:140` + `worker/progress.py:204`, SEV2). Two-line and ~five-line fixes; both directly hardening the newly-shipped SSE surface.
4. **Dockerfile production hardening** (`Dockerfile:1-34, :34`, two SEV2s). Add the non-root `app` user + flip the default CMD to `gunicorn -k uvicorn.workers.UvicornWorker -w 4`. Defense-in-depth that any pre-public-launch checklist would require; cheap to do now while Issue 86's deploy is still warm.
5. **Run the Locust-behind-PgBouncer load test** (`tests/perf/`). This is the **only** way to convert scale axes A/B/C/E/F from ⚠️ to ✅ — no amount of code reading can confirm the pool math holds, the SSE Redis pool doesn't exhaust at target concurrency, or backpressure degrades gracefully under contention. This is the gating action between **CONDITIONAL** and **YES**.

---

## Targeted audit (2026-05-30): display-vs-filter mismatches (post Issue 88)

Issue 88 closed a SEV-0 in which `youtube/analytics.py::check_data_gate` counted every Video row while `dna/builder.py::rank_videos` required metrics — gate said "ready", build said "0/0". A focused subagent audit looked for the same shape on three other surfaces (clip generation, review feedback, billing balance) plus the just-fixed data-gate path. Four findings, all the same shape; all promoted to `docs/issues.md` (89, 90, 91) or fixed inline with this commit.

| # | Title | Severity | Where | User-visible symptom | Status |
|---|-------|----------|-------|----------------------|--------|
| 1 | Data-gate `ready` uses AND, builder accepts OR | SEV-1 | `youtube/analytics.py:324`, `dna/builder.py:241` | Long-only or shorts-only creator sees "not ready" but build would succeed | ✅ Fixed inline with Issue 88 |
| 2 | Balance pre-check is `>0`, deduction needs `>= video_minutes` | SEV-1 | `billing/ledger.py:173,144`, `routers/videos.py:163`, `routers/clips.py:139` | Low-balance creator uploads a 60-min video, sees "Uploaded — processing…", then silent `failed` in DB | Spawned **Issue 89** |
| 3 | Catalog-synced videos pollute `/videos` list | SEV-2 | `routers/videos.py:60`, `routers/clips.py:75` | Dashboard shows "247 videos, all pending" after Issue 87 catalog sync; polling loop hammers `/status` for rows that will never transition | Spawned **Issue 90** |
| 4 | "Clips ready" counter ignores `render_status` | SEV-2 | `static/index.html:196`, `static/review.html:154` | Dashboard says "12 clips ready", reviewer scrolls past 12 placeholders showing "(not yet rendered)" | Spawned **Issue 91** |

Summary: 2 SEV-1s (one fixed, one filed), 2 SEV-2s (both filed), 0 SEV-0s. All four share the failure mode "count/boolean shown to user is computed from a broader predicate than the consumer enforces" — exactly the shape the Issue 88 fix codified as a class. The pattern likely recurs as the codebase grows; the `log_event` + diagnostic logging shipped with Issue 88 means the next instance is one log line away from the answer instead of a code-bisect.
