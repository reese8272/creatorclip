# improvement — assessed 2026-06-02

## Findings

- [BLOCKER resolved] routers/improvement.py:109-128 + models.py:669-700 — `UNIQUE(creator_id)` constraint on `improvement_briefs` table now present (models.py __table_args__, migration 0016_improvement_brief_unique.py verified). Router correctly wraps insert in try/except IntegrityError (lines 114-128) to catch concurrent first-insert race and return existing row. Regression test test_improvement_brief_async.py::test_post_debounces_when_already_pending validates. **Status: CLOSED Wave-9.**

- [SEV2] improvement/brief.py:93 — `web_search` tool registered without `max_uses` → unbounded billed search fan-out per brief. At hundreds-of-creators scale (e.g. 200 concurrent briefs × 5-10 searches each), this is a real cost amplifier (~$10-20/brief cycle on web_search billing). Rubric §5 (Anthropic SDK). **Status: OPEN carry-forward since Wave-4.** | fix: (1) add `ANTHROPIC_WEB_SEARCH_MAX_USES: int = 5` to config.py with env default + .env.example; (2) pass `"max_uses": settings.ANTHROPIC_WEB_SEARCH_MAX_USES` in tool dict at line 93 (3-4 LOC).

- [SEV2] improvement/brief.py:161-167 (and streaming path 139-147) — `tool_choice` not set on either call path (defaults `auto`), so model can skip `web_search` entirely. The brief's value proposition ("cite CURRENT YouTube algorithm factor") is silently weakened when this happens — a hallucinated or stale recommendation with no search grounding. Rubric §5. **Status: OPEN carry-forward since Wave-4.** | fix: (1) add `tool_choice={"type": "tool", "name": "web_search"}` (force at least one search) to `.messages.create()` call at line 161 and to `stream_and_emit()` call at line 139-147; (2) plumb `tool_choice` through worker/anthropic_stream.py as new kwarg (lines 44, 69); (3) add regression test: mock Claude response with at least one `tool_use` block, verify output contains grounding cite (5-10 LOC).

- [SEV2] improvement/brief.py:7-8, 58-60, 80-89 — docstring states "static prefix is below Sonnet 4.6's minimum cacheable size, so cache does not engage" yet line 88 marks the prefix with `cache_control: {"type": "ephemeral"}`. This causes a 1.25× write premium (cache write overhead) on a hit rate of ~0%, making every brief request *more* expensive than not caching. Rubric §5. **Status: OPEN carry-forward since Wave-4.** | fix: either (a) drop `cache_control` marker at line 88 entirely (prefix grows naturally, cache engages eventually at ~1024+ tokens), or (b) pad `_SYSTEM_INSTRUCTIONS` to push prefix past ~1024-token floor — mirror dna/brief.py's longer preamble by adding examples. Option (a) preferred (1-3 LOC). Update DECISIONS.md rationale.

- [cleanup] improvement/brief.py:134 — in-function import `from worker.anthropic_stream import stream_and_emit` lacks a "why" comment. Import IS load-bearing (avoids circular import with worker.tasks). Rubric §6. **Status: OPEN carry-forward since Wave-4.** | fix: add one-line comment above line 134: `# Local import avoids circular dependency with worker.tasks`.

## Rubric coverage

| Category | Status |
|---|---|
| 1 Resource lifecycle | **clean** — `_ANTHROPIC` is module-level singleton (lines 25-29) with timeout=60s + max_retries=2; `.with_options(timeout=120.0)` returns derived view, no rebuild. DB sessions via context manager (router GET/POST via Depends(get_session)). AsyncSessionLocal() in worker task with proper `async with` (tasks.py:1690). Row insert committed (line 115) before Celery enqueue (line 147). No file handles/subprocesses. |
| 2 Concurrency & scale | **clean** — UNIQUE(creator_id) constraint present + enforced (models.py __table_args__). IntegrityError catch on first insert (lines 114-128) correctly resolves concurrent-first-POST race. SELECT FOR UPDATE SKIP LOCKED (lines 86-92) guards existing-row re-POST. Per-creator isolation on all queries verified: routers 64, 88, 98; tasks.py 1671, 1724. No event-loop blocking. But: 3 SEV2s (unbounded web_search, no tool_choice, inert cache) are concurrency/cost concerns at scale. |
| 3 Security & compliance | **clean** — honesty disclaimer enforced in Python (lines 31-35) and appended on BOTH paths (line 158 streaming, line 183 non-streaming), never delegated to model. Token logging emits ONLY integer counts (lines 148-154, 169-175) — no channel title, creator id, analytics body. No OAuth/PII in scope. `_SYSTEM_INSTRUCTIONS` explicitly states "never promise virality." SQL parameterized via SQLAlchemy. |
| 4 Clip-quality | **n/a** — not a clip module. |
| 5 Anthropic SDK | **3 SEV2 OPEN** (no max_uses, no tool_choice, inert cache breakpoint at ~192 tokens). Token logging present: `in/cached_read/cached_creation/out` on both paths (4-field, correct). `max_tokens=2000` set on both. `web_search_20260209` is current GA tool. Cache breakpoint placed correctly per 2026 pattern — issue is only prefix staying below ~1024-token floor. Wave-3 SEV1 (streaming dropped tools) CLOSED — verified: brief.py:146 + anthropic_stream.py:69-76 both pass `tools=tools` when not None. |
| 6 Cleanliness & typing | **1 cleanup OPEN** (undocumented in-function import line 134). Prior cleanups CLOSED: analytics → `Mapping[str, object]` (line 65), magic 1000 → `_DNA_BRIEF_MAX_CHARS` (line 60). Bare `tuple` return type on _build_request:67 justified (anthropic 0.40 SDK stubs incompatible with full TypedDict, defer to Issue 84 SDK bump). |
| 7 Error handling | **clean** — brief.py not a router. `RuntimeError` on empty Claude response (line 179) appropriate for Celery context, mapped to safe message by worker (tasks.py:1786). Router uses Pydantic response models (BriefQueuedOut, ImprovementBriefOut); HTTP 202 on queue, 200 on GET. Error details safe (generic "Brief generation failed — try again.", no stack/PII). |
| 8 Config & paths | **clean** — ANTHROPIC_API_KEY, ANTHROPIC_MODEL, ANTHROPIC_WEB_SEARCH_TOOL flow through config.settings (pydantic-settings, fail-fast). No paths in module. SEV2 fixes would add ANTHROPIC_WEB_SEARCH_MAX_USES to config + .env.example (not present yet). |

## Module verdict

**NEEDS-WORK — 3 SEV2 carry-forwards, no BLOCKER.** The concurrent-first-insert race (prior BLOCKER) is now closed with UNIQUE constraint + IntegrityError handling. The 3 remaining SEV2s are cost/grounding defects: unbounded web_search calls can multiply billing, missing tool_choice weakens grounding quality, inert cache marker wastes tokens. All have concrete ~5-10 LOC fixes. The 1 cleanup is low-priority. Recommend bundling the 3 SEV2 fixes into one 20 LOC PR (add max_uses config, plumb tool_choice through both paths + stream_and_emit, drop cache_control marker, add regression test).

